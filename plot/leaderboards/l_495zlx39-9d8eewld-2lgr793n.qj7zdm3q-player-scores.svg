<svg xmlns="http://www.w3.org/2000/svg" lang="en">
	<title>Score by player for leaderboard l_495zlx39-9d8eewld-2lgr793n.qj7zdm3q</title>
	<metadata>
		<meta xmlns="http://www.w3.org/1999/xhtml" charset="utf-8" />
		<meta xmlns="http://www.w3.org/1999/xhtml" name="viewport" content="width=device-width" />
		<meta xmlns="http://www.w3.org/1999/xhtml" name="color-scheme" content="dark light" />
	</metadata>
	<style>
		*,
		::before,
		::after {
			box-sizing: border-box;
		}
		:root {
			background: var(--canvas-background);
			color: var(--canvas-foreground);
			fill: var(--canvas-background);
			stroke: var(--canvas-foreground);
			stroke-linejoin: round;
			stroke-linecap: round;
		}
		foreignObject {
			pointer-events: none;
		}
		foreignObject::before,
		foreignObject::after {
			content: "";
			position: absolute;
			z-index: 1;
			left: 20px;
			bottom: 20px;
			pointer-events: auto;
		}
		foreignObject::before {
			width: calc(100% - 40px);
			height: calc(20px + var(--date-size) * 1px);
		}
		foreignObject::after {
			width: calc(20px + var(--value-size) * 1px);
			height: calc(100% - 40px);
		}
		foreignObject &gt; div {
			position: absolute;
			z-index: 4;
			right: 0;
			top: 0;
			margin: 20px;
			border: 1px solid var(--canvas-foreground);
			background: var(--highlighted);
			font: 16px / 1.25 serif;
			transition: opacity 1s;
			pointer-events: auto;
		}
		foreignObject:is(:hover, :focus-within) &gt; div:not(:hover, :focus-within) {
			opacity: 0;
			pointer-events: none;
		}
		foreignObject &gt; div &gt; p {
			margin: 0;
		}
		label {
			display: table;
			border-collapse: separate;
			border-spacing: 10px;
			direction: rtl;
		}
		progress,
		input[type="checkbox"] {
			display: table-cell;
			width: 16px;
			height: 16px;
			margin: 2px;
			accent-color: var(--canvas-foreground);
			vertical-align: top;
			direction: ltr;
		}
		progress + span,
		input[type="checkbox"] + span {
			display: table-cell;
			vertical-align: top;
			direction: ltr;
		}
		input[type="checkbox"]:not(:disabled):is(:hover, :focus-within) + span {
			text-decoration: underline;
		}
		foreignObject &gt; svg:first-of-type,
		foreignObject &gt; svg:first-of-type + svg,
		foreignObject &gt; svg:first-of-type + svg + svg,
		foreignObject &gt; svg:first-of-type + svg + svg + svg {
			position: absolute;
			z-index: 2;
			margin: 0;
			padding: 10px;
			background: var(--highlighted);
			font: 16px / 1.25 serif;
			font-weight: bold;
			pointer-events: auto;
		}
		foreignObject &gt; svg:first-of-type,
		foreignObject &gt; svg:first-of-type + svg + svg {
			border-top: 1px solid #0000;
			border-bottom: 1px solid var(--canvas-foreground);
		}
		foreignObject &gt; svg:first-of-type + svg,
		foreignObject &gt; svg:first-of-type + svg + svg + svg {
			border-top: 1px dashed var(--canvas-foreground);
			border-bottom: 1px dashed #0000;
		}
		foreignObject &gt; svg:first-of-type,
		foreignObject &gt; svg:first-of-type + svg {
			width: calc(20px + var(--date-size) * 1px);
			height: 42px;
			bottom: 20px;
		}
		foreignObject &gt; svg:first-of-type {
			left: calc(40px + var(--value-size) * 1px);
			transform: rotate(90deg) translate(-100%, 0);
			transform-origin: left bottom;
		}
		foreignObject &gt; svg:first-of-type + svg {
			right: 20px;
			transform: rotate(90deg) translate(0, 100%);
			transform-origin: right bottom;
		}
		foreignObject &gt; svg:first-of-type + svg + svg,
		foreignObject &gt; svg:first-of-type + svg + svg + svg {
			width: calc(20px + var(--value-size) * 1px);
			height: 42px;
			left: 20px;
		}
		foreignObject &gt; svg:first-of-type + svg + svg {
			bottom: calc(40px + var(--date-size) * 1px);
		}
		foreignObject &gt; svg:first-of-type + svg + svg + svg {
			top: 20px;
		}
		foreignObject &gt; svg:first-of-type &gt; text,
		foreignObject &gt; svg:first-of-type + svg &gt; text,
		foreignObject &gt; svg:first-of-type + svg + svg &gt; text,
		foreignObject &gt; svg:first-of-type + svg + svg + svg &gt; text {
			fill: var(--canvas-foreground);
			stroke: none;
		}
		foreignObject &gt; svg:first-of-type &gt; text,
		foreignObject &gt; svg:first-of-type + svg &gt; text {
			cursor: vertical-text;
		}
		foreignObject &gt; svg:first-of-type + svg + svg + svg + svg {
			position: relative;
			z-index: 3;
			width: calc(100% - 60px - var(--value-size) * 1px);
			height: calc(100% - 60px - var(--date-size) * 1px);
			margin: 20px 20px calc(40px + var(--date-size) * 1px) calc(40px + var(--value-size) * 1px);
			border-left: 1px solid var(--canvas-foreground);
			border-right: 1px dashed var(--canvas-foreground);
			border-top: 1px dashed var(--canvas-foreground);
			border-bottom: 1px solid var(--canvas-foreground);
			background: var(--canvas-background);
			pointer-events: auto;
		}
		g &gt; rect {
			vector-effect: non-scaling-stroke;
			fill: var(--highlighted);
			stroke: none;
		}
		g &gt; rect[tabindex] {
			fill: var(--tier, #0000);
			cursor: help;
		}
		g &gt; rect[tabindex][data-tier="diamond"] {
			--tier: var(--diamond);
		}
		g &gt; rect[tabindex][data-tier="gold"] {
			--tier: var(--gold);
		}
		g &gt; rect[tabindex][data-tier="silver"] {
			--tier: var(--silver);
		}
		g &gt; rect[tabindex][data-tier="bronze"] {
			--tier: var(--bronze);
		}
		g &gt; path[tabindex] {
			vector-effect: non-scaling-stroke;
			fill: none;
			stroke: hsl(calc(var(--index) / var(--count) * 360), 50%, 50%);
			cursor: help;
		}
		g:is(:hover, :focus-within) &gt; path[tabindex] {
			stroke: var(--canvas-foreground);
		}
		g &gt; path[tabindex]:is(:hover, :focus-within) {
			outline: 0;
		}
		g &gt; path[tabindex]:first-of-type {
			stroke-width: 2;
		}
		g &gt; path[tabindex]:first-of-type:is(:hover, :focus-within) {
			stroke-width: 4;
		}
		g &gt; path[tabindex]:first-of-type:not(:last-of-type) {
			stroke-dasharray: 4 8;
		}
		g &gt; path[tabindex]:first-of-type + rect {
			fill: none;
		}
		g &gt; path[tabindex]:first-of-type:is(:hover, :focus-within) + rect {
			fill: var(--highlighted);
			pointer-events: none;
		}
		g &gt; path[tabindex]:first-of-type ~ path[tabindex] {
			stroke-width: 8;
		}
		g &gt; path[tabindex]:first-of-type ~ path[tabindex]:is(:hover, :focus-within) {
			stroke-width: 16;
		}
		g &gt; path[tabindex]:first-of-type ~ path[tabindex] + line,
		g &gt; path[tabindex]:first-of-type ~ path[tabindex] + line + line {
			vector-effect: non-scaling-stroke;
			fill: none;
			stroke: none;
			stroke-width: 2;
			stroke-dasharray: 4 8;
			pointer-events: none;
		}
		g &gt; path[tabindex]:first-of-type:is(:hover, :focus-within) + rect + path[tabindex] + line,
		g &gt; path[tabindex]:first-of-type:is(:hover, :focus-within) + rect + path[tabindex] + line + line,
		g &gt; path[tabindex]:first-of-type:is(:hover, :focus-within) ~ path[tabindex]:last-of-type + line,
		g &gt; path[tabindex]:first-of-type:is(:hover, :focus-within) ~ path[tabindex]:last-of-type + line + line,
		g &gt; path[tabindex]:first-of-type ~ path[tabindex]:is(:hover, :focus-within) + line,
		g &gt; path[tabindex]:first-of-type ~ path[tabindex]:is(:hover, :focus-within) + line + line {
			stroke: var(--faded);
		}
		@media (prefers-color-scheme: dark) {
			:root {
				--canvas-background: #000;
				--canvas-foreground: #ccc;
				--diamond: #0363;
				--gold: #6303;
				--silver: #3633;
				--bronze: #6363;
				--highlighted: #6663;
				--faded: #9993;
			}
		}
		@media (prefers-color-scheme: light) {
			:root {
				--canvas-background: #fff;
				--canvas-foreground: #333;
				--diamond: #9cf3;
				--gold: #fc93;
				--silver: #9c93;
				--bronze: #c9c3;
				--highlighted: #9993;
				--faded: #6663;
			}
		}
	</style>
	<script>
		"use strict";
		{
			const script = document.currentScript;
			const documentUrl = location.href;
			const scope = new URL("../../", documentUrl).pathname;
			const playersKey = `${scope}#players`;
			const leaderboardsKey = `${scope}#leaderboards`;
			const sessionKey = `${scope}#session`;
			const lockKey = `${scope}#lock`;
			async function importDataScript(dataScriptUrl, signal) {
				signal.throwIfAborted();
				if (documentUrl.protocol !== "file:") {
					const response = await fetch(dataScriptUrl, {
						signal,
					});
					if (!response.ok) {
						throw new Error(response.statusText);
					}
					const data = await response.json();
					return data;
				}
				return await new Promise((resolve, reject) =&gt; {
					const name = `callback${Math.random() * 2 ** 53}`;
					let called = false;
					let data = null;
					function callback(value) {
						if (!called) {
							called = true;
							data = value;
						}
					}
					window[name] = callback;
					const dataScript = document.createElementNS("http://www.w3.org/2000/svg", "script");
					dataScript.href = `${dataScriptUrl}&amp;callback=${name}`;
					function handleAbort(event) {
						delete window[name];
						dataScript.removeEventListener("error", handleError);
						dataScript.removeEventListener("load", handleLoad);
						dataScript.remove();
						const reason = signal.reason;
						reject(reason);
					}
					function handleError(event) {
						delete window[name];
						signal.removeEventListener("abort", handleAbort);
						dataScript.removeEventListener("load", handleLoad);
						dataScript.remove();
						const error = event.error;
						reject(error);
					}
					function handleLoad(event) {
						delete window[name];
						signal.removeEventListener("abort", handleAbort);
						dataScript.removeEventListener("error", handleError);
						dataScript.remove();
						resolve(data);
					}
					signal.addEventListener("abort", handleAbort, {
						once: true,
					});
					dataScript.addEventListener("error", handleError, {
						once: true,
					});
					dataScript.addEventListener("load", handleLoad, {
						once: true,
					});
					script.after(dataScript);
				});
			}
			async function waitForTimeout(delay, signal) {
				signal.throwIfAborted();
				return await new Promise((resolve, reject) =&gt; {
					let timeout = null;
					function handleAbort() {
						clearTimeout(timeout);
						const reason = signal.reason;
						reject(reason);
					}
					function handleTimeout() {
						signal.removeEventListener("abort", handleAbort);
						resolve(delay);
					}
					signal.addEventListener("abort", handleAbort, {
						once: true,
					});
					timeout = setTimeout(handleTimeout, delay);
				});
			}
			window.addEventListener("pageshow", (event) =&gt; {
				if (event.persisted) {
					console.log(`Successfully restored page from cache`);
				} else {
					console.log(`Unsuccessfully restored page from cache`);
				}
			});
			window.addEventListener("pagehide", (event) =&gt; {
				if (event.persisted) {
					console.log(`Successfully saved page to cache`);
				} else {
					console.log(`Unsuccessfully saved page to cache`);
				}
			});
			window.addEventListener("pageshow", async (event) =&gt; {
				const rootTitleElement = document.querySelector("title");
				const rootTitleContent = rootTitleElement.textContent;
				const pathTitleElements = Array.from(document.querySelectorAll("g &gt; path[tabindex]:first-of-type &gt; title"));
				const pathTitleContents = pathTitleElements.map((pathTitleElement) =&gt; {
					const pathTitleContent = pathTitleElement.textContent;
					return pathTitleContent;
				});
				const foreignObject = document.querySelector("foreignObject");
				const div = document.createElementNS("http://www.w3.org/1999/xhtml", "div");
				div.dir = "ltr";
				const p = document.createElementNS("http://www.w3.org/1999/xhtml", "p");
				const label = document.createElementNS("http://www.w3.org/1999/xhtml", "label");
				const progress = document.createElementNS("http://www.w3.org/1999/xhtml", "progress");
				label.append(progress);
				const span = document.createElementNS("http://www.w3.org/1999/xhtml", "span");
				span.textContent = "Loading names";
				label.append(span);
				p.append(label);
				div.append(p);
				foreignObject.prepend(div);
				let lock = false;
				let session = null;
				const broadcastChannel = new BroadcastChannel(sessionKey);
				const abortController = new AbortController();
				const signal = abortController.signal;
				const abort = abortController.abort.bind(abortController);
				const {promise, resolve} = Promise.withResolvers();
				broadcastChannel.addEventListener("message", (event) =&gt; {
					const data = event.data;
					const type = data.type;
					switch (type) {
						case "request": {
							if (lock &amp;&amp; signal.aborted) {
								broadcastChannel.postMessage({
									type: "response",
									session,
								});
							}
							break;
						}
						case "response": {
							if (!signal.aborted) {
								session = data.session;
								if (session != null) {
									const {players, leaderboards} = session;
									try {
										const cachedPlayers = JSON.stringify(players);
										const cachedLeaderboards = JSON.stringify(leaderboards);
										sessionStorage.setItem(playersKey, cachedPlayers);
										sessionStorage.setItem(leaderboardsKey, cachedLeaderboards);
									} catch {}
									console.log(`Successfully loaded names from peer`);
									abort();
									resolve();
								} else {
									console.log(`Unsuccessfully loaded names from peer`);
									abort();
									resolve();
								}
							}
							break;
						}
					}
				});
				try {
					const cachedPlayers = sessionStorage.getItem(playersKey);
					const cachedLeaderboards = sessionStorage.getItem(leaderboardsKey);
					if (cachedPlayers == null || cachedLeaderboards == null) {
						throw new Error();
					}
					const players = JSON.parse(cachedPlayers);
					const leaderboards = JSON.parse(cachedLeaderboards);
					session = {players, leaderboards};
				} catch {}
				if (session != null) {
					broadcastChannel.postMessage({
						type: "response",
						session,
					});
					console.log(`Successfully loaded names from local`);
					abort();
					resolve();
				} else {
					broadcastChannel.postMessage({
						type: "request",
					});
					console.log(`Unsuccessfully loaded names from local`);
				}
				(async () =&gt; {
					for (;;) {
						await navigator.locks.request(lockKey, async () =&gt; {
							lock = true;
							console.log(`Acquired lock`);
							if (!signal.aborted) {
								try {
									const players = Object.create(null);
									const leaderboards = Object.create(null);
									const games = ["9d3rrxyd", "w6jl2ned"];
									for (const gameId of games) {
										const {data} = await importDataScript(`https://www.speedrun.com/api/v1/games/${gameId}?embed=categories,levels,variables`, signal);
										const versions = data.variables.data.find((variable) =&gt; {
											return variable.name === "Version";
										});
										const levels = Object.fromEntries(data.levels.data.map((level) =&gt; {
											return [level.id, level];
										}));
										const categories = Object.fromEntries(data.categories.data.map((category) =&gt; {
											return [category.id, category];
										}));
										const variables = Object.fromEntries(data.variables.data.map((variable) =&gt; {
											return [variable.id, variable];
										}));
										console.log(`Got game`);
										await waitForTimeout(800, signal);
										const slice = 200;
										for (let offset = 0;; offset += slice) {
											const {data, pagination} = await importDataScript(`https://www.speedrun.com/api/v1/runs?game=${gameId}&amp;orderby=date&amp;direction=asc&amp;embed=players&amp;offset=${offset}&amp;max=${slice}`, signal);
											const {size} = pagination;
											if (size === 0) {
												break;
											}
											for (const run of data) {
												const status = run.status.status;
												if (status == null || status === "new") {
													continue;
												}
												const anonymous = (run.players.data[0]?.rel ?? "guest") !== "user";
												const player = !anonymous ? run.players.data[0].id : "814p2558";
												const playerName = !anonymous ? run.players.data[0].names.international : "anonymous";
												players[player] ??= playerName;
												const level = run.level;
												const category = run.category;
												const values = Object.entries(run.values).filter(([variable]) =&gt; {
													return variables[variable]?.["is-subcategory"] ?? false;
												}).map(([variable, value]) =&gt; {
													return `-${variable}.${value}`;
												});
												const leaderboard = `${level != null ? `l_${level}-` : ""}${category}${values.join("")}`;
												const levelName = run.level != null ? levels[run.level]?.name ?? null : null;
												const categoryName = categories[run.category]?.name ?? null;
												const valueNames = Object.entries(run.values).filter(([variable]) =&gt; {
													return variables[variable]?.["is-subcategory"] ?? false;
												}).map(([variable, value]) =&gt; {
													return variables[variable]?.values.values[value]?.label ?? null;
												}).filter((valueName) =&gt; {
													return valueName != null;
												});
												const leaderboardName = `${levelName != null ? `${levelName}: ` : ""}${categoryName ?? ""}${values.length !== 0 ? ` - ${valueNames.join(", ")}` : ""}`;
												leaderboards[leaderboard] ??= leaderboardName;
											}
											console.log(`Got runs ${offset}-${offset + size - 1}`);
											await waitForTimeout(800, signal);
										}
									}
									session = {players, leaderboards};
								} catch {}
								if (!signal.aborted) {
									if (session != null) {
										const {players, leaderboards} = session;
										try {
											const cachedPlayers = JSON.stringify(players);
											const cachedLeaderboards = JSON.stringify(leaderboards);
											sessionStorage.setItem(playersKey, cachedPlayers);
											sessionStorage.setItem(leaderboardsKey, cachedLeaderboards);
										} catch {}
										broadcastChannel.postMessage({
											type: "response",
											session,
										});
										console.log(`Successfully loaded names from remote`);
										abort();
										resolve();
									} else {
										broadcastChannel.postMessage({
											type: "response",
											session,
										});
										console.log(`Unsuccessfully loaded names from remote`);
										abort();
										resolve();
									}
								}
							}
							await new Promise((resolve) =&gt; {
								window.addEventListener("beforeunload", () =&gt; {
									resolve();
								}, {
									once: true,
								});
							});
						});
						lock = false;
						console.log(`Released lock`);
						await new Promise((resolve) =&gt; {
							window.addEventListener("pagehide", () =&gt; {
								resolve();
							}, {
								once: true,
							});
						});
						await new Promise((resolve) =&gt;{
							window.addEventListener("pageshow", () =&gt; {
								resolve();
							}, {
								once: true,
							});
						});
					}
				})();
				await promise;
				if (session == null) {
					const input = document.createElementNS("http://www.w3.org/1999/xhtml", "input");
					input.type = "checkbox";
					input.autocomplete = "off";
					input.disabled = true;
					input.indeterminate = true;
					progress.replaceWith(input);
					span.textContent = "Unavailable names";
					return;
				}
				const {players, leaderboards} = session;
				const hasPlayerRoot = rootTitleContent.includes(" for player");
				const hasLeaderboardRoot = rootTitleContent.includes(" for leaderboard");
				const rootTitle = {
					element: rootTitleElement,
					contentWithIds: rootTitleContent,
					contentWithNames: hasPlayerRoot ? rootTitleContent.replace(/ for player ([0-9_a-z]+)/, ($0, $1) =&gt; {
						return ` for player ${players[$1] ?? $1}`;
					}) : hasLeaderboardRoot ? rootTitleContent.replace(/ for leaderboard ((?:l_[0-9_a-z]+-)?[0-9_a-z]+(-[0-9_a-z]+\.[0-9_a-z]+)*)/, ($0, $1) =&gt; {
						return ` for leaderboard ${leaderboards[$1] ?? $1}`;
					}) : rootTitleContent,
				};
				const hasPlayerPaths = rootTitleContent.includes(" by player");
				const hasLeaderboardPaths = rootTitleContent.includes(" by leaderboard");
				const pathTitles = pathTitleElements.map((pathTitleElement, k) =&gt; {
					const pathTitleContent = pathTitleContents[k];
					const pathTitle = {
						element: pathTitleElement,
						contentWithIds: pathTitleContent,
						contentWithNames: hasPlayerPaths ? players[pathTitleContent] ?? pathTitleContent : hasLeaderboardPaths ? leaderboards[pathTitleContent] ?? pathTitleContent : pathTitleContent,
					};
					return pathTitle;
				});
				const input = document.createElementNS("http://www.w3.org/1999/xhtml", "input");
				input.type = "checkbox";
				input.autocomplete = "off";
				input.addEventListener("change", () =&gt; {
					input.disabled = true;
					if (input.checked) {
						rootTitle.element.textContent = rootTitle.contentWithNames;
						for (const pathTitle of pathTitles) {
							pathTitle.element.textContent = pathTitle.contentWithNames;
						}
					} else {
						rootTitle.element.textContent = rootTitle.contentWithIds;
						for (const pathTitle of pathTitles) {
							pathTitle.element.textContent = pathTitle.contentWithIds;
						}
					}
					input.disabled = false;
				});
				progress.replaceWith(input);
				span.textContent = "Reveal names";
			}, {
				once: true,
			});
		}
	</script>
	<script>
		"use strict";
		{
			document.addEventListener("DOMContentLoaded", (event) =&gt; {
				const minDateText = document.querySelector("foreignObject &gt; svg:first-of-type &gt; text");
				const maxDateText = document.querySelector("foreignObject &gt; svg:first-of-type + svg &gt; text");
				const startX = Math.round(Date.parse(minDateText.textContent) / 86400000);
				function xToDate(x) {
					const datetime = new Date((startX + x) * 86400000);
					const year = `${datetime.getUTCFullYear()}`.padStart(4, "0");
					const month = `${datetime.getUTCMonth() + 1}`.padStart(2, "0");
					const day = `${datetime.getUTCDate()}`.padStart(2, "0");
					const date = `${year}-${month}-${day}`;
					const size = date.length * 10;
					return {date, size};
				}
				function setMinDate(x) {
					const {date, size} = xToDate(Math.floor(x));
					minDateText.textLength.baseVal.value = size;
					minDateText.textContent = date;
				}
				function setMaxDate(x) {
					const {date, size} = xToDate(Math.ceil(x));
					maxDateText.textLength.baseVal.value = size;
					maxDateText.textContent = date;
				}
				const minValueText = document.querySelector("foreignObject &gt; svg:first-of-type + svg + svg &gt; text");
				const maxValueText = document.querySelector("foreignObject &gt; svg:first-of-type + svg + svg + svg &gt; text");
				const timed = minValueText.textContent.includes(":");
				function yToValue(y) {
					if (timed) {
						const minutes = `${(y - y % 6000) / 6000}`.padStart(2, "0");
						const seconds = `${(y - y % 100) / 100 % 60}`.padStart(2, "0");
						const centiseconds = `${y % 100}`.padStart(2, "0");
						const value = `${minutes}:${seconds}.${centiseconds}`;
						const size = value.length * 10;
						return {value, size};
					} else {
						const value = `${y}`;
						const size = value.length * 10;
						return {value, size};
					}
				}
				function setMinValue(y) {
					const {value, size} = yToValue(Math.floor(y));
					minValueText.textLength.baseVal.value = size;
					minValueText.textContent = value;
				}
				function setMaxValue(y) {
					const {value, size} = yToValue(Math.ceil(y));
					maxValueText.textLength.baseVal.value = size;
					maxValueText.textContent = value;
				}
				const svg = document.querySelector("foreignObject &gt; svg:first-of-type + svg + svg + svg + svg");
				svg.tabIndex = -1;
				svg.style.setProperty("outline", "0");
				if (CSS.supports("overflow-clip-margin", "8px")) {
					svg.style.setProperty("overflow", "clip");
					svg.style.setProperty("overflow-clip-margin", "8px");
				}
				svg.style.setProperty("touch-action", "none");
				const {x: baseX, y: baseY, width: baseWidth, height: baseHeight} = svg.viewBox.baseVal;
				let pointerCount = 0;
				const pointers = Object.create(null);
				const xPointerIds = [];
				const yPointerIds = [];
				let x = null;
				let y = null;
				let width = null;
				let height = null;
				svg.addEventListener("pointerdown", (event) =&gt; {
					const {pointerId} = event;
					if (pointerId in pointers) {
						return;
					}
					let {x: animX, y: animY, width: animWidth, height: animHeight} = svg.viewBox.baseVal;
					svg.setPointerCapture(pointerId);
					++pointerCount;
					const offsetX = event.offsetX;
					const offsetY = event.offsetY;
					pointers[pointerId] = {
						offsetX,
						offsetY,
					};
					xPointerIds.splice(xPointerIds.findLastIndex((pointerId) =&gt; {
						return pointers[pointerId].offsetX &lt;= offsetX;
					}) + 1, 0, pointerId);
					yPointerIds.splice(yPointerIds.findLastIndex((pointerId) =&gt; {
						return pointers[pointerId].offsetY &lt;= offsetY;
					}) + 1, 0, pointerId);
					const minOffsetX = pointers[xPointerIds[0]].offsetX;
					const minOffsetY = pointers[yPointerIds[0]].offsetY;
					const maxClientWidth = pointers[xPointerIds[pointerCount - 1]].offsetX - minOffsetX;
					const maxClientHeight = pointers[yPointerIds[pointerCount - 1]].offsetY - minOffsetY;
					width = Math.min(maxClientWidth / svg.clientWidth * animWidth, baseWidth);
					height = Math.min(maxClientHeight / svg.clientHeight * animHeight, baseHeight);
					x = Math.max(Math.min(animX + minOffsetX / svg.clientWidth * animWidth, baseX + baseWidth), baseX);
					y = Math.max(Math.min(animY + minOffsetY / svg.clientHeight * animHeight, baseY + baseHeight), baseY);
				});
				svg.addEventListener("pointerup", (event) =&gt; {
					const {pointerId} = event;
					if (!(pointerId in pointers)) {
						return;
					}
					let {x: animX, y: animY, width: animWidth, height: animHeight} = svg.viewBox.baseVal;
					svg.releasePointerCapture(pointerId);
					--pointerCount;
					delete pointers[pointerId];
					xPointerIds.splice(xPointerIds.lastIndexOf(pointerId), 1);
					yPointerIds.splice(yPointerIds.lastIndexOf(pointerId), 1);
					const minOffsetX = pointerCount !== 0 ? pointers[xPointerIds[0]].offsetX : null;
					const minOffsetY = pointerCount !== 0 ? pointers[yPointerIds[0]].offsetY : null;
					const maxClientWidth = pointerCount !== 0 ? pointers[xPointerIds[pointerCount - 1]].offsetX - minOffsetX : null;
					const maxClientHeight = pointerCount !== 0 ? pointers[yPointerIds[pointerCount - 1]].offsetY - minOffsetY : null;
					width = pointerCount !== 0 ? Math.min(maxClientWidth / svg.clientWidth * animWidth, baseWidth) : null;
					height = pointerCount !== 0 ? Math.min(maxClientHeight / svg.clientHeight * animHeight, baseHeight) : null;
					x = pointerCount !== 0 ? Math.max(Math.min(animX + minOffsetX / svg.clientWidth * animWidth, baseX + baseWidth), baseX) : null;
					y = pointerCount !== 0 ? Math.max(Math.min(animY + minOffsetY / svg.clientHeight * animHeight, baseY + baseHeight), baseY) : null;
				});
				svg.addEventListener("pointermove", (event) =&gt; {
					const {pointerId} = event;
					if (!(pointerId in pointers)) {
						return;
					}
					let {x: animX, y: animY, width: animWidth, height: animHeight} = svg.viewBox.baseVal;
					event.preventDefault();
					const offsetX = event.offsetX;
					const offsetY = event.offsetY;
					pointers[pointerId].offsetX = offsetX;
					pointers[pointerId].offsetY = offsetY;
					const oldXIndex = xPointerIds.lastIndexOf(pointerId);
					let newXIndex = oldXIndex;
					while (newXIndex &gt; 0 &amp;&amp; pointers[xPointerIds[newXIndex - 1]].offsetX &gt; offsetX) {
						--newXIndex;
					}
					xPointerIds.copyWithin(newXIndex + 1, newXIndex, oldXIndex);
					xPointerIds[newXIndex] = pointerId;
					while (newXIndex &lt; pointerCount - 1 &amp;&amp; pointers[xPointerIds[newXIndex + 1]].offsetX &lt; offsetX) {
						++newXIndex;
					}
					xPointerIds.copyWithin(oldXIndex, oldXIndex + 1, newXIndex + 1);
					xPointerIds[newXIndex] = pointerId;
					const oldYIndex = yPointerIds.lastIndexOf(pointerId);
					let newYIndex = oldYIndex;
					while (newYIndex &gt; 0 &amp;&amp; pointers[yPointerIds[newYIndex - 1]].offsetY &gt; offsetY) {
						--newYIndex;
					}
					yPointerIds.copyWithin(newYIndex + 1, newYIndex, oldYIndex);
					yPointerIds[newYIndex] = pointerId;
					while (newYIndex &lt; pointerCount - 1 &amp;&amp; pointers[yPointerIds[newYIndex + 1]].offsetY &lt; offsetY) {
						++newYIndex;
					}
					yPointerIds.copyWithin(oldYIndex, oldYIndex + 1, newYIndex + 1);
					yPointerIds[newYIndex] = pointerId;
					const minOffsetX = pointers[xPointerIds[0]].offsetX;
					const minOffsetY = pointers[yPointerIds[0]].offsetY;
					const maxClientWidth = pointers[xPointerIds[pointerCount - 1]].offsetX - minOffsetX;
					const maxClientHeight = pointers[yPointerIds[pointerCount - 1]].offsetY - minOffsetY;
					animWidth = width === 0 ? animWidth : maxClientWidth === 0 ? baseWidth : Math.min(svg.clientWidth / maxClientWidth * width, baseWidth);
					animHeight = height === 0 ? animHeight : maxClientHeight === 0 ? baseHeight : Math.min(svg.clientHeight / maxClientHeight * height, baseHeight);
					animX = Math.max(Math.min(x - minOffsetX / svg.clientWidth * animWidth, baseX + baseWidth - animWidth), baseX);
					animY = Math.max(Math.min(y - minOffsetY / svg.clientHeight * animHeight, baseY + baseHeight - animHeight), baseY);
					svg.viewBox.baseVal.x = animX;
					svg.viewBox.baseVal.y = animY;
					svg.viewBox.baseVal.width = animWidth;
					svg.viewBox.baseVal.height = animHeight;
					setMinDate(animX);
					setMaxDate(animX + animWidth);
					setMinValue(baseY * 2 + baseHeight - animHeight - animY);
					setMaxValue(baseY * 2 + baseHeight - animY);
				});
				const zoomIntensity = 1.25;
				svg.addEventListener("wheel", (event) =&gt; {
					let {x: animX, y: animY, width: animWidth, height: animHeight} = svg.viewBox.baseVal;
					const {ctrlKey, shiftKey} = event;
					event.preventDefault();
					if (pointerCount !== 0) {
						return;
					}
					if (!ctrlKey) {
						const vectorX = (shiftKey ? event.deltaY : event.deltaX) / svg.clientWidth * animWidth;
						const vectorY = (shiftKey ? 0 : event.deltaY) / svg.clientHeight * animHeight;
						animX = Math.max(Math.min(animX + vectorX, baseX + baseWidth - animWidth), baseX);
						animY = Math.max(Math.min(animY + vectorY, baseY + baseHeight - animHeight), baseY);
						svg.viewBox.baseVal.x = animX;
						svg.viewBox.baseVal.y = animY;
						setMinDate(animX);
						setMaxDate(animX + animWidth);
						setMinValue(baseY * 2 + baseHeight - animHeight - animY);
						setMaxValue(baseY * 2 + baseHeight - animY);
						return;
					}
					const ratioX = zoomIntensity ** Math.sign(shiftKey ? event.deltaY : event.deltaX);
					const ratioY = zoomIntensity ** Math.sign(shiftKey ? 0 : event.deltaY);
					const positionX = Math.max(0, Math.min(event.offsetX / svg.clientWidth, 1)) * animWidth;
					const positionY = Math.max(0, Math.min(event.offsetY / svg.clientHeight, 1)) * animHeight;
					animWidth = Math.min(animWidth * ratioX, baseWidth);
					animHeight = Math.min(animHeight * ratioY, baseHeight);
					animX = Math.max(Math.min(animX + positionX * (1 - ratioX), baseX + baseWidth - animWidth), baseX);
					animY = Math.max(Math.min(animY + positionY * (1 - ratioY), baseY + baseHeight - animHeight), baseY);
					svg.viewBox.baseVal.x = animX;
					svg.viewBox.baseVal.y = animY;
					svg.viewBox.baseVal.width = animWidth;
					svg.viewBox.baseVal.height = animHeight;
					setMinDate(animX);
					setMaxDate(animX + animWidth);
					setMinValue(baseY * 2 + baseHeight - animHeight - animY);
					setMaxValue(baseY * 2 + baseHeight - animY);
				}, {
					passive: false,
				});
				svg.addEventListener("keydown", (event) =&gt; {
					let {x: animX, y: animY, width: animWidth, height: animHeight} = svg.viewBox.baseVal;
					const {ctrlKey, shiftKey} = event;
					switch (event.code) {
						case "Space": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							const vectorY = shiftKey ? -animHeight : animHeight;
							animY = Math.max(Math.min(animY + vectorY, baseY + baseHeight - animHeight), baseY);
							svg.viewBox.baseVal.y = animY;
							setMinValue(baseY * 2 + baseHeight - animHeight - animY);
							setMaxValue(baseY * 2 + baseHeight - animY);
							return;
						}
						case "Equal":
						case "NumpadAdd":
						case "Minus":
						case "NumpadSubtract": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							const ratio = event.code === "Minus" || event.code === "NumpadSubtract" ? zoomIntensity : 1 / zoomIntensity;
							if (!shiftKey) {
								animHeight = Math.min(animHeight * ratio, baseHeight);
								animY = Math.max(Math.min(animY, baseY + baseHeight - animHeight), baseY);
								svg.viewBox.baseVal.y = animY;
								svg.viewBox.baseVal.height = animHeight;
								setMinValue(baseY * 2 + baseHeight - animHeight - animY);
								setMaxValue(baseY * 2 + baseHeight - animY);
								return;
							}
							animWidth = Math.min(animWidth * ratio, baseWidth);
							animX = Math.max(Math.min(animX, baseX + baseWidth - animWidth), baseX);
							svg.viewBox.baseVal.x = animX;
							svg.viewBox.baseVal.width = animWidth;
							setMinDate(animX);
							setMaxDate(animX + animWidth);
							return;
						}
						case "Digit0":
						case "Numpad0": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							if (!shiftKey) {
								animY = baseY;
								animHeight = baseHeight;
								svg.viewBox.baseVal.y = animY;
								svg.viewBox.baseVal.height = animHeight;
								setMinValue(baseY * 2 + baseHeight - animHeight - animY);
								setMaxValue(baseY * 2 + baseHeight - animY);
								return;
							}
							animX = baseX;
							animWidth = baseWidth;
							svg.viewBox.baseVal.x = animX;
							svg.viewBox.baseVal.width = animWidth;
							setMinDate(animX);
							setMaxDate(animX + animWidth);
							return;
						}
					}
					switch (event.key) {
						case "ArrowRight": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							const vectorX = 50 / svg.clientWidth * animWidth;
							animX = Math.max(Math.min(animX + vectorX, baseX + baseWidth - animWidth), baseX);
							svg.viewBox.baseVal.x = animX;
							setMinDate(animX);
							setMaxDate(animX + animWidth);
							return;
						}
						case "ArrowLeft": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							const vectorX = -50 / svg.clientWidth * animWidth;
							animX = Math.max(Math.min(animX + vectorX, baseX + baseWidth - animWidth), baseX);
							svg.viewBox.baseVal.x = animX;
							setMinDate(animX);
							setMaxDate(animX + animWidth);
							return;
						}
						case "ArrowDown": {
							if (!ctrlKey) {
								event.preventDefault();
								if (pointerCount !== 0) {
									return;
								}
								const vectorY = 50 / svg.clientHeight * animHeight;
								animY = Math.max(Math.min(animY + vectorY, baseY + baseHeight - animHeight), baseY);
								svg.viewBox.baseVal.y = animY;
								setMinValue(baseY * 2 + baseHeight - animHeight - animY);
								setMaxValue(baseY * 2 + baseHeight - animY);
								return;
							}
						}
						case "End": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							animY = baseY + baseHeight - animHeight;
							svg.viewBox.baseVal.y = animY;
							setMinValue(baseY * 2 + baseHeight - animHeight - animY);
							setMaxValue(baseY * 2 + baseHeight - animY);
							return;
						}
						case "ArrowUp": {
							if (!ctrlKey) {
								event.preventDefault();
								if (pointerCount !== 0) {
									return;
								}
								const vectorY = -50 / svg.clientHeight * animHeight;
								animY = Math.max(Math.min(animY + vectorY, baseY + baseHeight - animHeight), baseY);
								svg.viewBox.baseVal.y = animY;
								setMinValue(baseY * 2 + baseHeight - animHeight - animY);
								setMaxValue(baseY * 2 + baseHeight - animY);
								return;
							}
						}
						case "Home": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							animY = baseY;
							svg.viewBox.baseVal.y = animY;
							setMinValue(baseY * 2 + baseHeight - animHeight - animY);
							setMaxValue(baseY * 2 + baseHeight - animY);
							return;
						}
						case "PageDown":
						case "PageUp":
						case " ": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							const vectorY = event.key === "PageUp" || event.key === " " &amp;&amp; shiftKey ? -animHeight : animHeight;
							animY = Math.max(Math.min(animY + vectorY, baseY + baseHeight - animHeight), baseY);
							svg.viewBox.baseVal.y = animY;
							setMinValue(baseY * 2 + baseHeight - animHeight - animY);
							setMaxValue(baseY * 2 + baseHeight - animY);
							return;
						}
						case "+":
						case "-": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							const ratio = event.key === "-" ? 1 / zoomIntensity : zoomIntensity;
							animWidth = Math.min(animWidth * ratio, baseWidth);
							animHeight = Math.min(animHeight * ratio, baseHeight);
							animX = Math.max(Math.min(animX, baseX + baseWidth - animWidth), baseX);
							animY = Math.max(Math.min(animY, baseY + baseHeight - animHeight), baseY);
							svg.viewBox.baseVal.x = animX;
							svg.viewBox.baseVal.y = animY;
							svg.viewBox.baseVal.width = animWidth;
							svg.viewBox.baseVal.height = animHeight;
							setMinDate(animX);
							setMaxDate(animX + animWidth);
							setMinValue(baseY * 2 + baseHeight - animHeight - animY);
							setMaxValue(baseY * 2 + baseHeight - animY);
							return;
						}
						case "0": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							animX = baseX;
							animY = baseY;
							animWidth = baseWidth;
							animHeight = baseHeight;
							svg.viewBox.baseVal.x = animX;
							svg.viewBox.baseVal.y = animY;
							svg.viewBox.baseVal.width = animWidth;
							svg.viewBox.baseVal.height = animHeight;
							setMinDate(animX);
							setMaxDate(animX + animWidth);
							setMinValue(baseY * 2 + baseHeight - animHeight - animY);
							setMaxValue(baseY * 2 + baseHeight - animY);
							return;
						}
					}
				});
				svg.addEventListener("focusin", (event) =&gt; {
					if (!event.target.matches(":focus-visible")) {
						return;
					}
					event.preventDefault();
					if (pointerCount !== 0) {
						return;
					}
					let {x: animX, y: animY, width: animWidth, height: animHeight} = svg.viewBox.baseVal;
					let {x, y, width, height} = event.target.getBBox();
					width = Math.round(width);
					height = Math.round(height);
					x = Math.round(x);
					y = baseY * 2 + baseHeight - height - Math.round(y);
					if (x + width &lt; animX) {
						animX = x;
						if (width &gt; animWidth) {
							animX += (width - animWidth) / 2;
						}
					} else if (x &gt; animX + animWidth) {
						animX = x + width - animWidth;
						if (width &gt; animWidth) {
							animX -= (width - animWidth) / 2;
						}
					}
					if (y + height &lt; animY) {
						animY = y;
						if (height &gt; animHeight) {
							animY += (height - animHeight) / 2;
						}
					} else if (y &gt; animY + animHeight) {
						animY = y + height - animHeight;
						if (height &gt; animHeight) {
							animY -= (height - animHeight) / 2;
						}
					}
					svg.viewBox.baseVal.x = animX;
					svg.viewBox.baseVal.y = animY;
					setMinDate(animX);
					setMaxDate(animX + animWidth);
					setMinValue(baseY * 2 + baseHeight - animHeight - animY);
					setMaxValue(baseY * 2 + baseHeight - animY);
				});
			}, {
				once: true,
			});
		}
	</script>
	<foreignObject x="0" y="0" width="100%" height="100%" style="--date-size: 100; --value-size: 20;">
		<svg viewBox="0 0 100 20">
			<text x="50" y="10" dx="0" dy="2" textLength="100" lengthAdjust="spacingAndGlyphs" text-anchor="middle" dominant-baseline="middle">2024-05-04</text>
		</svg>
		<svg viewBox="0 0 100 20">
			<text x="50" y="10" dx="0" dy="2" textLength="100" lengthAdjust="spacingAndGlyphs" text-anchor="middle" dominant-baseline="middle">2026-03-31</text>
		</svg>
		<svg viewBox="0 0 20 20">
			<text x="10" y="10" dx="0" dy="2" textLength="10" lengthAdjust="spacingAndGlyphs" text-anchor="middle" dominant-baseline="middle">0</text>
		</svg>
		<svg viewBox="0 0 20 20">
			<text x="10" y="10" dx="0" dy="2" textLength="20" lengthAdjust="spacingAndGlyphs" text-anchor="middle" dominant-baseline="middle">41</text>
		</svg>
		<svg viewBox="0 0 696 41" preserveAspectRatio="none" overflow="visible">
			<g transform="translate(0 41) scale(1 -1)" style="--count: 41;">
				<g>
					<rect x="0" y="0" width="696" height="41"/>
				</g>
				<g style="--index: 0;">
					<path d="M0,0L0,0L0,1L1,1L1,1L2,1L2,1L45,1L45,1L57,1L57,1L68,1L68,1L128,1L128,1L130,1L130,1L148,1L148,1L156,1L156,1L232,1L232,1L295,1L295,1L302,1L302,1L315,1L315,1L331,1L331,1L336,1L336,1L370,1L370,1L405,1L405,1L410,1L410,1L420,1L420,1L446,1L446,1L447,1L447,1L449,1L449,1L483,1L483,1L497,1L497,1L506,1L506,1L556,1L556,1L584,1L584,1L600,1L600,1L627,1L627,1L628,1L628,1L630,1L630,1L638,1L638,1L642,1L642,1L649,1L649,1L672,1L672,1L673,1L673,1L696,1L696,1L696,1" tabindex="0">
						<title>j465mqd8</title>
					</path>
				</g>
				<g style="--index: 1;">
					<path d="M0,0L1,0L1,2L2,2L2,2L45,2L45,2L57,2L57,2L68,2L68,2L128,2L128,3L130,3L130,3L148,3L148,3L156,3L156,3L232,3L232,4L295,4L295,4L302,4L302,4L315,4L315,4L331,4L331,4L336,4L336,4L370,4L370,4L405,4L405,4L410,4L410,4L420,4L420,4L446,4L446,4L447,4L447,4L449,4L449,4L483,4L483,4L497,4L497,4L506,4L506,4L556,4L556,4L584,4L584,4L600,4L600,4L627,4L627,4L628,4L628,4L630,4L630,4L638,4L638,4L642,4L642,4L649,4L649,4L672,4L672,4L673,4L673,4L696,4L696,4L696,4" tabindex="0">
						<title>j5959zwx</title>
					</path>
				</g>
				<g style="--index: 2;">
					<path d="M0,0L1,0L1,3L2,3L2,3L45,3L45,3L57,3L57,4L68,4L68,4L128,4L128,5L130,5L130,6L148,6L148,6L156,6L156,7L232,7L232,8L295,8L295,8L302,8L302,8L315,8L315,9L331,9L331,9L336,9L336,9L370,9L370,9L405,9L405,9L410,9L410,9L420,9L420,8L446,8L446,9L447,9L447,10L449,10L449,10L454,10L454,9L456,9L456,8L483,8L483,10L497,10L497,11L506,11L506,11L556,11L556,12L584,12L584,13L600,13L600,14L627,14L627,14L628,14L628,14L630,14L630,15L638,15L638,15L642,15L642,16L649,16L649,16L672,16L672,16L673,16L673,17L696,17L696,18L696,18" tabindex="0">
						<title>jmz5yyex</title>
					</path>
				</g>
				<g style="--index: 3;">
					<path d="M0,0L2,0L2,4L45,4L45,4L46,4L46,4L57,4L57,5L68,5L68,5L128,5L128,6L130,6L130,7L148,7L148,8L156,8L156,9L232,9L232,10L295,10L295,10L302,10L302,11L315,11L315,12L331,12L331,13L335,13L335,11L336,11L336,11L370,11L370,12L378,12L378,11L405,11L405,11L410,11L410,11L420,11L420,11L446,11L446,12L447,12L447,13L449,13L449,13L483,13L483,15L497,15L497,16L506,16L506,17L556,17L556,18L584,18L584,19L600,19L600,20L627,20L627,20L628,20L628,20L630,20L630,21L638,21L638,21L642,21L642,22L649,22L649,22L672,22L672,22L673,22L673,23L696,23L696,24L696,24" tabindex="0">
						<title>x3wdlvkj</title>
					</path>
				</g>
				<g style="--index: 4;">
					<path d="M0,0L45,0L45,5L46,5L46,5L57,5L57,6L68,6L68,6L128,6L128,7L130,7L130,8L148,8L148,9L156,9L156,10L232,10L232,11L295,11L295,11L302,11L302,12L315,12L315,13L331,13L331,14L336,14L336,14L354,14L354,13L370,13L370,14L405,14L405,14L410,14L410,15L420,15L420,16L446,16L446,17L447,17L447,18L449,18L449,18L483,18L483,20L497,20L497,21L506,21L506,22L556,22L556,23L584,23L584,24L600,24L600,25L627,25L627,25L628,25L628,25L630,25L630,26L638,26L638,26L642,26L642,27L649,27L649,28L672,28L672,28L673,28L673,29L696,29L696,30L696,30" tabindex="0">
						<title>8v0mryvx</title>
					</path>
				</g>
				<g style="--index: 5;">
					<path d="M0,0L57,0L57,3L68,3L68,3L128,3L128,4L130,4L130,4L148,4L148,4L156,4L156,4L232,4L232,5L295,5L295,5L302,5L302,5L315,5L315,5L331,5L331,5L336,5L336,5L370,5L370,5L405,5L405,5L410,5L410,5L420,5L420,5L446,5L446,5L447,5L447,5L449,5L449,5L483,5L483,5L497,5L497,5L506,5L506,5L556,5L556,5L584,5L584,5L600,5L600,5L627,5L627,5L628,5L628,5L630,5L630,5L638,5L638,5L642,5L642,5L649,5L649,5L672,5L672,5L673,5L673,5L696,5L696,5L696,5" tabindex="0">
						<title>j0gyekmx</title>
					</path>
				</g>
				<g style="--index: 6;">
					<path d="M0,0L68,0L68,7L128,7L128,8L130,8L130,9L148,9L148,10L156,10L156,11L232,11L232,12L295,12L295,13L302,13L302,14L315,14L315,15L331,15L331,17L336,17L336,17L338,17L338,18L370,18L370,19L405,19L405,20L410,20L410,21L420,21L420,22L446,22L446,23L447,23L447,24L449,24L449,25L483,25L483,27L497,27L497,28L506,28L506,29L556,29L556,30L566,30L566,29L571,29L571,30L584,30L584,31L600,31L600,32L627,32L627,33L628,33L628,34L630,34L630,34L638,34L638,35L641,35L641,36L642,36L642,37L649,37L649,38L672,38L672,39L673,39L673,40L696,40L696,41L696,41" tabindex="0">
						<title>jn2m23wj</title>
					</path>
				</g>
				<g style="--index: 7;">
					<path d="M0,0L128,0L128,2L130,2L130,2L148,2L148,2L156,2L156,2L232,2L232,2L295,2L295,2L302,2L302,2L315,2L315,2L331,2L331,2L336,2L336,2L370,2L370,2L405,2L405,2L410,2L410,2L420,2L420,2L446,2L446,2L447,2L447,2L449,2L449,2L483,2L483,2L497,2L497,2L506,2L506,2L556,2L556,2L584,2L584,2L600,2L600,2L627,2L627,2L628,2L628,2L630,2L630,2L638,2L638,2L642,2L642,2L649,2L649,2L672,2L672,2L673,2L673,2L696,2L696,2L696,2" tabindex="0">
						<title>jo1kw67x</title>
					</path>
				</g>
				<g style="--index: 8;">
					<path d="M0,0L130,0L130,5L148,5L148,5L156,5L156,5L232,5L232,6L295,6L295,6L302,6L302,6L315,6L315,7L331,7L331,7L336,7L336,7L370,7L370,7L405,7L405,7L410,7L410,7L420,7L420,7L446,7L446,7L447,7L447,8L449,8L449,8L454,8L454,7L483,7L483,7L497,7L497,7L506,7L506,7L556,7L556,7L584,7L584,7L600,7L600,7L627,7L627,7L628,7L628,7L630,7L630,8L638,8L638,8L642,8L642,8L649,8L649,8L672,8L672,8L673,8L673,8L696,8L696,9L696,9" tabindex="0">
						<title>j0ewnqw8</title>
					</path>
				</g>
				<g style="--index: 9;">
					<path d="M0,0L148,0L148,7L156,7L156,8L232,8L232,9L295,9L295,9L302,9L302,10L315,10L315,11L331,11L331,10L336,10L336,10L370,10L370,10L405,10L405,10L410,10L410,10L420,10L420,10L446,10L446,11L447,11L447,12L449,12L449,12L483,12L483,14L497,14L497,15L506,15L506,16L556,16L556,17L569,17L569,16L584,16L584,17L600,17L600,18L627,18L627,18L628,18L628,18L630,18L630,19L638,19L638,19L642,19L642,20L649,20L649,20L672,20L672,20L673,20L673,21L696,21L696,22L696,22" tabindex="0">
						<title>0jmw6wex</title>
					</path>
				</g>
				<g style="--index: 10;">
					<path d="M0,0L156,0L156,6L232,6L232,7L295,7L295,7L302,7L302,7L315,7L315,8L331,8L331,8L336,8L336,8L370,8L370,8L405,8L405,8L410,8L410,8L420,8L420,9L446,9L446,10L447,10L447,11L449,11L449,11L456,11L456,10L483,10L483,12L497,12L497,13L506,13L506,13L556,13L556,14L569,14L569,17L584,17L584,18L600,18L600,19L627,19L627,19L628,19L628,19L630,19L630,20L638,20L638,20L642,20L642,21L649,21L649,21L672,21L672,21L673,21L673,22L696,22L696,23L696,23" tabindex="0">
						<title>8w3l21qj</title>
					</path>
				</g>
				<g style="--index: 11;">
					<path d="M0,0L232,0L232,3L295,3L295,3L302,3L302,3L315,3L315,3L331,3L331,3L336,3L336,3L370,3L370,3L405,3L405,3L410,3L410,3L420,3L420,3L446,3L446,3L447,3L447,3L449,3L449,3L483,3L483,3L497,3L497,3L506,3L506,3L556,3L556,3L584,3L584,3L600,3L600,3L627,3L627,3L628,3L628,3L630,3L630,3L638,3L638,3L642,3L642,3L649,3L649,3L672,3L672,3L673,3L673,3L696,3L696,3L696,3" tabindex="0">
						<title>j0wm7v98</title>
					</path>
				</g>
				<g style="--index: 12;">
					<path d="M0,0L295,0L295,12L302,12L302,13L315,13L315,14L331,14L331,16L336,16L336,16L370,16L370,17L405,17L405,17L410,17L410,18L420,18L420,19L446,19L446,20L447,20L447,21L449,21L449,21L483,21L483,23L497,23L497,24L506,24L506,25L556,25L556,26L584,26L584,27L600,27L600,28L627,28L627,28L628,28L628,29L630,29L630,30L635,30L635,29L638,29L638,30L642,30L642,31L649,31L649,32L665,32L665,31L672,31L672,31L673,31L673,32L696,32L696,33L696,33" tabindex="0">
						<title>xkon93k8</title>
					</path>
				</g>
				<g style="--index: 13;">
					<path d="M0,0L302,0L302,9L315,9L315,10L331,10L331,12L335,12L335,12L336,12L336,12L370,12L370,13L378,13L378,12L405,12L405,12L410,12L410,13L420,13L420,14L446,14L446,15L447,15L447,16L449,16L449,16L483,16L483,18L497,18L497,19L506,19L506,20L556,20L556,21L584,21L584,22L600,22L600,23L627,23L627,23L628,23L628,23L630,23L630,24L638,24L638,24L642,24L642,25L649,25L649,26L672,26L672,26L673,26L673,27L696,27L696,28L696,28" tabindex="0">
						<title>j2pzvvp8</title>
					</path>
				</g>
				<g style="--index: 14;">
					<path d="M0,0L315,0L315,6L331,6L331,6L336,6L336,6L370,6L370,6L405,6L405,6L410,6L410,6L420,6L420,6L446,6L446,6L447,6L447,7L449,7L449,7L454,7L454,6L483,6L483,6L497,6L497,6L506,6L506,6L556,6L556,6L584,6L584,6L600,6L600,6L627,6L627,6L628,6L628,6L630,6L630,7L638,7L638,7L642,7L642,7L649,7L649,7L672,7L672,7L673,7L673,7L696,7L696,8L696,8" tabindex="0">
						<title>j9ve5wox</title>
					</path>
				</g>
				<g style="--index: 15;">
					<path d="M0,0L331,0L331,11L335,11L335,13L336,13L336,13L354,13L354,15L370,15L370,16L405,16L405,16L410,16L410,17L420,17L420,18L446,18L446,19L447,19L447,20L449,20L449,20L483,20L483,22L497,22L497,23L506,23L506,24L556,24L556,25L584,25L584,26L600,26L600,27L627,27L627,27L628,27L628,28L630,28L630,29L635,29L635,32L638,32L638,33L642,33L642,34L649,34L649,35L672,35L672,35L673,35L673,36L696,36L696,37L696,37" tabindex="0">
						<title>8gop7k2x</title>
					</path>
				</g>
				<g style="--index: 16;">
					<path d="M0,0L331,0L331,15L336,15L336,15L354,15L354,14L370,14L370,15L405,15L405,15L410,15L410,16L420,16L420,17L446,17L446,18L447,18L447,19L449,19L449,19L483,19L483,21L497,21L497,22L506,22L506,23L556,23L556,24L584,24L584,25L600,25L600,26L627,26L627,26L628,26L628,26L630,26L630,27L638,27L638,27L642,27L642,28L649,28L649,29L672,29L672,29L673,29L673,30L696,30L696,31L696,31" tabindex="0">
						<title>81pdyqq8</title>
					</path>
				</g>
				<g style="--index: 17;">
					<path d="M0,0L336,0L336,18L338,18L338,17L370,17L370,18L405,18L405,19L410,19L410,20L420,20L420,21L446,21L446,22L447,22L447,23L449,23L449,24L483,24L483,26L497,26L497,27L506,27L506,28L556,28L556,29L566,29L566,28L584,28L584,29L600,29L600,30L627,30L627,31L628,31L628,32L630,32L630,33L638,33L638,34L642,34L642,35L649,35L649,36L672,36L672,36L673,36L673,37L696,37L696,38L696,38" tabindex="0">
						<title>jo19m63x</title>
					</path>
				</g>
				<g style="--index: 18;">
					<path d="M0,0L370,0L370,11L378,11L378,13L405,13L405,13L410,13L410,14L420,14L420,15L446,15L446,16L447,16L447,17L449,17L449,17L483,17L483,19L497,19L497,20L506,20L506,21L556,21L556,22L584,22L584,23L600,23L600,24L627,24L627,24L628,24L628,24L630,24L630,25L638,25L638,25L642,25L642,26L649,26L649,27L672,27L672,27L673,27L673,28L696,28L696,29L696,29" tabindex="0">
						<title>8qpl62dx</title>
					</path>
				</g>
				<g style="--index: 19;">
					<path d="M0,0L405,0L405,18L410,18L410,19L420,19L420,20L446,20L446,21L447,21L447,22L449,22L449,23L483,23L483,25L497,25L497,26L506,26L506,27L556,27L556,28L566,28L566,30L571,30L571,29L584,29L584,30L600,30L600,31L627,31L627,32L628,32L628,33L630,33L630,35L638,35L638,36L641,36L641,35L642,35L642,36L649,36L649,37L672,37L672,38L673,38L673,39L696,39L696,40L696,40" tabindex="0">
						<title>j2p4pel8</title>
					</path>
				</g>
				<g style="--index: 20;">
					<path d="M0,0L410,0L410,12L420,12L420,12L446,12L446,13L447,13L447,14L449,14L449,14L483,14L483,16L497,16L497,17L506,17L506,18L556,18L556,19L584,19L584,20L600,20L600,21L627,21L627,21L628,21L628,21L630,21L630,22L638,22L638,22L642,22L642,23L649,23L649,24L672,24L672,24L673,24L673,25L696,25L696,26L696,26" tabindex="0">
						<title>xkpw7g6j</title>
					</path>
				</g>
				<g style="--index: 21;">
					<path d="M0,0L420,0L420,13L446,13L446,14L447,14L447,15L449,15L449,15L483,15L483,17L497,17L497,18L506,18L506,19L556,19L556,20L584,20L584,21L600,21L600,22L627,22L627,22L628,22L628,22L630,22L630,23L638,23L638,23L642,23L642,24L649,24L649,25L672,25L672,25L673,25L673,26L696,26L696,27L696,27" tabindex="0">
						<title>x356nrej</title>
					</path>
				</g>
				<g style="--index: 22;">
					<path d="M0,0L446,0L446,8L447,8L447,9L449,9L449,9L453,9L453,9L454,9L454,8L456,8L456,11L483,11L483,13L497,13L497,14L506,14L506,15L556,15L556,16L569,16L569,15L584,15L584,16L600,16L600,17L627,17L627,17L628,17L628,17L630,17L630,18L638,18L638,18L642,18L642,19L649,19L649,19L672,19L672,19L673,19L673,20L696,20L696,21L696,21" tabindex="0">
						<title>j2pmdqp8</title>
					</path>
				</g>
				<g style="--index: 23;">
					<path d="M0,0L447,0L447,6L449,6L449,6L454,6L454,10L456,10L456,9L483,9L483,11L497,11L497,12L506,12L506,12L556,12L556,13L584,13L584,14L600,14L600,15L627,15L627,15L628,15L628,15L630,15L630,16L638,16L638,16L642,16L642,17L649,17L649,17L672,17L672,17L673,17L673,18L696,18L696,19L696,19" tabindex="0">
						<title>jn15mm18</title>
					</path>
				</g>
				<g style="--index: 24;">
					<path d="M0,0L449,0L449,22L483,22L483,24L497,24L497,25L506,25L506,26L556,26L556,27L584,27L584,28L600,28L600,29L627,29L627,29L628,29L628,30L630,30L630,31L635,31L635,30L638,30L638,31L642,31L642,32L649,32L649,33L665,33L665,32L672,32L672,32L673,32L673,33L696,33L696,34L696,34" tabindex="0">
						<title>jo1zrpnx</title>
					</path>
				</g>
				<g style="--index: 25;">
					<path d="M0,0L483,0L483,9L497,9L497,9L506,9L506,9L556,9L556,10L584,10L584,11L600,11L600,11L627,11L627,11L628,11L628,11L630,11L630,12L638,12L638,12L642,12L642,12L649,12L649,12L672,12L672,12L673,12L673,13L696,13L696,14L696,14" tabindex="0">
						<title>xkzy0d7x</title>
					</path>
				</g>
				<g style="--index: 26;">
					<path d="M0,0L483,0L483,8L497,8L497,8L506,8L506,8L556,8L556,8L584,8L584,8L600,8L600,8L627,8L627,8L628,8L628,8L630,8L630,9L638,9L638,9L642,9L642,9L649,9L649,9L672,9L672,9L673,9L673,9L696,9L696,10L696,10" tabindex="0">
						<title>8gzn56rj</title>
					</path>
				</g>
				<g style="--index: 27;">
					<path d="M0,0L497,0L497,10L506,10L506,10L556,10L556,11L584,11L584,12L600,12L600,12L627,12L627,12L628,12L628,12L630,12L630,13L638,13L638,13L642,13L642,13L649,13L649,13L672,13L672,13L673,13L673,14L696,14L696,15L696,15" tabindex="0">
						<title>8gzloyyj</title>
					</path>
				</g>
				<g style="--index: 28;">
					<path d="M0,0L506,0L506,14L556,14L556,15L569,15L569,14L584,14L584,15L600,15L600,16L627,16L627,16L628,16L628,16L630,16L630,17L638,17L638,17L642,17L642,18L649,18L649,18L672,18L672,18L673,18L673,19L696,19L696,20L696,20" tabindex="0">
						<title>x7g52m6x</title>
					</path>
				</g>
				<g style="--index: 29;">
					<path d="M0,0L556,0L556,9L584,9L584,10L600,10L600,10L627,10L627,10L628,10L628,10L630,10L630,11L638,11L638,11L642,11L642,11L649,11L649,11L672,11L672,11L673,11L673,11L696,11L696,12L696,12" tabindex="0">
						<title>8q01170j</title>
					</path>
				</g>
				<g style="--index: 30;">
					<path d="M0,0L584,0L584,9L600,9L600,9L627,9L627,9L628,9L628,9L630,9L630,10L638,10L638,10L642,10L642,10L649,10L649,10L672,10L672,10L673,10L673,10L696,10L696,11L696,11" tabindex="0">
						<title>8logk9r8</title>
					</path>
				</g>
				<g style="--index: 31;">
					<path d="M0,0L600,0L600,13L627,13L627,13L628,13L628,13L630,13L630,14L638,14L638,14L642,14L642,14L649,14L649,14L672,14L672,14L673,14L673,15L696,15L696,16L696,16" tabindex="0">
						<title>8gzpn2yj</title>
					</path>
				</g>
				<g style="--index: 32;">
					<path d="M0,0L627,0L627,30L628,30L628,31L630,31L630,32L635,32L635,31L638,31L638,32L642,32L642,33L649,33L649,34L665,34L665,33L672,33L672,33L673,33L673,34L696,34L696,35L696,35" tabindex="0">
						<title>8wg2g1vx</title>
					</path>
				</g>
				<g style="--index: 33;">
					<path d="M0,0L628,0L628,27L630,27L630,28L638,28L638,29L642,29L642,30L649,30L649,31L665,31L665,30L672,30L672,30L673,30L673,31L696,31L696,32L696,32" tabindex="0">
						<title>x7glv30x</title>
					</path>
				</g>
				<g style="--index: 34;">
					<path d="M0,0L630,0L630,6L638,6L638,6L642,6L642,6L649,6L649,6L672,6L672,6L673,6L673,6L696,6L696,6L696,6" tabindex="0">
						<title>j22y2l7j</title>
					</path>
				</g>
				<g style="--index: 35;">
					<path d="M0,0L638,0L638,28L642,28L642,29L649,29L649,30L665,30L665,34L672,34L672,34L673,34L673,35L696,35L696,36L696,36" tabindex="0">
						<title>joz7zge8</title>
					</path>
				</g>
				<g style="--index: 36;">
					<path d="M0,0L642,0L642,15L649,15L649,15L672,15L672,15L673,15L673,16L696,16L696,17L696,17" tabindex="0">
						<title>8lov05v8</title>
					</path>
				</g>
				<g style="--index: 37;">
					<path d="M0,0L649,0L649,23L672,23L672,23L673,23L673,24L696,24L696,25L696,25" tabindex="0">
						<title>j9l6qooj</title>
					</path>
				</g>
				<g style="--index: 38;">
					<path d="M0,0L672,0L672,37L673,37L673,38L696,38L696,39L696,39" tabindex="0">
						<title>81v0nq38</title>
					</path>
				</g>
				<g style="--index: 39;">
					<path d="M0,0L673,0L673,12L696,12L696,13L696,13" tabindex="0">
						<title>x7zrzqq8</title>
					</path>
				</g>
				<g style="--index: 40;">
					<path d="M0,0L696,0L696,7L696,7" tabindex="0">
						<title>j5r3pmwx</title>
					</path>
				</g>
			</g>
		</svg>
	</foreignObject>
</svg>
