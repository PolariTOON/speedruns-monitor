<svg xmlns="http://www.w3.org/2000/svg" lang="en">
	<title>Score by leaderboard for player 48grel1x</title>
	<metadata>
		<meta xmlns="http://www.w3.org/1999/xhtml" charset="utf-8" />
		<meta xmlns="http://www.w3.org/1999/xhtml" name="viewport" content="width=device-width" />
		<meta xmlns="http://www.w3.org/1999/xhtml" name="color-scheme" content="dark light" />
	</metadata>
	<style>
		*,
		::before,
		::after {
			box-sizing: border-box;
		}
		:root {
			background: var(--canvas-background);
			color: var(--canvas-foreground);
			fill: var(--canvas-background);
			stroke: var(--canvas-foreground);
			stroke-linejoin: round;
			stroke-linecap: round;
		}
		foreignObject {
			pointer-events: none;
		}
		foreignObject::before,
		foreignObject::after {
			content: "";
			position: absolute;
			z-index: 1;
			left: 20px;
			bottom: 20px;
			pointer-events: auto;
		}
		foreignObject::before {
			width: calc(100% - 40px);
			height: calc(20px + var(--date-size) * 1px);
		}
		foreignObject::after {
			width: calc(20px + var(--value-size) * 1px);
			height: calc(100% - 40px);
		}
		foreignObject &gt; div {
			position: absolute;
			z-index: 4;
			right: 0;
			top: 0;
			margin: 20px;
			border: 1px solid var(--canvas-foreground);
			background: var(--highlighted);
			font: 16px / 1.25 serif;
			transition: opacity 1s;
			pointer-events: auto;
		}
		foreignObject:is(:hover, :focus-within) &gt; div:not(:hover, :focus-within) {
			opacity: 0;
			pointer-events: none;
		}
		foreignObject &gt; div &gt; p {
			margin: 0;
		}
		label {
			display: table;
			border-collapse: separate;
			border-spacing: 10px;
			direction: rtl;
		}
		progress,
		input[type="checkbox"] {
			display: table-cell;
			width: 16px;
			height: 16px;
			margin: 2px;
			accent-color: var(--canvas-foreground);
			vertical-align: top;
			direction: ltr;
		}
		progress + span,
		input[type="checkbox"] + span {
			display: table-cell;
			vertical-align: top;
			direction: ltr;
		}
		input[type="checkbox"]:not(:disabled):is(:hover, :focus-within) + span {
			text-decoration: underline;
		}
		foreignObject &gt; svg:first-of-type,
		foreignObject &gt; svg:first-of-type + svg,
		foreignObject &gt; svg:first-of-type + svg + svg,
		foreignObject &gt; svg:first-of-type + svg + svg + svg {
			position: absolute;
			z-index: 2;
			margin: 0;
			padding: 10px;
			background: var(--highlighted);
			font: 16px / 1.25 serif;
			font-weight: bold;
			pointer-events: auto;
		}
		foreignObject &gt; svg:first-of-type,
		foreignObject &gt; svg:first-of-type + svg + svg {
			border-top: 1px solid #0000;
			border-bottom: 1px solid var(--canvas-foreground);
		}
		foreignObject &gt; svg:first-of-type + svg,
		foreignObject &gt; svg:first-of-type + svg + svg + svg {
			border-top: 1px dashed var(--canvas-foreground);
			border-bottom: 1px dashed #0000;
		}
		foreignObject &gt; svg:first-of-type,
		foreignObject &gt; svg:first-of-type + svg {
			width: calc(20px + var(--date-size) * 1px);
			height: 42px;
			bottom: 20px;
		}
		foreignObject &gt; svg:first-of-type {
			left: calc(40px + var(--value-size) * 1px);
			transform: rotate(90deg) translate(-100%, 0);
			transform-origin: left bottom;
		}
		foreignObject &gt; svg:first-of-type + svg {
			right: 20px;
			transform: rotate(90deg) translate(0, 100%);
			transform-origin: right bottom;
		}
		foreignObject &gt; svg:first-of-type + svg + svg,
		foreignObject &gt; svg:first-of-type + svg + svg + svg {
			width: calc(20px + var(--value-size) * 1px);
			height: 42px;
			left: 20px;
		}
		foreignObject &gt; svg:first-of-type + svg + svg {
			bottom: calc(40px + var(--date-size) * 1px);
		}
		foreignObject &gt; svg:first-of-type + svg + svg + svg {
			top: 20px;
		}
		foreignObject &gt; svg:first-of-type &gt; text,
		foreignObject &gt; svg:first-of-type + svg &gt; text,
		foreignObject &gt; svg:first-of-type + svg + svg &gt; text,
		foreignObject &gt; svg:first-of-type + svg + svg + svg &gt; text {
			fill: var(--canvas-foreground);
			stroke: none;
		}
		foreignObject &gt; svg:first-of-type &gt; text,
		foreignObject &gt; svg:first-of-type + svg &gt; text {
			cursor: vertical-text;
		}
		foreignObject &gt; svg:first-of-type + svg + svg + svg + svg {
			position: relative;
			z-index: 3;
			width: calc(100% - 60px - var(--value-size) * 1px);
			height: calc(100% - 60px - var(--date-size) * 1px);
			margin: 20px 20px calc(40px + var(--date-size) * 1px) calc(40px + var(--value-size) * 1px);
			border-left: 1px solid var(--canvas-foreground);
			border-right: 1px dashed var(--canvas-foreground);
			border-top: 1px dashed var(--canvas-foreground);
			border-bottom: 1px solid var(--canvas-foreground);
			background: var(--canvas-background);
			pointer-events: auto;
		}
		g &gt; rect {
			vector-effect: non-scaling-stroke;
			fill: var(--highlighted);
			stroke: none;
		}
		g &gt; rect[tabindex] {
			fill: var(--tier, #0000);
			cursor: help;
		}
		g &gt; rect[tabindex][data-tier="diamond"] {
			--tier: var(--diamond);
		}
		g &gt; rect[tabindex][data-tier="gold"] {
			--tier: var(--gold);
		}
		g &gt; rect[tabindex][data-tier="silver"] {
			--tier: var(--silver);
		}
		g &gt; rect[tabindex][data-tier="bronze"] {
			--tier: var(--bronze);
		}
		g &gt; path[tabindex] {
			vector-effect: non-scaling-stroke;
			fill: none;
			stroke: hsl(calc(var(--index) / var(--count) * 360), 50%, 50%);
			cursor: help;
		}
		g:is(:hover, :focus-within) &gt; path[tabindex] {
			stroke: var(--canvas-foreground);
		}
		g &gt; path[tabindex]:is(:hover, :focus-within) {
			outline: 0;
		}
		g &gt; path[tabindex]:first-of-type {
			stroke-width: 2;
		}
		g &gt; path[tabindex]:first-of-type:is(:hover, :focus-within) {
			stroke-width: 4;
		}
		g &gt; path[tabindex]:first-of-type:not(:last-of-type) {
			stroke-dasharray: 4 8;
		}
		g &gt; path[tabindex]:first-of-type + rect {
			fill: none;
		}
		g &gt; path[tabindex]:first-of-type:is(:hover, :focus-within) + rect {
			fill: var(--highlighted);
			pointer-events: none;
		}
		g &gt; path[tabindex]:first-of-type ~ path[tabindex] {
			stroke-width: 8;
		}
		g &gt; path[tabindex]:first-of-type ~ path[tabindex]:is(:hover, :focus-within) {
			stroke-width: 16;
		}
		g &gt; path[tabindex]:first-of-type ~ path[tabindex] + line,
		g &gt; path[tabindex]:first-of-type ~ path[tabindex] + line + line {
			vector-effect: non-scaling-stroke;
			fill: none;
			stroke: none;
			stroke-width: 2;
			stroke-dasharray: 4 8;
			pointer-events: none;
		}
		g &gt; path[tabindex]:first-of-type:is(:hover, :focus-within) + rect + path[tabindex] + line,
		g &gt; path[tabindex]:first-of-type:is(:hover, :focus-within) + rect + path[tabindex] + line + line,
		g &gt; path[tabindex]:first-of-type:is(:hover, :focus-within) ~ path[tabindex]:last-of-type + line,
		g &gt; path[tabindex]:first-of-type:is(:hover, :focus-within) ~ path[tabindex]:last-of-type + line + line,
		g &gt; path[tabindex]:first-of-type ~ path[tabindex]:is(:hover, :focus-within) + line,
		g &gt; path[tabindex]:first-of-type ~ path[tabindex]:is(:hover, :focus-within) + line + line {
			stroke: var(--faded);
		}
		@media (prefers-color-scheme: dark) {
			:root {
				--canvas-background: #000;
				--canvas-foreground: #ccc;
				--diamond: #0363;
				--gold: #6303;
				--silver: #3633;
				--bronze: #6363;
				--highlighted: #6663;
				--faded: #9993;
			}
		}
		@media (prefers-color-scheme: light) {
			:root {
				--canvas-background: #fff;
				--canvas-foreground: #333;
				--diamond: #9cf3;
				--gold: #fc93;
				--silver: #9c93;
				--bronze: #c9c3;
				--highlighted: #9993;
				--faded: #6663;
			}
		}
	</style>
	<script>
		"use strict";
		{
			const script = document.currentScript;
			const documentUrl = location.href;
			const scope = new URL("../../", documentUrl).pathname;
			const playersKey = `${scope}#players`;
			const leaderboardsKey = `${scope}#leaderboards`;
			const sessionKey = `${scope}#session`;
			const lockKey = `${scope}#lock`;
			async function importDataScript(dataScriptUrl, signal) {
				signal.throwIfAborted();
				if (documentUrl.protocol !== "file:") {
					const response = await fetch(dataScriptUrl, {
						signal,
					});
					if (!response.ok) {
						throw new Error(response.statusText);
					}
					const data = await response.json();
					return data;
				}
				return await new Promise((resolve, reject) =&gt; {
					const name = `callback${Math.random() * 2 ** 53}`;
					let called = false;
					let data = null;
					function callback(value) {
						if (!called) {
							called = true;
							data = value;
						}
					}
					window[name] = callback;
					const dataScript = document.createElementNS("http://www.w3.org/2000/svg", "script");
					dataScript.href = `${dataScriptUrl}&amp;callback=${name}`;
					function handleAbort(event) {
						delete window[name];
						dataScript.removeEventListener("error", handleError);
						dataScript.removeEventListener("load", handleLoad);
						dataScript.remove();
						const reason = signal.reason;
						reject(reason);
					}
					function handleError(event) {
						delete window[name];
						signal.removeEventListener("abort", handleAbort);
						dataScript.removeEventListener("load", handleLoad);
						dataScript.remove();
						const error = event.error;
						reject(error);
					}
					function handleLoad(event) {
						delete window[name];
						signal.removeEventListener("abort", handleAbort);
						dataScript.removeEventListener("error", handleError);
						dataScript.remove();
						resolve(data);
					}
					signal.addEventListener("abort", handleAbort, {
						once: true,
					});
					dataScript.addEventListener("error", handleError, {
						once: true,
					});
					dataScript.addEventListener("load", handleLoad, {
						once: true,
					});
					script.after(dataScript);
				});
			}
			async function waitForTimeout(delay, signal) {
				signal.throwIfAborted();
				return await new Promise((resolve, reject) =&gt; {
					let timeout = null;
					function handleAbort() {
						clearTimeout(timeout);
						const reason = signal.reason;
						reject(reason);
					}
					function handleTimeout() {
						signal.removeEventListener("abort", handleAbort);
						resolve(delay);
					}
					signal.addEventListener("abort", handleAbort, {
						once: true,
					});
					timeout = setTimeout(handleTimeout, delay);
				});
			}
			window.addEventListener("pageshow", (event) =&gt; {
				if (event.persisted) {
					console.log(`Successfully restored page from cache`);
				} else {
					console.log(`Unsuccessfully restored page from cache`);
				}
			});
			window.addEventListener("pagehide", (event) =&gt; {
				if (event.persisted) {
					console.log(`Successfully saved page to cache`);
				} else {
					console.log(`Unsuccessfully saved page to cache`);
				}
			});
			window.addEventListener("pageshow", async (event) =&gt; {
				const rootTitleElement = document.querySelector("title");
				const rootTitleContent = rootTitleElement.textContent;
				const pathTitleElements = Array.from(document.querySelectorAll("g &gt; path[tabindex]:first-of-type &gt; title"));
				const pathTitleContents = pathTitleElements.map((pathTitleElement) =&gt; {
					const pathTitleContent = pathTitleElement.textContent;
					return pathTitleContent;
				});
				const foreignObject = document.querySelector("foreignObject");
				const div = document.createElementNS("http://www.w3.org/1999/xhtml", "div");
				div.dir = "ltr";
				const p = document.createElementNS("http://www.w3.org/1999/xhtml", "p");
				const label = document.createElementNS("http://www.w3.org/1999/xhtml", "label");
				const progress = document.createElementNS("http://www.w3.org/1999/xhtml", "progress");
				label.append(progress);
				const span = document.createElementNS("http://www.w3.org/1999/xhtml", "span");
				span.textContent = "Loading names";
				label.append(span);
				p.append(label);
				div.append(p);
				foreignObject.prepend(div);
				let lock = false;
				let session = null;
				const broadcastChannel = new BroadcastChannel(sessionKey);
				const abortController = new AbortController();
				const signal = abortController.signal;
				const abort = abortController.abort.bind(abortController);
				const {promise, resolve} = Promise.withResolvers();
				broadcastChannel.addEventListener("message", (event) =&gt; {
					const data = event.data;
					const type = data.type;
					switch (type) {
						case "request": {
							if (lock &amp;&amp; signal.aborted) {
								broadcastChannel.postMessage({
									type: "response",
									session,
								});
							}
							break;
						}
						case "response": {
							if (!signal.aborted) {
								session = data.session;
								if (session != null) {
									const {players, leaderboards} = session;
									try {
										const cachedPlayers = JSON.stringify(players);
										const cachedLeaderboards = JSON.stringify(leaderboards);
										sessionStorage.setItem(playersKey, cachedPlayers);
										sessionStorage.setItem(leaderboardsKey, cachedLeaderboards);
									} catch {}
									console.log(`Successfully loaded names from peer`);
									abort();
									resolve();
								} else {
									console.log(`Unsuccessfully loaded names from peer`);
									abort();
									resolve();
								}
							}
							break;
						}
					}
				});
				try {
					const cachedPlayers = sessionStorage.getItem(playersKey);
					const cachedLeaderboards = sessionStorage.getItem(leaderboardsKey);
					if (cachedPlayers == null || cachedLeaderboards == null) {
						throw new Error();
					}
					const players = JSON.parse(cachedPlayers);
					const leaderboards = JSON.parse(cachedLeaderboards);
					session = {players, leaderboards};
				} catch {}
				if (session != null) {
					broadcastChannel.postMessage({
						type: "response",
						session,
					});
					console.log(`Successfully loaded names from local`);
					abort();
					resolve();
				} else {
					broadcastChannel.postMessage({
						type: "request",
					});
					console.log(`Unsuccessfully loaded names from local`);
				}
				(async () =&gt; {
					for (;;) {
						await navigator.locks.request(lockKey, async () =&gt; {
							lock = true;
							console.log(`Acquired lock`);
							if (!signal.aborted) {
								try {
									const players = Object.create(null);
									const leaderboards = Object.create(null);
									const games = ["9d3rrxyd", "w6jl2ned"];
									for (const gameId of games) {
										const {data} = await importDataScript(`https://www.speedrun.com/api/v1/games/${gameId}?embed=categories,levels,variables`, signal);
										const versions = data.variables.data.find((variable) =&gt; {
											return variable.name === "Version";
										});
										const levels = Object.fromEntries(data.levels.data.map((level) =&gt; {
											return [level.id, level];
										}));
										const categories = Object.fromEntries(data.categories.data.map((category) =&gt; {
											return [category.id, category];
										}));
										const variables = Object.fromEntries(data.variables.data.map((variable) =&gt; {
											return [variable.id, variable];
										}));
										console.log(`Got game`);
										await waitForTimeout(800, signal);
										const slice = 200;
										for (let offset = 0;; offset += slice) {
											const {data, pagination} = await importDataScript(`https://www.speedrun.com/api/v1/runs?game=${gameId}&amp;orderby=date&amp;direction=asc&amp;embed=players&amp;offset=${offset}&amp;max=${slice}`, signal);
											const {size} = pagination;
											if (size === 0) {
												break;
											}
											for (const run of data) {
												const status = run.status.status;
												if (status == null || status === "new") {
													continue;
												}
												const anonymous = (run.players.data[0]?.rel ?? "guest") !== "user";
												const player = !anonymous ? run.players.data[0].id : "814p2558";
												const playerName = !anonymous ? run.players.data[0].names.international : "anonymous";
												players[player] ??= playerName;
												const level = run.level;
												const category = run.category;
												const values = Object.entries(run.values).filter(([variable]) =&gt; {
													return variables[variable]?.["is-subcategory"] ?? false;
												}).map(([variable, value]) =&gt; {
													return `-${variable}.${value}`;
												});
												const leaderboard = `${level != null ? `l_${level}-` : ""}${category}${values.join("")}`;
												const levelName = run.level != null ? levels[run.level]?.name ?? null : null;
												const categoryName = categories[run.category]?.name ?? null;
												const valueNames = Object.entries(run.values).filter(([variable]) =&gt; {
													return variables[variable]?.["is-subcategory"] ?? false;
												}).map(([variable, value]) =&gt; {
													return variables[variable]?.values.values[value]?.label ?? null;
												}).filter((valueName) =&gt; {
													return valueName != null;
												});
												const leaderboardName = `${levelName != null ? `${levelName}: ` : ""}${categoryName ?? ""}${values.length !== 0 ? ` - ${valueNames.join(", ")}` : ""}`;
												leaderboards[leaderboard] ??= leaderboardName;
											}
											console.log(`Got runs ${offset}-${offset + size - 1}`);
											await waitForTimeout(800, signal);
										}
									}
									session = {players, leaderboards};
								} catch {}
								if (!signal.aborted) {
									if (session != null) {
										const {players, leaderboards} = session;
										try {
											const cachedPlayers = JSON.stringify(players);
											const cachedLeaderboards = JSON.stringify(leaderboards);
											sessionStorage.setItem(playersKey, cachedPlayers);
											sessionStorage.setItem(leaderboardsKey, cachedLeaderboards);
										} catch {}
										broadcastChannel.postMessage({
											type: "response",
											session,
										});
										console.log(`Successfully loaded names from remote`);
										abort();
										resolve();
									} else {
										broadcastChannel.postMessage({
											type: "response",
											session,
										});
										console.log(`Unsuccessfully loaded names from remote`);
										abort();
										resolve();
									}
								}
							}
							await new Promise((resolve) =&gt; {
								window.addEventListener("beforeunload", () =&gt; {
									resolve();
								}, {
									once: true,
								});
							});
						});
						lock = false;
						console.log(`Released lock`);
						await new Promise((resolve) =&gt; {
							window.addEventListener("pagehide", () =&gt; {
								resolve();
							}, {
								once: true,
							});
						});
						await new Promise((resolve) =&gt;{
							window.addEventListener("pageshow", () =&gt; {
								resolve();
							}, {
								once: true,
							});
						});
					}
				})();
				await promise;
				if (session == null) {
					const input = document.createElementNS("http://www.w3.org/1999/xhtml", "input");
					input.type = "checkbox";
					input.autocomplete = "off";
					input.disabled = true;
					input.indeterminate = true;
					progress.replaceWith(input);
					span.textContent = "Unavailable names";
					return;
				}
				const {players, leaderboards} = session;
				const hasPlayerRoot = rootTitleContent.includes(" for player");
				const hasLeaderboardRoot = rootTitleContent.includes(" for leaderboard");
				const rootTitle = {
					element: rootTitleElement,
					contentWithIds: rootTitleContent,
					contentWithNames: hasPlayerRoot ? rootTitleContent.replace(/ for player ([0-9_a-z]+)/, ($0, $1) =&gt; {
						return ` for player ${players[$1] ?? $1}`;
					}) : hasLeaderboardRoot ? rootTitleContent.replace(/ for leaderboard ((?:l_[0-9_a-z]+-)?[0-9_a-z]+(-[0-9_a-z]+\.[0-9_a-z]+)*)/, ($0, $1) =&gt; {
						return ` for leaderboard ${leaderboards[$1] ?? $1}`;
					}) : rootTitleContent,
				};
				const hasPlayerPaths = rootTitleContent.includes(" by player");
				const hasLeaderboardPaths = rootTitleContent.includes(" by leaderboard");
				const pathTitles = pathTitleElements.map((pathTitleElement, k) =&gt; {
					const pathTitleContent = pathTitleContents[k];
					const pathTitle = {
						element: pathTitleElement,
						contentWithIds: pathTitleContent,
						contentWithNames: hasPlayerPaths ? players[pathTitleContent] ?? pathTitleContent : hasLeaderboardPaths ? leaderboards[pathTitleContent] ?? pathTitleContent : pathTitleContent,
					};
					return pathTitle;
				});
				const input = document.createElementNS("http://www.w3.org/1999/xhtml", "input");
				input.type = "checkbox";
				input.autocomplete = "off";
				input.addEventListener("change", () =&gt; {
					input.disabled = true;
					if (input.checked) {
						rootTitle.element.textContent = rootTitle.contentWithNames;
						for (const pathTitle of pathTitles) {
							pathTitle.element.textContent = pathTitle.contentWithNames;
						}
					} else {
						rootTitle.element.textContent = rootTitle.contentWithIds;
						for (const pathTitle of pathTitles) {
							pathTitle.element.textContent = pathTitle.contentWithIds;
						}
					}
					input.disabled = false;
				});
				progress.replaceWith(input);
				span.textContent = "Reveal names";
			}, {
				once: true,
			});
		}
	</script>
	<script>
		"use strict";
		{
			document.addEventListener("DOMContentLoaded", (event) =&gt; {
				const minDateText = document.querySelector("foreignObject &gt; svg:first-of-type &gt; text");
				const maxDateText = document.querySelector("foreignObject &gt; svg:first-of-type + svg &gt; text");
				const startX = Math.round(Date.parse(minDateText.textContent) / 86400000);
				function xToDate(x) {
					const datetime = new Date((startX + x) * 86400000);
					const year = `${datetime.getUTCFullYear()}`.padStart(4, "0");
					const month = `${datetime.getUTCMonth() + 1}`.padStart(2, "0");
					const day = `${datetime.getUTCDate()}`.padStart(2, "0");
					const date = `${year}-${month}-${day}`;
					const size = date.length * 10;
					return {date, size};
				}
				function setMinDate(x) {
					const {date, size} = xToDate(Math.floor(x));
					minDateText.textLength.baseVal.value = size;
					minDateText.textContent = date;
				}
				function setMaxDate(x) {
					const {date, size} = xToDate(Math.ceil(x));
					maxDateText.textLength.baseVal.value = size;
					maxDateText.textContent = date;
				}
				const minValueText = document.querySelector("foreignObject &gt; svg:first-of-type + svg + svg &gt; text");
				const maxValueText = document.querySelector("foreignObject &gt; svg:first-of-type + svg + svg + svg &gt; text");
				const timed = minValueText.textContent.includes(":");
				function yToValue(y) {
					if (timed) {
						const minutes = `${(y - y % 6000) / 6000}`.padStart(2, "0");
						const seconds = `${(y - y % 100) / 100 % 60}`.padStart(2, "0");
						const centiseconds = `${y % 100}`.padStart(2, "0");
						const value = `${minutes}:${seconds}.${centiseconds}`;
						const size = value.length * 10;
						return {value, size};
					} else {
						const value = `${y}`;
						const size = value.length * 10;
						return {value, size};
					}
				}
				function setMinValue(y) {
					const {value, size} = yToValue(Math.floor(y));
					minValueText.textLength.baseVal.value = size;
					minValueText.textContent = value;
				}
				function setMaxValue(y) {
					const {value, size} = yToValue(Math.ceil(y));
					maxValueText.textLength.baseVal.value = size;
					maxValueText.textContent = value;
				}
				const svg = document.querySelector("foreignObject &gt; svg:first-of-type + svg + svg + svg + svg");
				svg.tabIndex = -1;
				svg.style.setProperty("outline", "0");
				if (CSS.supports("overflow-clip-margin", "8px")) {
					svg.style.setProperty("overflow", "clip");
					svg.style.setProperty("overflow-clip-margin", "8px");
				}
				svg.style.setProperty("touch-action", "none");
				const {x: baseX, y: baseY, width: baseWidth, height: baseHeight} = svg.viewBox.baseVal;
				let pointerCount = 0;
				const pointers = Object.create(null);
				const xPointerIds = [];
				const yPointerIds = [];
				let x = null;
				let y = null;
				let width = null;
				let height = null;
				svg.addEventListener("pointerdown", (event) =&gt; {
					const {pointerId} = event;
					if (pointerId in pointers) {
						return;
					}
					let {x: animX, y: animY, width: animWidth, height: animHeight} = svg.viewBox.baseVal;
					svg.setPointerCapture(pointerId);
					++pointerCount;
					const offsetX = event.offsetX;
					const offsetY = event.offsetY;
					pointers[pointerId] = {
						offsetX,
						offsetY,
					};
					xPointerIds.splice(xPointerIds.findLastIndex((pointerId) =&gt; {
						return pointers[pointerId].offsetX &lt;= offsetX;
					}) + 1, 0, pointerId);
					yPointerIds.splice(yPointerIds.findLastIndex((pointerId) =&gt; {
						return pointers[pointerId].offsetY &lt;= offsetY;
					}) + 1, 0, pointerId);
					const minOffsetX = pointers[xPointerIds[0]].offsetX;
					const minOffsetY = pointers[yPointerIds[0]].offsetY;
					const maxClientWidth = pointers[xPointerIds[pointerCount - 1]].offsetX - minOffsetX;
					const maxClientHeight = pointers[yPointerIds[pointerCount - 1]].offsetY - minOffsetY;
					width = Math.min(maxClientWidth / svg.clientWidth * animWidth, baseWidth);
					height = Math.min(maxClientHeight / svg.clientHeight * animHeight, baseHeight);
					x = Math.max(Math.min(animX + minOffsetX / svg.clientWidth * animWidth, baseX + baseWidth), baseX);
					y = Math.max(Math.min(animY + minOffsetY / svg.clientHeight * animHeight, baseY + baseHeight), baseY);
				});
				svg.addEventListener("pointerup", (event) =&gt; {
					const {pointerId} = event;
					if (!(pointerId in pointers)) {
						return;
					}
					let {x: animX, y: animY, width: animWidth, height: animHeight} = svg.viewBox.baseVal;
					svg.releasePointerCapture(pointerId);
					--pointerCount;
					delete pointers[pointerId];
					xPointerIds.splice(xPointerIds.lastIndexOf(pointerId), 1);
					yPointerIds.splice(yPointerIds.lastIndexOf(pointerId), 1);
					const minOffsetX = pointerCount !== 0 ? pointers[xPointerIds[0]].offsetX : null;
					const minOffsetY = pointerCount !== 0 ? pointers[yPointerIds[0]].offsetY : null;
					const maxClientWidth = pointerCount !== 0 ? pointers[xPointerIds[pointerCount - 1]].offsetX - minOffsetX : null;
					const maxClientHeight = pointerCount !== 0 ? pointers[yPointerIds[pointerCount - 1]].offsetY - minOffsetY : null;
					width = pointerCount !== 0 ? Math.min(maxClientWidth / svg.clientWidth * animWidth, baseWidth) : null;
					height = pointerCount !== 0 ? Math.min(maxClientHeight / svg.clientHeight * animHeight, baseHeight) : null;
					x = pointerCount !== 0 ? Math.max(Math.min(animX + minOffsetX / svg.clientWidth * animWidth, baseX + baseWidth), baseX) : null;
					y = pointerCount !== 0 ? Math.max(Math.min(animY + minOffsetY / svg.clientHeight * animHeight, baseY + baseHeight), baseY) : null;
				});
				svg.addEventListener("pointermove", (event) =&gt; {
					const {pointerId} = event;
					if (!(pointerId in pointers)) {
						return;
					}
					let {x: animX, y: animY, width: animWidth, height: animHeight} = svg.viewBox.baseVal;
					event.preventDefault();
					const offsetX = event.offsetX;
					const offsetY = event.offsetY;
					pointers[pointerId].offsetX = offsetX;
					pointers[pointerId].offsetY = offsetY;
					const oldXIndex = xPointerIds.lastIndexOf(pointerId);
					let newXIndex = oldXIndex;
					while (newXIndex &gt; 0 &amp;&amp; pointers[xPointerIds[newXIndex - 1]].offsetX &gt; offsetX) {
						--newXIndex;
					}
					xPointerIds.copyWithin(newXIndex + 1, newXIndex, oldXIndex);
					xPointerIds[newXIndex] = pointerId;
					while (newXIndex &lt; pointerCount - 1 &amp;&amp; pointers[xPointerIds[newXIndex + 1]].offsetX &lt; offsetX) {
						++newXIndex;
					}
					xPointerIds.copyWithin(oldXIndex, oldXIndex + 1, newXIndex + 1);
					xPointerIds[newXIndex] = pointerId;
					const oldYIndex = yPointerIds.lastIndexOf(pointerId);
					let newYIndex = oldYIndex;
					while (newYIndex &gt; 0 &amp;&amp; pointers[yPointerIds[newYIndex - 1]].offsetY &gt; offsetY) {
						--newYIndex;
					}
					yPointerIds.copyWithin(newYIndex + 1, newYIndex, oldYIndex);
					yPointerIds[newYIndex] = pointerId;
					while (newYIndex &lt; pointerCount - 1 &amp;&amp; pointers[yPointerIds[newYIndex + 1]].offsetY &lt; offsetY) {
						++newYIndex;
					}
					yPointerIds.copyWithin(oldYIndex, oldYIndex + 1, newYIndex + 1);
					yPointerIds[newYIndex] = pointerId;
					const minOffsetX = pointers[xPointerIds[0]].offsetX;
					const minOffsetY = pointers[yPointerIds[0]].offsetY;
					const maxClientWidth = pointers[xPointerIds[pointerCount - 1]].offsetX - minOffsetX;
					const maxClientHeight = pointers[yPointerIds[pointerCount - 1]].offsetY - minOffsetY;
					animWidth = width === 0 ? animWidth : maxClientWidth === 0 ? baseWidth : Math.min(svg.clientWidth / maxClientWidth * width, baseWidth);
					animHeight = height === 0 ? animHeight : maxClientHeight === 0 ? baseHeight : Math.min(svg.clientHeight / maxClientHeight * height, baseHeight);
					animX = Math.max(Math.min(x - minOffsetX / svg.clientWidth * animWidth, baseX + baseWidth - animWidth), baseX);
					animY = Math.max(Math.min(y - minOffsetY / svg.clientHeight * animHeight, baseY + baseHeight - animHeight), baseY);
					svg.viewBox.baseVal.x = animX;
					svg.viewBox.baseVal.y = animY;
					svg.viewBox.baseVal.width = animWidth;
					svg.viewBox.baseVal.height = animHeight;
					setMinDate(animX);
					setMaxDate(animX + animWidth);
					setMinValue(baseY * 2 + baseHeight - animHeight - animY);
					setMaxValue(baseY * 2 + baseHeight - animY);
				});
				const zoomIntensity = 1.25;
				svg.addEventListener("wheel", (event) =&gt; {
					let {x: animX, y: animY, width: animWidth, height: animHeight} = svg.viewBox.baseVal;
					const {ctrlKey, shiftKey} = event;
					event.preventDefault();
					if (pointerCount !== 0) {
						return;
					}
					if (!ctrlKey) {
						const vectorX = (shiftKey ? event.deltaY : event.deltaX) / svg.clientWidth * animWidth;
						const vectorY = (shiftKey ? 0 : event.deltaY) / svg.clientHeight * animHeight;
						animX = Math.max(Math.min(animX + vectorX, baseX + baseWidth - animWidth), baseX);
						animY = Math.max(Math.min(animY + vectorY, baseY + baseHeight - animHeight), baseY);
						svg.viewBox.baseVal.x = animX;
						svg.viewBox.baseVal.y = animY;
						setMinDate(animX);
						setMaxDate(animX + animWidth);
						setMinValue(baseY * 2 + baseHeight - animHeight - animY);
						setMaxValue(baseY * 2 + baseHeight - animY);
						return;
					}
					const ratioX = zoomIntensity ** Math.sign(shiftKey ? event.deltaY : event.deltaX);
					const ratioY = zoomIntensity ** Math.sign(shiftKey ? 0 : event.deltaY);
					const positionX = Math.max(0, Math.min(event.offsetX / svg.clientWidth, 1)) * animWidth;
					const positionY = Math.max(0, Math.min(event.offsetY / svg.clientHeight, 1)) * animHeight;
					animWidth = Math.min(animWidth * ratioX, baseWidth);
					animHeight = Math.min(animHeight * ratioY, baseHeight);
					animX = Math.max(Math.min(animX + positionX * (1 - ratioX), baseX + baseWidth - animWidth), baseX);
					animY = Math.max(Math.min(animY + positionY * (1 - ratioY), baseY + baseHeight - animHeight), baseY);
					svg.viewBox.baseVal.x = animX;
					svg.viewBox.baseVal.y = animY;
					svg.viewBox.baseVal.width = animWidth;
					svg.viewBox.baseVal.height = animHeight;
					setMinDate(animX);
					setMaxDate(animX + animWidth);
					setMinValue(baseY * 2 + baseHeight - animHeight - animY);
					setMaxValue(baseY * 2 + baseHeight - animY);
				}, {
					passive: false,
				});
				svg.addEventListener("keydown", (event) =&gt; {
					let {x: animX, y: animY, width: animWidth, height: animHeight} = svg.viewBox.baseVal;
					const {ctrlKey, shiftKey} = event;
					switch (event.code) {
						case "Space": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							const vectorY = shiftKey ? -animHeight : animHeight;
							animY = Math.max(Math.min(animY + vectorY, baseY + baseHeight - animHeight), baseY);
							svg.viewBox.baseVal.y = animY;
							setMinValue(baseY * 2 + baseHeight - animHeight - animY);
							setMaxValue(baseY * 2 + baseHeight - animY);
							return;
						}
						case "Equal":
						case "NumpadAdd":
						case "Minus":
						case "NumpadSubtract": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							const ratio = event.code === "Minus" || event.code === "NumpadSubtract" ? zoomIntensity : 1 / zoomIntensity;
							if (!shiftKey) {
								animHeight = Math.min(animHeight * ratio, baseHeight);
								animY = Math.max(Math.min(animY, baseY + baseHeight - animHeight), baseY);
								svg.viewBox.baseVal.y = animY;
								svg.viewBox.baseVal.height = animHeight;
								setMinValue(baseY * 2 + baseHeight - animHeight - animY);
								setMaxValue(baseY * 2 + baseHeight - animY);
								return;
							}
							animWidth = Math.min(animWidth * ratio, baseWidth);
							animX = Math.max(Math.min(animX, baseX + baseWidth - animWidth), baseX);
							svg.viewBox.baseVal.x = animX;
							svg.viewBox.baseVal.width = animWidth;
							setMinDate(animX);
							setMaxDate(animX + animWidth);
							return;
						}
						case "Digit0":
						case "Numpad0": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							if (!shiftKey) {
								animY = baseY;
								animHeight = baseHeight;
								svg.viewBox.baseVal.y = animY;
								svg.viewBox.baseVal.height = animHeight;
								setMinValue(baseY * 2 + baseHeight - animHeight - animY);
								setMaxValue(baseY * 2 + baseHeight - animY);
								return;
							}
							animX = baseX;
							animWidth = baseWidth;
							svg.viewBox.baseVal.x = animX;
							svg.viewBox.baseVal.width = animWidth;
							setMinDate(animX);
							setMaxDate(animX + animWidth);
							return;
						}
					}
					switch (event.key) {
						case "ArrowRight": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							const vectorX = 50 / svg.clientWidth * animWidth;
							animX = Math.max(Math.min(animX + vectorX, baseX + baseWidth - animWidth), baseX);
							svg.viewBox.baseVal.x = animX;
							setMinDate(animX);
							setMaxDate(animX + animWidth);
							return;
						}
						case "ArrowLeft": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							const vectorX = -50 / svg.clientWidth * animWidth;
							animX = Math.max(Math.min(animX + vectorX, baseX + baseWidth - animWidth), baseX);
							svg.viewBox.baseVal.x = animX;
							setMinDate(animX);
							setMaxDate(animX + animWidth);
							return;
						}
						case "ArrowDown": {
							if (!ctrlKey) {
								event.preventDefault();
								if (pointerCount !== 0) {
									return;
								}
								const vectorY = 50 / svg.clientHeight * animHeight;
								animY = Math.max(Math.min(animY + vectorY, baseY + baseHeight - animHeight), baseY);
								svg.viewBox.baseVal.y = animY;
								setMinValue(baseY * 2 + baseHeight - animHeight - animY);
								setMaxValue(baseY * 2 + baseHeight - animY);
								return;
							}
						}
						case "End": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							animY = baseY + baseHeight - animHeight;
							svg.viewBox.baseVal.y = animY;
							setMinValue(baseY * 2 + baseHeight - animHeight - animY);
							setMaxValue(baseY * 2 + baseHeight - animY);
							return;
						}
						case "ArrowUp": {
							if (!ctrlKey) {
								event.preventDefault();
								if (pointerCount !== 0) {
									return;
								}
								const vectorY = -50 / svg.clientHeight * animHeight;
								animY = Math.max(Math.min(animY + vectorY, baseY + baseHeight - animHeight), baseY);
								svg.viewBox.baseVal.y = animY;
								setMinValue(baseY * 2 + baseHeight - animHeight - animY);
								setMaxValue(baseY * 2 + baseHeight - animY);
								return;
							}
						}
						case "Home": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							animY = baseY;
							svg.viewBox.baseVal.y = animY;
							setMinValue(baseY * 2 + baseHeight - animHeight - animY);
							setMaxValue(baseY * 2 + baseHeight - animY);
							return;
						}
						case "PageDown":
						case "PageUp":
						case " ": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							const vectorY = event.key === "PageUp" || event.key === " " &amp;&amp; shiftKey ? -animHeight : animHeight;
							animY = Math.max(Math.min(animY + vectorY, baseY + baseHeight - animHeight), baseY);
							svg.viewBox.baseVal.y = animY;
							setMinValue(baseY * 2 + baseHeight - animHeight - animY);
							setMaxValue(baseY * 2 + baseHeight - animY);
							return;
						}
						case "+":
						case "-": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							const ratio = event.key === "-" ? 1 / zoomIntensity : zoomIntensity;
							animWidth = Math.min(animWidth * ratio, baseWidth);
							animHeight = Math.min(animHeight * ratio, baseHeight);
							animX = Math.max(Math.min(animX, baseX + baseWidth - animWidth), baseX);
							animY = Math.max(Math.min(animY, baseY + baseHeight - animHeight), baseY);
							svg.viewBox.baseVal.x = animX;
							svg.viewBox.baseVal.y = animY;
							svg.viewBox.baseVal.width = animWidth;
							svg.viewBox.baseVal.height = animHeight;
							setMinDate(animX);
							setMaxDate(animX + animWidth);
							setMinValue(baseY * 2 + baseHeight - animHeight - animY);
							setMaxValue(baseY * 2 + baseHeight - animY);
							return;
						}
						case "0": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							animX = baseX;
							animY = baseY;
							animWidth = baseWidth;
							animHeight = baseHeight;
							svg.viewBox.baseVal.x = animX;
							svg.viewBox.baseVal.y = animY;
							svg.viewBox.baseVal.width = animWidth;
							svg.viewBox.baseVal.height = animHeight;
							setMinDate(animX);
							setMaxDate(animX + animWidth);
							setMinValue(baseY * 2 + baseHeight - animHeight - animY);
							setMaxValue(baseY * 2 + baseHeight - animY);
							return;
						}
					}
				});
				svg.addEventListener("focusin", (event) =&gt; {
					if (!event.target.matches(":focus-visible")) {
						return;
					}
					event.preventDefault();
					if (pointerCount !== 0) {
						return;
					}
					let {x: animX, y: animY, width: animWidth, height: animHeight} = svg.viewBox.baseVal;
					let {x, y, width, height} = event.target.getBBox();
					width = Math.round(width);
					height = Math.round(height);
					x = Math.round(x);
					y = baseY * 2 + baseHeight - height - Math.round(y);
					if (x + width &lt; animX) {
						animX = x;
						if (width &gt; animWidth) {
							animX += (width - animWidth) / 2;
						}
					} else if (x &gt; animX + animWidth) {
						animX = x + width - animWidth;
						if (width &gt; animWidth) {
							animX -= (width - animWidth) / 2;
						}
					}
					if (y + height &lt; animY) {
						animY = y;
						if (height &gt; animHeight) {
							animY += (height - animHeight) / 2;
						}
					} else if (y &gt; animY + animHeight) {
						animY = y + height - animHeight;
						if (height &gt; animHeight) {
							animY -= (height - animHeight) / 2;
						}
					}
					svg.viewBox.baseVal.x = animX;
					svg.viewBox.baseVal.y = animY;
					setMinDate(animX);
					setMaxDate(animX + animWidth);
					setMinValue(baseY * 2 + baseHeight - animHeight - animY);
					setMaxValue(baseY * 2 + baseHeight - animY);
				});
			}, {
				once: true,
			});
		}
	</script>
	<foreignObject x="0" y="0" width="100%" height="100%" style="--date-size: 100; --value-size: 30;">
		<svg viewBox="0 0 100 20">
			<text x="50" y="10" dx="0" dy="2" textLength="100" lengthAdjust="spacingAndGlyphs" text-anchor="middle" dominant-baseline="middle">2017-12-25</text>
		</svg>
		<svg viewBox="0 0 100 20">
			<text x="50" y="10" dx="0" dy="2" textLength="100" lengthAdjust="spacingAndGlyphs" text-anchor="middle" dominant-baseline="middle">2026-04-29</text>
		</svg>
		<svg viewBox="0 0 30 20">
			<text x="15" y="10" dx="0" dy="2" textLength="10" lengthAdjust="spacingAndGlyphs" text-anchor="middle" dominant-baseline="middle">0</text>
		</svg>
		<svg viewBox="0 0 30 20">
			<text x="15" y="10" dx="0" dy="2" textLength="30" lengthAdjust="spacingAndGlyphs" text-anchor="middle" dominant-baseline="middle">194</text>
		</svg>
		<svg viewBox="0 0 3047 194" preserveAspectRatio="none" overflow="visible">
			<g transform="translate(0 194) scale(1 -1)" style="--count: 8;">
				<g>
					<rect x="0" y="0" width="3047" height="194"/>
				</g>
				<g style="--index: 0;">
					<path d="M0,0L0,0L0,1L6,1L6,1L9,1L9,1L2629,1L2629,1L2799,1L2799,1L2943,1L2943,1L2983,1L2983,1L2992,1L2992,1L3047,1" tabindex="0">
						<title>l_r9gn1qpd-xd144xzd-38dz6v1n.ln8kgdol</title>
					</path>
				</g>
				<g style="--index: 1;">
					<path d="M0,0L4,0L4,1L5,1L5,2L6,2L6,1L9,1L9,1L10,1L10,3L12,3L12,3L2629,3L2629,3L2663,3L2663,3L2943,3L2943,3L2953,3L2953,3L2980,3L2980,3L2987,3L2987,3L2992,3L2992,3L3038,3L3038,4L3043,4L3043,5L3047,5" tabindex="0">
						<title>l_r9gn1qpd-9d8eewld-0nwoqwkl.q75zmmn1</title>
					</path>
				</g>
				<g style="--index: 2;">
					<path d="M0,0L21,0L21,2L22,2L22,2L30,2L30,2L71,2L71,1L80,1L80,2L87,2L87,3L108,3L108,2L117,2L117,2L370,2L370,3L597,3L597,4L751,4L751,5L772,5L772,6L882,6L882,6L907,6L907,7L990,7L990,8L1047,8L1047,9L1048,9L1048,10L1049,10L1049,11L1063,11L1063,12L1066,12L1066,13L1068,13L1068,14L1069,14L1069,15L1070,15L1070,16L1083,16L1083,17L1085,17L1085,18L1088,18L1088,18L1089,18L1089,17L1129,17L1129,17L1143,17L1143,18L1160,18L1160,19L1163,19L1163,20L1198,20L1198,21L1202,21L1202,22L1206,22L1206,23L1220,23L1220,24L1226,24L1226,25L1232,25L1232,25L1256,25L1256,26L1270,26L1270,27L1275,27L1275,27L1283,27L1283,27L1304,27L1304,28L1382,28L1382,27L1383,27L1383,28L1392,28L1392,29L1403,29L1403,30L1406,30L1406,32L1427,32L1427,33L1439,33L1439,33L1442,33L1442,34L1445,34L1445,33L1458,33L1458,34L1477,34L1477,34L1481,34L1481,35L1482,35L1482,36L1493,36L1493,37L1496,37L1496,38L1508,38L1508,39L1511,39L1511,40L1516,40L1516,41L1523,41L1523,42L1525,42L1525,43L1527,43L1527,42L1532,42L1532,43L1534,43L1534,43L1539,43L1539,44L1541,44L1541,45L1542,45L1542,46L1546,46L1546,47L1549,47L1549,48L1564,48L1564,49L1579,49L1579,50L1587,50L1587,51L1618,51L1618,52L1619,52L1619,53L1620,53L1620,54L1623,54L1623,55L1625,55L1625,56L1653,56L1653,57L1655,57L1655,58L1657,58L1657,59L1661,59L1661,60L1664,60L1664,61L1665,61L1665,62L1666,62L1666,63L1676,63L1676,65L1694,65L1694,66L1696,66L1696,67L1699,67L1699,68L1701,68L1701,70L1703,70L1703,70L1706,70L1706,72L1708,72L1708,73L1710,73L1710,72L1713,72L1713,73L1721,73L1721,74L1722,74L1722,75L1726,75L1726,76L1729,76L1729,77L1732,77L1732,77L1737,77L1737,78L1747,78L1747,79L1749,79L1749,80L1751,80L1751,81L1752,81L1752,82L1754,82L1754,83L1755,83L1755,84L1777,84L1777,85L1784,85L1784,86L1801,86L1801,87L1805,87L1805,88L1809,88L1809,89L1813,89L1813,90L1819,90L1819,90L1832,90L1832,91L1837,91L1837,91L1842,91L1842,92L1850,92L1850,93L1851,93L1851,93L1856,93L1856,93L1869,93L1869,94L1874,94L1874,95L1876,95L1876,97L1880,97L1880,99L1881,99L1881,100L1890,100L1890,101L1898,101L1898,101L1899,101L1899,100L1903,100L1903,101L1908,101L1908,101L1912,101L1912,102L1923,102L1923,103L1924,103L1924,104L1928,104L1928,104L1932,104L1932,104L1934,104L1934,105L1937,105L1937,106L1938,106L1938,107L1958,107L1958,108L1959,108L1959,109L1966,109L1966,109L1968,109L1968,110L1973,110L1973,111L1980,111L1980,112L1981,112L1981,112L1983,112L1983,112L1993,112L1993,113L2007,113L2007,114L2014,114L2014,115L2017,115L2017,115L2037,115L2037,116L2048,116L2048,117L2089,117L2089,118L2101,118L2101,118L2122,118L2122,119L2128,119L2128,120L2134,120L2134,121L2140,121L2140,122L2148,122L2148,123L2175,123L2175,124L2191,124L2191,123L2204,123L2204,124L2229,124L2229,125L2271,125L2271,126L2272,126L2272,127L2292,127L2292,128L2295,128L2295,129L2302,129L2302,130L2304,130L2304,129L2327,129L2327,130L2337,130L2337,131L2339,131L2339,131L2345,131L2345,132L2375,132L2375,132L2378,132L2378,131L2401,131L2401,133L2408,133L2408,134L2416,134L2416,135L2429,135L2429,135L2430,135L2430,135L2437,135L2437,136L2442,136L2442,136L2446,136L2446,137L2448,137L2448,138L2458,138L2458,139L2470,139L2470,139L2478,139L2478,140L2498,140L2498,140L2503,140L2503,140L2504,140L2504,140L2519,140L2519,140L2531,140L2531,141L2537,141L2537,142L2540,142L2540,143L2546,143L2546,144L2549,144L2549,145L2558,145L2558,144L2577,144L2577,144L2583,144L2583,145L2585,145L2585,146L2595,146L2595,147L2596,147L2596,148L2600,148L2600,149L2606,149L2606,150L2608,150L2608,150L2611,150L2611,150L2616,150L2616,150L2625,150L2625,151L2634,151L2634,153L2639,153L2639,154L2640,154L2640,156L2643,156L2643,156L2646,156L2646,157L2650,157L2650,157L2661,157L2661,156L2663,156L2663,156L2664,156L2664,157L2666,157L2666,157L2667,157L2667,158L2668,158L2668,158L2672,158L2672,159L2673,159L2673,159L2675,159L2675,160L2680,160L2680,161L2684,161L2684,162L2720,162L2720,163L2735,163L2735,162L2741,162L2741,162L2747,162L2747,162L2752,162L2752,164L2759,164L2759,165L2764,165L2764,165L2768,165L2768,165L2772,165L2772,165L2774,165L2774,165L2776,165L2776,165L2789,165L2789,166L2797,166L2797,166L2799,166L2799,167L2808,167L2808,168L2819,168L2819,168L2823,168L2823,169L2826,169L2826,170L2839,170L2839,170L2852,170L2852,171L2855,171L2855,172L2862,172L2862,173L2865,173L2865,174L2875,174L2875,175L2893,175L2893,176L2894,176L2894,176L2895,176L2895,177L2900,177L2900,177L2903,177L2903,177L2909,177L2909,177L2912,177L2912,178L2918,178L2918,179L2929,179L2929,180L2937,180L2937,180L2938,180L2938,181L2939,181L2939,181L2942,181L2942,181L2954,181L2954,181L2960,181L2960,181L2970,181L2970,181L2972,181L2972,181L2974,181L2974,181L2975,181L2975,181L2989,181L2989,182L2992,182L2992,183L2998,183L2998,185L3002,185L3002,185L3004,185L3004,186L3006,186L3006,187L3007,187L3007,185L3011,185L3011,186L3018,186L3018,187L3031,187L3031,189L3034,189L3034,190L3036,190L3036,191L3040,191L3040,191L3041,191L3041,192L3045,192L3045,193L3047,193L3047,194L3047,194" tabindex="0">
						<title>l_r9gn1qpd-9d8eewld-0nwoqwkl.p12dx87q</title>
					</path>
				</g>
				<g style="--index: 3;">
					<path d="M0,0L32,0L32,5L41,5L41,4L44,4L44,3L87,3L87,5L108,5L108,5L115,5L115,5L597,5L597,6L657,6L657,6L857,6L857,6L871,6L871,6L933,6L933,6L980,6L980,7L1048,7L1048,7L1064,7L1064,7L1065,7L1065,7L1125,7L1125,7L1132,7L1132,7L1139,7L1139,8L1140,8L1140,8L1144,8L1144,8L1154,8L1154,8L1163,8L1163,9L1180,9L1180,9L1200,9L1200,9L1208,9L1208,9L1211,9L1211,10L1221,10L1221,10L1223,10L1223,10L1377,10L1377,11L1409,11L1409,11L1480,11L1480,11L1485,11L1485,11L1495,11L1495,11L1496,11L1496,12L1516,12L1516,13L1532,13L1532,13L1536,13L1536,13L1538,13L1538,14L1587,14L1587,14L1618,14L1618,14L1646,14L1646,15L1653,15L1653,15L1657,15L1657,15L1658,15L1658,15L1676,15L1676,15L1689,15L1689,15L1701,15L1701,15L1705,15L1705,15L1716,15L1716,15L1717,15L1717,15L1751,15L1751,16L1770,16L1770,17L1773,17L1773,17L1819,17L1819,18L1860,18L1860,19L1908,19L1908,19L1912,19L1912,19L1923,19L1923,19L1952,19L1952,19L1959,19L1959,20L1966,20L1966,20L2001,20L2001,20L2013,20L2013,20L2058,20L2058,21L2064,21L2064,21L2069,21L2069,22L2175,22L2175,22L2182,22L2182,22L2212,22L2212,22L2321,22L2321,23L2332,23L2332,24L2337,24L2337,24L2352,24L2352,24L2365,24L2365,24L2367,24L2367,24L2378,24L2378,24L2384,24L2384,25L2386,25L2386,25L2440,25L2440,25L2470,25L2470,25L2484,25L2484,26L2513,26L2513,26L2529,26L2529,26L2538,26L2538,27L2551,27L2551,27L2554,27L2554,28L2572,28L2572,28L2577,28L2577,28L2592,28L2592,28L2600,28L2600,29L2607,29L2607,30L2608,30L2608,30L2610,30L2610,30L2613,30L2613,30L2616,30L2616,31L2617,31L2617,31L2622,31L2622,31L2628,31L2628,31L2638,31L2638,31L2658,31L2658,32L2662,32L2662,32L2668,32L2668,32L2672,32L2672,32L2673,32L2673,32L2684,32L2684,32L2686,32L2686,32L2703,32L2703,32L2747,32L2747,32L2764,32L2764,32L2767,32L2767,32L2786,32L2786,32L2789,32L2789,32L2793,32L2793,32L2796,32L2796,32L2801,32L2801,32L2809,32L2809,32L2814,32L2814,32L2815,32L2815,32L2820,32L2820,32L2823,32L2823,32L2825,32L2825,32L2827,32L2827,32L2838,32L2838,32L2840,32L2840,32L2862,32L2862,32L2867,32L2867,32L2878,32L2878,32L2883,32L2883,32L2892,32L2892,32L2893,32L2893,33L2896,33L2896,33L2900,33L2900,33L2903,33L2903,33L2904,33L2904,33L2909,33L2909,33L2916,33L2916,33L2923,33L2923,33L2924,33L2924,33L2925,33L2925,33L2926,33L2926,33L2928,33L2928,33L2931,33L2931,33L2933,33L2933,33L2934,33L2934,33L2937,33L2937,33L2938,33L2938,33L2939,33L2939,33L2940,33L2940,33L2948,33L2948,33L2950,33L2950,33L2954,33L2954,34L2955,34L2955,34L2959,34L2959,34L2960,34L2960,34L2965,34L2965,34L2966,34L2966,34L2969,34L2969,34L2972,34L2972,34L2987,34L2987,34L2988,34L2988,34L2992,34L2992,34L2996,34L2996,34L3000,34L3000,34L3002,34L3002,35L3003,35L3003,34L3010,34L3010,34L3017,34L3017,34L3021,34L3021,34L3024,34L3024,34L3025,34L3025,34L3028,34L3028,34L3029,34L3029,34L3037,34L3037,34L3040,34L3040,34L3041,34L3041,34L3047,34L3047,34L3047,34" tabindex="0">
						<title>l_r9gn1qpd-9d8eewld-0nwoqwkl.xqkjmxnl</title>
					</path>
				</g>
				<g style="--index: 4;">
					<path d="M0,0L32,0L32,4L33,4L33,4L42,4L42,3L84,3L84,4L97,4L97,4L115,4L115,4L429,4L429,4L596,4L596,5L656,5L656,6L868,6L868,6L886,6L886,6L1048,6L1048,7L1061,7L1061,7L1070,7L1070,7L1087,7L1087,8L1120,8L1120,9L1121,9L1121,8L1126,8L1126,8L1155,8L1155,8L1163,8L1163,9L1200,9L1200,8L1214,8L1214,9L1217,9L1217,10L1414,10L1414,11L1440,11L1440,12L1443,12L1443,13L1444,13L1444,14L1469,14L1469,14L1499,14L1499,15L1500,15L1500,16L1528,16L1528,17L1533,17L1533,18L1552,18L1552,19L1553,19L1553,18L1568,18L1568,19L1588,19L1588,20L1593,20L1593,21L1595,21L1595,20L1597,20L1597,21L1615,21L1615,21L1617,21L1617,22L1629,22L1629,23L1656,23L1656,24L1676,24L1676,25L1681,25L1681,26L1698,26L1698,27L1719,27L1719,28L1721,28L1721,29L1725,29L1725,30L1787,30L1787,31L1793,31L1793,32L1857,32L1857,33L1902,33L1902,34L1907,34L1907,34L1910,34L1910,35L1912,35L1912,36L1923,36L1923,39L1952,39L1952,40L1961,40L1961,41L1963,41L1963,42L1966,42L1966,43L1980,43L1980,45L1986,45L1986,46L1994,46L1994,47L1996,47L1996,46L2005,46L2005,47L2024,47L2024,48L2032,48L2032,49L2036,49L2036,51L2049,51L2049,52L2052,52L2052,53L2058,53L2058,54L2064,54L2064,55L2069,55L2069,56L2085,56L2085,56L2099,56L2099,56L2112,56L2112,57L2113,57L2113,58L2116,58L2116,58L2122,58L2122,59L2175,59L2175,60L2179,60L2179,60L2230,60L2230,61L2231,61L2231,62L2233,62L2233,62L2234,62L2234,63L2235,63L2235,62L2249,62L2249,63L2254,63L2254,64L2276,64L2276,65L2282,65L2282,66L2288,66L2288,67L2299,67L2299,68L2321,68L2321,69L2336,69L2336,70L2341,70L2341,71L2351,71L2351,73L2368,73L2368,74L2381,74L2381,75L2387,75L2387,76L2388,76L2388,77L2391,77L2391,77L2393,77L2393,77L2415,77L2415,77L2417,77L2417,78L2419,78L2419,79L2431,79L2431,80L2432,80L2432,80L2433,80L2433,79L2441,79L2441,80L2448,80L2448,81L2449,81L2449,82L2474,82L2474,83L2484,83L2484,84L2511,84L2511,85L2526,85L2526,86L2534,86L2534,87L2535,87L2535,88L2547,88L2547,89L2585,89L2585,90L2593,90L2593,92L2597,92L2597,93L2603,93L2603,94L2621,94L2621,96L2641,96L2641,96L2643,96L2643,97L2652,97L2652,97L2662,97L2662,98L2668,98L2668,99L2670,99L2670,100L2672,100L2672,100L2678,100L2678,100L2684,100L2684,101L2685,101L2685,102L2686,102L2686,102L2713,102L2713,103L2729,103L2729,105L2732,105L2732,106L2738,106L2738,106L2747,106L2747,107L2748,107L2748,107L2750,107L2750,108L2751,108L2751,107L2752,107L2752,108L2759,108L2759,109L2762,109L2762,110L2763,110L2763,111L2764,111L2764,111L2769,111L2769,112L2774,112L2774,112L2783,112L2783,113L2791,113L2791,114L2793,114L2793,115L2805,115L2805,116L2806,116L2806,117L2809,117L2809,118L2812,118L2812,119L2822,119L2822,120L2823,120L2823,121L2826,121L2826,122L2827,122L2827,121L2829,121L2829,122L2833,122L2833,123L2834,123L2834,124L2835,124L2835,125L2838,125L2838,126L2841,126L2841,127L2847,127L2847,127L2857,127L2857,128L2858,128L2858,129L2862,129L2862,131L2868,131L2868,132L2869,132L2869,133L2876,133L2876,134L2878,134L2878,134L2882,134L2882,135L2889,135L2889,135L2892,135L2892,137L2894,137L2894,137L2896,137L2896,138L2897,138L2897,139L2903,139L2903,139L2906,139L2906,140L2910,140L2910,139L2916,139L2916,139L2917,139L2917,140L2919,140L2919,141L2921,141L2921,141L2924,141L2924,142L2927,142L2927,143L2932,143L2932,144L2934,144L2934,144L2936,144L2936,144L2937,144L2937,144L2938,144L2938,145L2939,145L2939,147L2946,147L2946,148L2949,148L2949,149L2952,149L2952,150L2954,150L2954,150L2955,150L2955,150L2956,150L2956,151L2957,151L2957,151L2960,151L2960,152L2969,152L2969,153L2972,153L2972,154L2974,154L2974,154L2975,154L2975,154L2980,154L2980,155L2982,155L2982,156L2985,156L2985,157L2986,157L2986,158L2987,158L2987,159L2989,159L2989,160L2990,160L2990,161L2994,161L2994,162L3001,162L3001,163L3004,163L3004,165L3007,165L3007,165L3008,165L3008,165L3015,165L3015,166L3017,166L3017,167L3018,167L3018,168L3023,168L3023,167L3024,167L3024,168L3025,168L3025,169L3026,169L3026,170L3027,170L3027,171L3028,171L3028,172L3030,172L3030,173L3035,173L3035,173L3036,173L3036,174L3038,174L3038,174L3041,174L3041,175L3044,175L3044,177L3047,177L3047,178L3047,178" tabindex="0">
						<title>l_r9gn1qpd-9d8eewld-0nwoqwkl.81ppdgg1</title>
					</path>
				</g>
				<g style="--index: 5;">
					<path d="M0,0L33,0L33,1L200,1L200,1L1023,1L1023,2L1044,2L1044,2L1109,2L1109,3L1229,3L1229,4L1241,4L1241,5L1391,5L1391,5L1411,5L1411,6L1706,6L1706,6L2672,6L2672,6L2813,6L2813,6L2820,6L2820,6L2946,6L2946,6L2992,6L2992,6L3043,6L3043,6L3047,6" tabindex="0">
						<title>l_r9gn1qpd-xd144xzd-38dz6v1n.814y45w1</title>
					</path>
				</g>
				<g style="--index: 6;">
					<path d="M0,0L105,0L105,1L107,1L107,2L108,2L108,1L109,1L109,2L195,2L195,2L196,2L196,2L877,2L877,2L1061,2L1061,2L1069,2L1069,2L1084,2L1084,2L1159,2L1159,2L1777,2L1777,2L1865,2L1865,3L1939,3L1939,3L1940,3L1940,3L1971,3L1971,3L2134,3L2134,3L2286,3L2286,3L2296,3L2296,3L2329,3L2329,3L2466,3L2466,3L2476,3L2476,3L2478,3L2478,3L2564,3L2564,3L2566,3L2566,3L2626,3L2626,3L2637,3L2637,3L2739,3L2739,3L2748,3L2748,3L2758,3L2758,3L2759,3L2759,3L2764,3L2764,3L2767,3L2767,3L2768,3L2768,3L2769,3L2769,3L2783,3L2783,3L2797,3L2797,3L2805,3L2805,3L2807,3L2807,3L2821,3L2821,3L2829,3L2829,3L2851,3L2851,3L2862,3L2862,3L2906,3L2906,3L2949,3L2949,3L2961,3L2961,3L2970,3L2970,3L2977,3L2977,3L2994,3L2994,3L2995,3L2995,3L2999,3L2999,3L3000,3L3000,3L3012,3L3012,3L3015,3L3015,4L3018,4L3018,4L3035,4L3035,4L3047,4" tabindex="0">
						<title>l_495zlx39-9d8eewld-2lgr793n.81ppd5g1</title>
					</path>
				</g>
				<g style="--index: 7;">
					<path d="M0,0L249,0L249,1L252,1L252,2L262,2L262,2L1113,2L1113,3L1130,3L1130,4L1229,4L1229,5L1244,5L1244,6L1266,6L1266,5L1319,5L1319,5L1369,5L1369,6L1698,6L1698,6L1972,6L1972,6L1976,6L1976,6L2016,6L2016,7L2068,7L2068,8L2127,8L2127,9L2180,9L2180,8L2199,8L2199,8L2200,8L2200,8L2299,8L2299,8L2323,8L2323,8L2535,8L2535,8L2610,8L2610,8L2615,8L2615,8L2625,8L2625,8L2632,8L2632,8L2765,8L2765,8L2771,8L2771,8L2903,8L2903,8L2945,8L2945,8L2956,8L2956,8L2963,8L2963,8L2973,8L2973,8L3008,8L3008,8L3035,8L3035,9L3036,9L3036,9L3037,9L3037,9L3043,9L3043,10L3046,10L3046,10L3047,10L3047,10L3047,10" tabindex="0">
						<title>l_ldykv1zw-rkl3znrk-wl3059ol.4qypgj4q</title>
					</path>
				</g>
			</g>
		</svg>
	</foreignObject>
</svg>
