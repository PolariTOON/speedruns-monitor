<svg xmlns="http://www.w3.org/2000/svg" lang="en">
	<title>Score by leaderboard for player j0grn39x</title>
	<metadata>
		<meta xmlns="http://www.w3.org/1999/xhtml" charset="utf-8" />
		<meta xmlns="http://www.w3.org/1999/xhtml" name="viewport" content="width=device-width" />
		<meta xmlns="http://www.w3.org/1999/xhtml" name="color-scheme" content="dark light" />
	</metadata>
	<style>
		*,
		::before,
		::after {
			box-sizing: border-box;
		}
		:root {
			background: var(--canvas-background);
			color: var(--canvas-foreground);
			fill: var(--canvas-background);
			stroke: var(--canvas-foreground);
			stroke-linejoin: round;
			stroke-linecap: round;
		}
		foreignObject {
			pointer-events: none;
		}
		foreignObject::before,
		foreignObject::after {
			content: "";
			position: absolute;
			z-index: 1;
			left: 20px;
			bottom: 20px;
			pointer-events: auto;
		}
		foreignObject::before {
			width: calc(100% - 40px);
			height: calc(20px + var(--date-size) * 1px);
		}
		foreignObject::after {
			width: calc(20px + var(--value-size) * 1px);
			height: calc(100% - 40px);
		}
		foreignObject &gt; div {
			position: absolute;
			z-index: 4;
			right: 0;
			top: 0;
			margin: 20px;
			border: 1px solid var(--canvas-foreground);
			background: var(--highlighted);
			font: 16px / 1.25 serif;
			transition: opacity 1s;
			pointer-events: auto;
		}
		foreignObject:is(:hover, :focus-within) &gt; div:not(:hover, :focus-within) {
			opacity: 0;
			pointer-events: none;
		}
		foreignObject &gt; div &gt; p {
			margin: 0;
		}
		label {
			display: table;
			border-collapse: separate;
			border-spacing: 10px;
			direction: rtl;
		}
		progress,
		input[type="checkbox"] {
			display: table-cell;
			width: 16px;
			height: 16px;
			margin: 2px;
			accent-color: var(--canvas-foreground);
			vertical-align: top;
			direction: ltr;
		}
		progress + span,
		input[type="checkbox"] + span {
			display: table-cell;
			vertical-align: top;
			direction: ltr;
		}
		input[type="checkbox"]:not(:disabled):is(:hover, :focus-within) + span {
			text-decoration: underline;
		}
		foreignObject &gt; svg:first-of-type,
		foreignObject &gt; svg:first-of-type + svg,
		foreignObject &gt; svg:first-of-type + svg + svg,
		foreignObject &gt; svg:first-of-type + svg + svg + svg {
			position: absolute;
			z-index: 2;
			margin: 0;
			padding: 10px;
			background: var(--highlighted);
			font: 16px / 1.25 serif;
			font-weight: bold;
			pointer-events: auto;
		}
		foreignObject &gt; svg:first-of-type,
		foreignObject &gt; svg:first-of-type + svg + svg {
			border-top: 1px solid #0000;
			border-bottom: 1px solid var(--canvas-foreground);
		}
		foreignObject &gt; svg:first-of-type + svg,
		foreignObject &gt; svg:first-of-type + svg + svg + svg {
			border-top: 1px dashed var(--canvas-foreground);
			border-bottom: 1px dashed #0000;
		}
		foreignObject &gt; svg:first-of-type,
		foreignObject &gt; svg:first-of-type + svg {
			width: calc(20px + var(--date-size) * 1px);
			height: 42px;
			bottom: 20px;
		}
		foreignObject &gt; svg:first-of-type {
			left: calc(40px + var(--value-size) * 1px);
			transform: rotate(90deg) translate(-100%, 0);
			transform-origin: left bottom;
		}
		foreignObject &gt; svg:first-of-type + svg {
			right: 20px;
			transform: rotate(90deg) translate(0, 100%);
			transform-origin: right bottom;
		}
		foreignObject &gt; svg:first-of-type + svg + svg,
		foreignObject &gt; svg:first-of-type + svg + svg + svg {
			width: calc(20px + var(--value-size) * 1px);
			height: 42px;
			left: 20px;
		}
		foreignObject &gt; svg:first-of-type + svg + svg {
			bottom: calc(40px + var(--date-size) * 1px);
		}
		foreignObject &gt; svg:first-of-type + svg + svg + svg {
			top: 20px;
		}
		foreignObject &gt; svg:first-of-type &gt; text,
		foreignObject &gt; svg:first-of-type + svg &gt; text,
		foreignObject &gt; svg:first-of-type + svg + svg &gt; text,
		foreignObject &gt; svg:first-of-type + svg + svg + svg &gt; text {
			fill: var(--canvas-foreground);
			stroke: none;
		}
		foreignObject &gt; svg:first-of-type &gt; text,
		foreignObject &gt; svg:first-of-type + svg &gt; text {
			cursor: vertical-text;
		}
		foreignObject &gt; svg:first-of-type + svg + svg + svg + svg {
			position: relative;
			z-index: 3;
			width: calc(100% - 60px - var(--value-size) * 1px);
			height: calc(100% - 60px - var(--date-size) * 1px);
			margin: 20px 20px calc(40px + var(--date-size) * 1px) calc(40px + var(--value-size) * 1px);
			border-left: 1px solid var(--canvas-foreground);
			border-right: 1px dashed var(--canvas-foreground);
			border-top: 1px dashed var(--canvas-foreground);
			border-bottom: 1px solid var(--canvas-foreground);
			background: var(--canvas-background);
			pointer-events: auto;
		}
		g &gt; rect {
			vector-effect: non-scaling-stroke;
			fill: var(--highlighted);
			stroke: none;
		}
		g &gt; rect[tabindex] {
			fill: var(--tier, #0000);
			cursor: help;
		}
		g &gt; rect[tabindex][data-tier="diamond"] {
			--tier: var(--diamond);
		}
		g &gt; rect[tabindex][data-tier="gold"] {
			--tier: var(--gold);
		}
		g &gt; rect[tabindex][data-tier="silver"] {
			--tier: var(--silver);
		}
		g &gt; rect[tabindex][data-tier="bronze"] {
			--tier: var(--bronze);
		}
		g &gt; path[tabindex] {
			vector-effect: non-scaling-stroke;
			fill: none;
			stroke: hsl(calc(var(--index) / var(--count) * 360), 50%, 50%);
			cursor: help;
		}
		g:is(:hover, :focus-within) &gt; path[tabindex] {
			stroke: var(--canvas-foreground);
		}
		g &gt; path[tabindex]:is(:hover, :focus-within) {
			outline: 0;
		}
		g &gt; path[tabindex]:first-of-type {
			stroke-width: 2;
		}
		g &gt; path[tabindex]:first-of-type:is(:hover, :focus-within) {
			stroke-width: 4;
		}
		g &gt; path[tabindex]:first-of-type:not(:last-of-type) {
			stroke-dasharray: 4 8;
		}
		g &gt; path[tabindex]:first-of-type + rect {
			fill: none;
		}
		g &gt; path[tabindex]:first-of-type:is(:hover, :focus-within) + rect {
			fill: var(--highlighted);
			pointer-events: none;
		}
		g &gt; path[tabindex]:first-of-type ~ path[tabindex] {
			stroke-width: 8;
		}
		g &gt; path[tabindex]:first-of-type ~ path[tabindex]:is(:hover, :focus-within) {
			stroke-width: 16;
		}
		g &gt; path[tabindex]:first-of-type ~ path[tabindex] + line,
		g &gt; path[tabindex]:first-of-type ~ path[tabindex] + line + line {
			vector-effect: non-scaling-stroke;
			fill: none;
			stroke: none;
			stroke-width: 2;
			stroke-dasharray: 4 8;
			pointer-events: none;
		}
		g &gt; path[tabindex]:first-of-type:is(:hover, :focus-within) + rect + path[tabindex] + line,
		g &gt; path[tabindex]:first-of-type:is(:hover, :focus-within) + rect + path[tabindex] + line + line,
		g &gt; path[tabindex]:first-of-type:is(:hover, :focus-within) ~ path[tabindex]:last-of-type + line,
		g &gt; path[tabindex]:first-of-type:is(:hover, :focus-within) ~ path[tabindex]:last-of-type + line + line,
		g &gt; path[tabindex]:first-of-type ~ path[tabindex]:is(:hover, :focus-within) + line,
		g &gt; path[tabindex]:first-of-type ~ path[tabindex]:is(:hover, :focus-within) + line + line {
			stroke: var(--faded);
		}
		@media (prefers-color-scheme: dark) {
			:root {
				--canvas-background: #000;
				--canvas-foreground: #ccc;
				--diamond: #0363;
				--gold: #6303;
				--silver: #3633;
				--bronze: #6363;
				--highlighted: #6663;
				--faded: #9993;
			}
		}
		@media (prefers-color-scheme: light) {
			:root {
				--canvas-background: #fff;
				--canvas-foreground: #333;
				--diamond: #9cf3;
				--gold: #fc93;
				--silver: #9c93;
				--bronze: #c9c3;
				--highlighted: #9993;
				--faded: #6663;
			}
		}
	</style>
	<script>
		"use strict";
		{
			const script = document.currentScript;
			const documentUrl = location.href;
			const scope = new URL("../../", documentUrl).pathname;
			const playersKey = `${scope}#players`;
			const leaderboardsKey = `${scope}#leaderboards`;
			const sessionKey = `${scope}#session`;
			const lockKey = `${scope}#lock`;
			async function importDataScript(dataScriptUrl, signal) {
				signal.throwIfAborted();
				if (documentUrl.protocol !== "file:") {
					const response = await fetch(dataScriptUrl, {
						signal,
					});
					if (!response.ok) {
						throw new Error(response.statusText);
					}
					const data = await response.json();
					return data;
				}
				return await new Promise((resolve, reject) =&gt; {
					const name = `callback${Math.random() * 2 ** 53}`;
					let called = false;
					let data = null;
					function callback(value) {
						if (!called) {
							called = true;
							data = value;
						}
					}
					window[name] = callback;
					const dataScript = document.createElementNS("http://www.w3.org/2000/svg", "script");
					dataScript.href = `${dataScriptUrl}&amp;callback=${name}`;
					function handleAbort(event) {
						delete window[name];
						dataScript.removeEventListener("error", handleError);
						dataScript.removeEventListener("load", handleLoad);
						dataScript.remove();
						const reason = signal.reason;
						reject(reason);
					}
					function handleError(event) {
						delete window[name];
						signal.removeEventListener("abort", handleAbort);
						dataScript.removeEventListener("load", handleLoad);
						dataScript.remove();
						const error = event.error;
						reject(error);
					}
					function handleLoad(event) {
						delete window[name];
						signal.removeEventListener("abort", handleAbort);
						dataScript.removeEventListener("error", handleError);
						dataScript.remove();
						resolve(data);
					}
					signal.addEventListener("abort", handleAbort, {
						once: true,
					});
					dataScript.addEventListener("error", handleError, {
						once: true,
					});
					dataScript.addEventListener("load", handleLoad, {
						once: true,
					});
					script.after(dataScript);
				});
			}
			async function waitForTimeout(delay, signal) {
				signal.throwIfAborted();
				return await new Promise((resolve, reject) =&gt; {
					let timeout = null;
					function handleAbort() {
						clearTimeout(timeout);
						const reason = signal.reason;
						reject(reason);
					}
					function handleTimeout() {
						signal.removeEventListener("abort", handleAbort);
						resolve(delay);
					}
					signal.addEventListener("abort", handleAbort, {
						once: true,
					});
					timeout = setTimeout(handleTimeout, delay);
				});
			}
			window.addEventListener("pageshow", (event) =&gt; {
				if (event.persisted) {
					console.log(`Successfully restored page from cache`);
				} else {
					console.log(`Unsuccessfully restored page from cache`);
				}
			});
			window.addEventListener("pagehide", (event) =&gt; {
				if (event.persisted) {
					console.log(`Successfully saved page to cache`);
				} else {
					console.log(`Unsuccessfully saved page to cache`);
				}
			});
			window.addEventListener("pageshow", async (event) =&gt; {
				const rootTitleElement = document.querySelector("title");
				const rootTitleContent = rootTitleElement.textContent;
				const pathTitleElements = Array.from(document.querySelectorAll("g &gt; path[tabindex]:first-of-type &gt; title"));
				const pathTitleContents = pathTitleElements.map((pathTitleElement) =&gt; {
					const pathTitleContent = pathTitleElement.textContent;
					return pathTitleContent;
				});
				const foreignObject = document.querySelector("foreignObject");
				const div = document.createElementNS("http://www.w3.org/1999/xhtml", "div");
				div.dir = "ltr";
				const p = document.createElementNS("http://www.w3.org/1999/xhtml", "p");
				const label = document.createElementNS("http://www.w3.org/1999/xhtml", "label");
				const progress = document.createElementNS("http://www.w3.org/1999/xhtml", "progress");
				label.append(progress);
				const span = document.createElementNS("http://www.w3.org/1999/xhtml", "span");
				span.textContent = "Loading names";
				label.append(span);
				p.append(label);
				div.append(p);
				foreignObject.prepend(div);
				let lock = false;
				let session = null;
				const broadcastChannel = new BroadcastChannel(sessionKey);
				const abortController = new AbortController();
				const signal = abortController.signal;
				const abort = abortController.abort.bind(abortController);
				const {promise, resolve} = Promise.withResolvers();
				broadcastChannel.addEventListener("message", (event) =&gt; {
					const data = event.data;
					const type = data.type;
					switch (type) {
						case "request": {
							if (lock &amp;&amp; signal.aborted) {
								broadcastChannel.postMessage({
									type: "response",
									session,
								});
							}
							break;
						}
						case "response": {
							if (!signal.aborted) {
								session = data.session;
								if (session != null) {
									const {players, leaderboards} = session;
									try {
										const cachedPlayers = JSON.stringify(players);
										const cachedLeaderboards = JSON.stringify(leaderboards);
										sessionStorage.setItem(playersKey, cachedPlayers);
										sessionStorage.setItem(leaderboardsKey, cachedLeaderboards);
									} catch {}
									console.log(`Successfully loaded names from peer`);
									abort();
									resolve();
								} else {
									console.log(`Unsuccessfully loaded names from peer`);
									abort();
									resolve();
								}
							}
							break;
						}
					}
				});
				try {
					const cachedPlayers = sessionStorage.getItem(playersKey);
					const cachedLeaderboards = sessionStorage.getItem(leaderboardsKey);
					if (cachedPlayers == null || cachedLeaderboards == null) {
						throw new Error();
					}
					const players = JSON.parse(cachedPlayers);
					const leaderboards = JSON.parse(cachedLeaderboards);
					session = {players, leaderboards};
				} catch {}
				if (session != null) {
					broadcastChannel.postMessage({
						type: "response",
						session,
					});
					console.log(`Successfully loaded names from local`);
					abort();
					resolve();
				} else {
					broadcastChannel.postMessage({
						type: "request",
					});
					console.log(`Unsuccessfully loaded names from local`);
				}
				(async () =&gt; {
					for (;;) {
						await navigator.locks.request(lockKey, async () =&gt; {
							lock = true;
							console.log(`Acquired lock`);
							if (!signal.aborted) {
								try {
									const players = Object.create(null);
									const leaderboards = Object.create(null);
									const games = ["9d3rrxyd", "w6jl2ned"];
									for (const gameId of games) {
										const {data} = await importDataScript(`https://www.speedrun.com/api/v1/games/${gameId}?embed=categories,levels,variables`, signal);
										const versions = data.variables.data.find((variable) =&gt; {
											return variable.name === "Version";
										});
										const levels = Object.fromEntries(data.levels.data.map((level) =&gt; {
											return [level.id, level];
										}));
										const categories = Object.fromEntries(data.categories.data.map((category) =&gt; {
											return [category.id, category];
										}));
										const variables = Object.fromEntries(data.variables.data.map((variable) =&gt; {
											return [variable.id, variable];
										}));
										console.log(`Got game`);
										await waitForTimeout(800, signal);
										const slice = 200;
										for (let offset = 0;; offset += slice) {
											const {data, pagination} = await importDataScript(`https://www.speedrun.com/api/v1/runs?game=${gameId}&amp;orderby=date&amp;direction=asc&amp;embed=players&amp;offset=${offset}&amp;max=${slice}`, signal);
											const {size} = pagination;
											if (size === 0) {
												break;
											}
											for (const run of data) {
												const status = run.status.status;
												if (status == null || status === "new") {
													continue;
												}
												const player = run.players.data[0].rel === "user" ? run.players.data[0].id : "814p2558";
												const playerName = run.players.data[0].rel === "user" ? run.players.data[0].names.international : "anonymous";
												players[player] ??= playerName;
												const level = run.level;
												const category = run.category;
												const values = Object.entries(run.values).filter(([variable]) =&gt; {
													return variables[variable]?.["is-subcategory"] ?? false;
												}).map(([variable, value]) =&gt; {
													return `-${variable}.${value}`;
												});
												const leaderboard = `${level != null ? `l_${level}-` : ""}${category}${values.join("")}`;
												const levelName = run.level != null ? levels[run.level]?.name ?? null : null;
												const categoryName = categories[run.category]?.name ?? null;
												const valueNames = Object.entries(run.values).filter(([variable]) =&gt; {
													return variables[variable]?.["is-subcategory"] ?? false;
												}).map(([variable, value]) =&gt; {
													return variables[variable]?.values.values[value]?.label ?? null;
												}).filter((valueName) =&gt; {
													return valueName != null;
												});
												const leaderboardName = `${levelName != null ? `${levelName}: ` : ""}${categoryName ?? ""}${values.length !== 0 ? ` - ${valueNames.join(", ")}` : ""}`;
												leaderboards[leaderboard] ??= leaderboardName;
											}
											console.log(`Got runs ${offset}-${offset + size - 1}`);
											await waitForTimeout(800, signal);
										}
									}
									session = {players, leaderboards};
								} catch {}
								if (!signal.aborted) {
									if (session != null) {
										const {players, leaderboards} = session;
										try {
											const cachedPlayers = JSON.stringify(players);
											const cachedLeaderboards = JSON.stringify(leaderboards);
											sessionStorage.setItem(playersKey, cachedPlayers);
											sessionStorage.setItem(leaderboardsKey, cachedLeaderboards);
										} catch {}
										broadcastChannel.postMessage({
											type: "response",
											session,
										});
										console.log(`Successfully loaded names from remote`);
										abort();
										resolve();
									} else {
										broadcastChannel.postMessage({
											type: "response",
											session,
										});
										console.log(`Unsuccessfully loaded names from remote`);
										abort();
										resolve();
									}
								}
							}
							await new Promise((resolve) =&gt; {
								window.addEventListener("beforeunload", () =&gt; {
									resolve();
								}, {
									once: true,
								});
							});
						});
						lock = false;
						console.log(`Released lock`);
						await new Promise((resolve) =&gt; {
							window.addEventListener("pagehide", () =&gt; {
								resolve();
							}, {
								once: true,
							});
						});
						await new Promise((resolve) =&gt;{
							window.addEventListener("pageshow", () =&gt; {
								resolve();
							}, {
								once: true,
							});
						});
					}
				})();
				await promise;
				if (session == null) {
					const input = document.createElementNS("http://www.w3.org/1999/xhtml", "input");
					input.type = "checkbox";
					input.autocomplete = "off";
					input.disabled = true;
					input.indeterminate = true;
					progress.replaceWith(input);
					span.textContent = "Unavailable names";
					return;
				}
				const {players, leaderboards} = session;
				const hasPlayerRoot = rootTitleContent.includes(" for player");
				const hasLeaderboardRoot = rootTitleContent.includes(" for leaderboard");
				const rootTitle = {
					element: rootTitleElement,
					contentWithIds: rootTitleContent,
					contentWithNames: hasPlayerRoot ? rootTitleContent.replace(/ for player ([0-9_a-z]+)/, ($0, $1) =&gt; {
						return ` for player ${players[$1] ?? $1}`;
					}) : hasLeaderboardRoot ? rootTitleContent.replace(/ for leaderboard ((?:l_[0-9_a-z]+-)?[0-9_a-z]+(-[0-9_a-z]+\.[0-9_a-z]+)*)/, ($0, $1) =&gt; {
						return ` for leaderboard ${leaderboards[$1] ?? $1}`;
					}) : rootTitleContent,
				};
				const hasPlayerPaths = rootTitleContent.includes(" by player");
				const hasLeaderboardPaths = rootTitleContent.includes(" by leaderboard");
				const pathTitles = pathTitleElements.map((pathTitleElement, k) =&gt; {
					const pathTitleContent = pathTitleContents[k];
					const pathTitle = {
						element: pathTitleElement,
						contentWithIds: pathTitleContent,
						contentWithNames: hasPlayerPaths ? players[pathTitleContent] ?? pathTitleContent : hasLeaderboardPaths ? leaderboards[pathTitleContent] ?? pathTitleContent : pathTitleContent,
					};
					return pathTitle;
				});
				const input = document.createElementNS("http://www.w3.org/1999/xhtml", "input");
				input.type = "checkbox";
				input.autocomplete = "off";
				input.addEventListener("change", () =&gt; {
					input.disabled = true;
					if (input.checked) {
						rootTitle.element.textContent = rootTitle.contentWithNames;
						for (const pathTitle of pathTitles) {
							pathTitle.element.textContent = pathTitle.contentWithNames;
						}
					} else {
						rootTitle.element.textContent = rootTitle.contentWithIds;
						for (const pathTitle of pathTitles) {
							pathTitle.element.textContent = pathTitle.contentWithIds;
						}
					}
					input.disabled = false;
				});
				progress.replaceWith(input);
				span.textContent = "Reveal names";
			}, {
				once: true,
			});
		}
	</script>
	<script>
		"use strict";
		{
			document.addEventListener("DOMContentLoaded", (event) =&gt; {
				const minDateText = document.querySelector("foreignObject &gt; svg:first-of-type &gt; text");
				const maxDateText = document.querySelector("foreignObject &gt; svg:first-of-type + svg &gt; text");
				const startX = Math.round(Date.parse(minDateText.textContent) / 86400000);
				function xToDate(x) {
					const datetime = new Date((startX + x) * 86400000);
					const year = `${datetime.getUTCFullYear()}`.padStart(4, "0");
					const month = `${datetime.getUTCMonth() + 1}`.padStart(2, "0");
					const day = `${datetime.getUTCDate()}`.padStart(2, "0");
					const date = `${year}-${month}-${day}`;
					const size = date.length * 10;
					return {date, size};
				}
				function setMinDate(x) {
					const {date, size} = xToDate(Math.floor(x));
					minDateText.textLength.baseVal.value = size;
					minDateText.textContent = date;
				}
				function setMaxDate(x) {
					const {date, size} = xToDate(Math.ceil(x));
					maxDateText.textLength.baseVal.value = size;
					maxDateText.textContent = date;
				}
				const minValueText = document.querySelector("foreignObject &gt; svg:first-of-type + svg + svg &gt; text");
				const maxValueText = document.querySelector("foreignObject &gt; svg:first-of-type + svg + svg + svg &gt; text");
				const timed = minValueText.textContent.includes(":");
				function yToValue(y) {
					if (timed) {
						const minutes = `${(y - y % 6000) / 6000}`.padStart(2, "0");
						const seconds = `${(y - y % 100) / 100 % 60}`.padStart(2, "0");
						const centiseconds = `${y % 100}`.padStart(2, "0");
						const value = `${minutes}:${seconds}.${centiseconds}`;
						const size = value.length * 10;
						return {value, size};
					} else {
						const value = `${y}`;
						const size = value.length * 10;
						return {value, size};
					}
				}
				function setMinValue(y) {
					const {value, size} = yToValue(Math.floor(y));
					minValueText.textLength.baseVal.value = size;
					minValueText.textContent = value;
				}
				function setMaxValue(y) {
					const {value, size} = yToValue(Math.ceil(y));
					maxValueText.textLength.baseVal.value = size;
					maxValueText.textContent = value;
				}
				const svg = document.querySelector("foreignObject &gt; svg:first-of-type + svg + svg + svg + svg");
				svg.tabIndex = -1;
				svg.style.setProperty("outline", "0");
				if (CSS.supports("overflow-clip-margin", "8px")) {
					svg.style.setProperty("overflow", "clip");
					svg.style.setProperty("overflow-clip-margin", "8px");
				}
				svg.style.setProperty("touch-action", "none");
				const {x: baseX, y: baseY, width: baseWidth, height: baseHeight} = svg.viewBox.baseVal;
				let pointerCount = 0;
				const pointers = Object.create(null);
				const xPointerIds = [];
				const yPointerIds = [];
				let x = null;
				let y = null;
				let width = null;
				let height = null;
				svg.addEventListener("pointerdown", (event) =&gt; {
					const {pointerId} = event;
					if (pointerId in pointers) {
						return;
					}
					let {x: animX, y: animY, width: animWidth, height: animHeight} = svg.viewBox.baseVal;
					svg.setPointerCapture(pointerId);
					++pointerCount;
					const offsetX = event.offsetX;
					const offsetY = event.offsetY;
					pointers[pointerId] = {
						offsetX,
						offsetY,
					};
					xPointerIds.splice(xPointerIds.findLastIndex((pointerId) =&gt; {
						return pointers[pointerId].offsetX &lt;= offsetX;
					}) + 1, 0, pointerId);
					yPointerIds.splice(yPointerIds.findLastIndex((pointerId) =&gt; {
						return pointers[pointerId].offsetY &lt;= offsetY;
					}) + 1, 0, pointerId);
					const minOffsetX = pointers[xPointerIds[0]].offsetX;
					const minOffsetY = pointers[yPointerIds[0]].offsetY;
					const maxClientWidth = pointers[xPointerIds[pointerCount - 1]].offsetX - minOffsetX;
					const maxClientHeight = pointers[yPointerIds[pointerCount - 1]].offsetY - minOffsetY;
					width = Math.min(maxClientWidth / svg.clientWidth * animWidth, baseWidth);
					height = Math.min(maxClientHeight / svg.clientHeight * animHeight, baseHeight);
					x = Math.max(Math.min(animX + minOffsetX / svg.clientWidth * animWidth, baseX + baseWidth), baseX);
					y = Math.max(Math.min(animY + minOffsetY / svg.clientHeight * animHeight, baseY + baseHeight), baseY);
				});
				svg.addEventListener("pointerup", (event) =&gt; {
					const {pointerId} = event;
					if (!(pointerId in pointers)) {
						return;
					}
					let {x: animX, y: animY, width: animWidth, height: animHeight} = svg.viewBox.baseVal;
					svg.releasePointerCapture(pointerId);
					--pointerCount;
					delete pointers[pointerId];
					xPointerIds.splice(xPointerIds.lastIndexOf(pointerId), 1);
					yPointerIds.splice(yPointerIds.lastIndexOf(pointerId), 1);
					const minOffsetX = pointerCount !== 0 ? pointers[xPointerIds[0]].offsetX : null;
					const minOffsetY = pointerCount !== 0 ? pointers[yPointerIds[0]].offsetY : null;
					const maxClientWidth = pointerCount !== 0 ? pointers[xPointerIds[pointerCount - 1]].offsetX - minOffsetX : null;
					const maxClientHeight = pointerCount !== 0 ? pointers[yPointerIds[pointerCount - 1]].offsetY - minOffsetY : null;
					width = pointerCount !== 0 ? Math.min(maxClientWidth / svg.clientWidth * animWidth, baseWidth) : null;
					height = pointerCount !== 0 ? Math.min(maxClientHeight / svg.clientHeight * animHeight, baseHeight) : null;
					x = pointerCount !== 0 ? Math.max(Math.min(animX + minOffsetX / svg.clientWidth * animWidth, baseX + baseWidth), baseX) : null;
					y = pointerCount !== 0 ? Math.max(Math.min(animY + minOffsetY / svg.clientHeight * animHeight, baseY + baseHeight), baseY) : null;
				});
				svg.addEventListener("pointermove", (event) =&gt; {
					const {pointerId} = event;
					if (!(pointerId in pointers)) {
						return;
					}
					let {x: animX, y: animY, width: animWidth, height: animHeight} = svg.viewBox.baseVal;
					event.preventDefault();
					const offsetX = event.offsetX;
					const offsetY = event.offsetY;
					pointers[pointerId].offsetX = offsetX;
					pointers[pointerId].offsetY = offsetY;
					const oldXIndex = xPointerIds.lastIndexOf(pointerId);
					let newXIndex = oldXIndex;
					while (newXIndex &gt; 0 &amp;&amp; pointers[xPointerIds[newXIndex - 1]].offsetX &gt; offsetX) {
						--newXIndex;
					}
					xPointerIds.copyWithin(newXIndex + 1, newXIndex, oldXIndex);
					xPointerIds[newXIndex] = pointerId;
					while (newXIndex &lt; pointerCount - 1 &amp;&amp; pointers[xPointerIds[newXIndex + 1]].offsetX &lt; offsetX) {
						++newXIndex;
					}
					xPointerIds.copyWithin(oldXIndex, oldXIndex + 1, newXIndex + 1);
					xPointerIds[newXIndex] = pointerId;
					const oldYIndex = yPointerIds.lastIndexOf(pointerId);
					let newYIndex = oldYIndex;
					while (newYIndex &gt; 0 &amp;&amp; pointers[yPointerIds[newYIndex - 1]].offsetY &gt; offsetY) {
						--newYIndex;
					}
					yPointerIds.copyWithin(newYIndex + 1, newYIndex, oldYIndex);
					yPointerIds[newYIndex] = pointerId;
					while (newYIndex &lt; pointerCount - 1 &amp;&amp; pointers[yPointerIds[newYIndex + 1]].offsetY &lt; offsetY) {
						++newYIndex;
					}
					yPointerIds.copyWithin(oldYIndex, oldYIndex + 1, newYIndex + 1);
					yPointerIds[newYIndex] = pointerId;
					const minOffsetX = pointers[xPointerIds[0]].offsetX;
					const minOffsetY = pointers[yPointerIds[0]].offsetY;
					const maxClientWidth = pointers[xPointerIds[pointerCount - 1]].offsetX - minOffsetX;
					const maxClientHeight = pointers[yPointerIds[pointerCount - 1]].offsetY - minOffsetY;
					animWidth = width === 0 ? animWidth : maxClientWidth === 0 ? baseWidth : Math.min(svg.clientWidth / maxClientWidth * width, baseWidth);
					animHeight = height === 0 ? animHeight : maxClientHeight === 0 ? baseHeight : Math.min(svg.clientHeight / maxClientHeight * height, baseHeight);
					animX = Math.max(Math.min(x - minOffsetX / svg.clientWidth * animWidth, baseX + baseWidth - animWidth), baseX);
					animY = Math.max(Math.min(y - minOffsetY / svg.clientHeight * animHeight, baseY + baseHeight - animHeight), baseY);
					svg.viewBox.baseVal.x = animX;
					svg.viewBox.baseVal.y = animY;
					svg.viewBox.baseVal.width = animWidth;
					svg.viewBox.baseVal.height = animHeight;
					setMinDate(animX);
					setMaxDate(animX + animWidth);
					setMinValue(baseY * 2 + baseHeight - animHeight - animY);
					setMaxValue(baseY * 2 + baseHeight - animY);
				});
				const zoomIntensity = 1.25;
				svg.addEventListener("wheel", (event) =&gt; {
					let {x: animX, y: animY, width: animWidth, height: animHeight} = svg.viewBox.baseVal;
					const {ctrlKey, shiftKey} = event;
					event.preventDefault();
					if (pointerCount !== 0) {
						return;
					}
					if (!ctrlKey) {
						const vectorX = (shiftKey ? event.deltaY : event.deltaX) / svg.clientWidth * animWidth;
						const vectorY = (shiftKey ? 0 : event.deltaY) / svg.clientHeight * animHeight;
						animX = Math.max(Math.min(animX + vectorX, baseX + baseWidth - animWidth), baseX);
						animY = Math.max(Math.min(animY + vectorY, baseY + baseHeight - animHeight), baseY);
						svg.viewBox.baseVal.x = animX;
						svg.viewBox.baseVal.y = animY;
						setMinDate(animX);
						setMaxDate(animX + animWidth);
						setMinValue(baseY * 2 + baseHeight - animHeight - animY);
						setMaxValue(baseY * 2 + baseHeight - animY);
						return;
					}
					const ratioX = zoomIntensity ** Math.sign(shiftKey ? event.deltaY : event.deltaX);
					const ratioY = zoomIntensity ** Math.sign(shiftKey ? 0 : event.deltaY);
					const positionX = Math.max(0, Math.min(event.offsetX / svg.clientWidth, 1)) * animWidth;
					const positionY = Math.max(0, Math.min(event.offsetY / svg.clientHeight, 1)) * animHeight;
					animWidth = Math.min(animWidth * ratioX, baseWidth);
					animHeight = Math.min(animHeight * ratioY, baseHeight);
					animX = Math.max(Math.min(animX + positionX * (1 - ratioX), baseX + baseWidth - animWidth), baseX);
					animY = Math.max(Math.min(animY + positionY * (1 - ratioY), baseY + baseHeight - animHeight), baseY);
					svg.viewBox.baseVal.x = animX;
					svg.viewBox.baseVal.y = animY;
					svg.viewBox.baseVal.width = animWidth;
					svg.viewBox.baseVal.height = animHeight;
					setMinDate(animX);
					setMaxDate(animX + animWidth);
					setMinValue(baseY * 2 + baseHeight - animHeight - animY);
					setMaxValue(baseY * 2 + baseHeight - animY);
				}, {
					passive: false,
				});
				svg.addEventListener("keydown", (event) =&gt; {
					let {x: animX, y: animY, width: animWidth, height: animHeight} = svg.viewBox.baseVal;
					const {ctrlKey, shiftKey} = event;
					switch (event.code) {
						case "Space": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							const vectorY = shiftKey ? -animHeight : animHeight;
							animY = Math.max(Math.min(animY + vectorY, baseY + baseHeight - animHeight), baseY);
							svg.viewBox.baseVal.y = animY;
							setMinValue(baseY * 2 + baseHeight - animHeight - animY);
							setMaxValue(baseY * 2 + baseHeight - animY);
							return;
						}
						case "Equal":
						case "NumpadAdd":
						case "Minus":
						case "NumpadSubtract": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							const ratio = event.code === "Minus" || event.code === "NumpadSubtract" ? zoomIntensity : 1 / zoomIntensity;
							if (!shiftKey) {
								animHeight = Math.min(animHeight * ratio, baseHeight);
								animY = Math.max(Math.min(animY, baseY + baseHeight - animHeight), baseY);
								svg.viewBox.baseVal.y = animY;
								svg.viewBox.baseVal.height = animHeight;
								setMinValue(baseY * 2 + baseHeight - animHeight - animY);
								setMaxValue(baseY * 2 + baseHeight - animY);
								return;
							}
							animWidth = Math.min(animWidth * ratio, baseWidth);
							animX = Math.max(Math.min(animX, baseX + baseWidth - animWidth), baseX);
							svg.viewBox.baseVal.x = animX;
							svg.viewBox.baseVal.width = animWidth;
							setMinDate(animX);
							setMaxDate(animX + animWidth);
							return;
						}
						case "Digit0":
						case "Numpad0": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							if (!shiftKey) {
								animY = baseY;
								animHeight = baseHeight;
								svg.viewBox.baseVal.y = animY;
								svg.viewBox.baseVal.height = animHeight;
								setMinValue(baseY * 2 + baseHeight - animHeight - animY);
								setMaxValue(baseY * 2 + baseHeight - animY);
								return;
							}
							animX = baseX;
							animWidth = baseWidth;
							svg.viewBox.baseVal.x = animX;
							svg.viewBox.baseVal.width = animWidth;
							setMinDate(animX);
							setMaxDate(animX + animWidth);
							return;
						}
					}
					switch (event.key) {
						case "ArrowRight": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							const vectorX = 50 / svg.clientWidth * animWidth;
							animX = Math.max(Math.min(animX + vectorX, baseX + baseWidth - animWidth), baseX);
							svg.viewBox.baseVal.x = animX;
							setMinDate(animX);
							setMaxDate(animX + animWidth);
							return;
						}
						case "ArrowLeft": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							const vectorX = -50 / svg.clientWidth * animWidth;
							animX = Math.max(Math.min(animX + vectorX, baseX + baseWidth - animWidth), baseX);
							svg.viewBox.baseVal.x = animX;
							setMinDate(animX);
							setMaxDate(animX + animWidth);
							return;
						}
						case "ArrowDown": {
							if (!ctrlKey) {
								event.preventDefault();
								if (pointerCount !== 0) {
									return;
								}
								const vectorY = 50 / svg.clientHeight * animHeight;
								animY = Math.max(Math.min(animY + vectorY, baseY + baseHeight - animHeight), baseY);
								svg.viewBox.baseVal.y = animY;
								setMinValue(baseY * 2 + baseHeight - animHeight - animY);
								setMaxValue(baseY * 2 + baseHeight - animY);
								return;
							}
						}
						case "End": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							animY = baseY + baseHeight - animHeight;
							svg.viewBox.baseVal.y = animY;
							setMinValue(baseY * 2 + baseHeight - animHeight - animY);
							setMaxValue(baseY * 2 + baseHeight - animY);
							return;
						}
						case "ArrowUp": {
							if (!ctrlKey) {
								event.preventDefault();
								if (pointerCount !== 0) {
									return;
								}
								const vectorY = -50 / svg.clientHeight * animHeight;
								animY = Math.max(Math.min(animY + vectorY, baseY + baseHeight - animHeight), baseY);
								svg.viewBox.baseVal.y = animY;
								setMinValue(baseY * 2 + baseHeight - animHeight - animY);
								setMaxValue(baseY * 2 + baseHeight - animY);
								return;
							}
						}
						case "Home": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							animY = baseY;
							svg.viewBox.baseVal.y = animY;
							setMinValue(baseY * 2 + baseHeight - animHeight - animY);
							setMaxValue(baseY * 2 + baseHeight - animY);
							return;
						}
						case "PageDown":
						case "PageUp":
						case " ": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							const vectorY = event.key === "PageUp" || event.key === " " &amp;&amp; shiftKey ? -animHeight : animHeight;
							animY = Math.max(Math.min(animY + vectorY, baseY + baseHeight - animHeight), baseY);
							svg.viewBox.baseVal.y = animY;
							setMinValue(baseY * 2 + baseHeight - animHeight - animY);
							setMaxValue(baseY * 2 + baseHeight - animY);
							return;
						}
						case "+":
						case "-": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							const ratio = event.key === "-" ? 1 / zoomIntensity : zoomIntensity;
							animWidth = Math.min(animWidth * ratio, baseWidth);
							animHeight = Math.min(animHeight * ratio, baseHeight);
							animX = Math.max(Math.min(animX, baseX + baseWidth - animWidth), baseX);
							animY = Math.max(Math.min(animY, baseY + baseHeight - animHeight), baseY);
							svg.viewBox.baseVal.x = animX;
							svg.viewBox.baseVal.y = animY;
							svg.viewBox.baseVal.width = animWidth;
							svg.viewBox.baseVal.height = animHeight;
							setMinDate(animX);
							setMaxDate(animX + animWidth);
							setMinValue(baseY * 2 + baseHeight - animHeight - animY);
							setMaxValue(baseY * 2 + baseHeight - animY);
							return;
						}
						case "0": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							animX = baseX;
							animY = baseY;
							animWidth = baseWidth;
							animHeight = baseHeight;
							svg.viewBox.baseVal.x = animX;
							svg.viewBox.baseVal.y = animY;
							svg.viewBox.baseVal.width = animWidth;
							svg.viewBox.baseVal.height = animHeight;
							setMinDate(animX);
							setMaxDate(animX + animWidth);
							setMinValue(baseY * 2 + baseHeight - animHeight - animY);
							setMaxValue(baseY * 2 + baseHeight - animY);
							return;
						}
					}
				});
				svg.addEventListener("focusin", (event) =&gt; {
					if (!event.target.matches(":focus-visible")) {
						return;
					}
					event.preventDefault();
					if (pointerCount !== 0) {
						return;
					}
					let {x: animX, y: animY, width: animWidth, height: animHeight} = svg.viewBox.baseVal;
					let {x, y, width, height} = event.target.getBBox();
					width = Math.round(width);
					height = Math.round(height);
					x = Math.round(x);
					y = baseY * 2 + baseHeight - height - Math.round(y);
					if (x + width &lt; animX) {
						animX = x;
						if (width &gt; animWidth) {
							animX += (width - animWidth) / 2;
						}
					} else if (x &gt; animX + animWidth) {
						animX = x + width - animWidth;
						if (width &gt; animWidth) {
							animX -= (width - animWidth) / 2;
						}
					}
					if (y + height &lt; animY) {
						animY = y;
						if (height &gt; animHeight) {
							animY += (height - animHeight) / 2;
						}
					} else if (y &gt; animY + animHeight) {
						animY = y + height - animHeight;
						if (height &gt; animHeight) {
							animY -= (height - animHeight) / 2;
						}
					}
					svg.viewBox.baseVal.x = animX;
					svg.viewBox.baseVal.y = animY;
					setMinDate(animX);
					setMaxDate(animX + animWidth);
					setMinValue(baseY * 2 + baseHeight - animHeight - animY);
					setMaxValue(baseY * 2 + baseHeight - animY);
				});
			}, {
				once: true,
			});
		}
	</script>
	<foreignObject x="0" y="0" width="100%" height="100%" style="--date-size: 100; --value-size: 20;">
		<svg viewBox="0 0 100 20">
			<text x="50" y="10" dx="0" dy="2" textLength="100" lengthAdjust="spacingAndGlyphs" text-anchor="middle" dominant-baseline="middle">2022-07-20</text>
		</svg>
		<svg viewBox="0 0 100 20">
			<text x="50" y="10" dx="0" dy="2" textLength="100" lengthAdjust="spacingAndGlyphs" text-anchor="middle" dominant-baseline="middle">2025-10-31</text>
		</svg>
		<svg viewBox="0 0 20 20">
			<text x="10" y="10" dx="0" dy="2" textLength="10" lengthAdjust="spacingAndGlyphs" text-anchor="middle" dominant-baseline="middle">0</text>
		</svg>
		<svg viewBox="0 0 20 20">
			<text x="10" y="10" dx="0" dy="2" textLength="20" lengthAdjust="spacingAndGlyphs" text-anchor="middle" dominant-baseline="middle">87</text>
		</svg>
		<svg viewBox="0 0 1199 87" preserveAspectRatio="none" overflow="visible">
			<g transform="translate(0 87) scale(1 -1)" style="--count: 9;">
				<g>
					<rect x="0" y="0" width="1199" height="87"/>
				</g>
				<g style="--index: 0;">
					<path d="M0,0L0,0L0,7L10,7L10,7L33,7L33,7L39,7L39,8L41,8L41,9L1104,9L1104,9L1146,9L1146,9L1151,9L1151,9L1152,9L1152,9L1199,9" tabindex="0">
						<title>xd17x94d-9l71pv4l.zqo20y2l</title>
					</path>
				</g>
				<g style="--index: 1;">
					<path d="M0,0L8,0L8,13L13,13L13,13L30,13L30,13L51,13L51,14L53,14L53,14L57,14L57,14L119,14L119,14L125,14L125,14L189,14L189,15L234,15L234,15L239,15L239,15L242,15L242,15L244,15L244,16L255,16L255,18L284,18L284,18L293,18L293,19L295,19L295,20L298,20L298,20L312,20L312,21L318,21L318,22L326,22L326,23L328,23L328,22L337,22L337,23L356,23L356,24L364,24L364,24L368,24L368,26L381,26L381,26L384,26L384,26L390,26L390,27L396,27L396,28L401,28L401,28L417,28L417,28L431,28L431,28L444,28L444,29L445,29L445,30L448,30L448,30L454,30L454,30L507,30L507,31L511,31L511,31L562,31L562,31L563,31L563,32L565,32L565,32L566,32L566,33L567,33L567,32L581,32L581,32L586,32L586,33L608,33L608,34L614,34L614,35L620,35L620,36L631,36L631,36L653,36L653,37L668,37L668,37L673,37L673,37L683,37L683,39L685,39L685,38L700,38L700,39L713,39L713,40L719,40L719,41L720,41L720,41L723,41L723,41L725,41L725,41L747,41L747,41L749,41L749,41L751,41L751,42L763,42L763,43L764,43L764,43L773,43L773,43L780,43L780,43L781,43L781,43L806,43L806,43L816,43L816,43L843,43L843,44L858,44L858,44L866,44L866,44L867,44L867,45L879,45L879,46L917,46L917,47L925,47L925,48L929,48L929,49L935,49L935,50L953,50L953,50L973,50L973,50L975,50L975,51L984,51L984,51L994,51L994,52L1002,52L1002,53L1004,53L1004,53L1010,53L1010,53L1016,53L1016,54L1017,54L1017,54L1018,54L1018,54L1045,54L1045,55L1061,55L1061,56L1064,56L1064,56L1070,56L1070,56L1079,56L1079,56L1080,56L1080,56L1082,56L1082,57L1083,57L1083,56L1084,56L1084,56L1091,56L1091,57L1094,57L1094,58L1095,58L1095,59L1096,59L1096,59L1101,59L1101,59L1106,59L1106,59L1115,59L1115,60L1123,60L1123,60L1125,60L1125,60L1137,60L1137,60L1138,60L1138,61L1141,61L1141,61L1144,61L1144,61L1154,61L1154,62L1155,62L1155,63L1158,63L1158,63L1161,63L1161,63L1165,63L1165,63L1166,63L1166,64L1167,64L1167,64L1170,64L1170,64L1173,64L1173,65L1179,65L1179,65L1189,65L1189,66L1190,66L1190,66L1194,66L1194,66L1199,66" tabindex="0">
						<title>l_r9gn1qpd-9d8eewld-0nwoqwkl.81ppdgg1</title>
					</path>
				</g>
				<g style="--index: 2;">
					<path d="M0,0L8,0L8,5L21,5L21,5L24,5L24,5L34,5L34,5L36,5L36,6L51,6L51,6L60,6L60,6L108,6L108,7L109,7L109,7L115,7L115,7L120,7L120,7L182,7L182,7L189,7L189,7L203,7L203,7L215,7L215,7L242,7L242,8L256,8L256,8L279,8L279,8L324,8L324,8L331,8L331,8L340,8L340,9L356,9L356,10L389,10L389,11L404,11L404,11L417,11L417,10L466,10L466,10L507,10L507,10L509,10L509,10L542,10L542,11L564,11L564,12L568,12L568,11L572,11L572,11L651,11L651,11L655,11L655,12L718,12L718,12L749,12L749,12L788,12L788,12L802,12L802,12L863,12L863,12L864,12L864,12L899,12L899,12L910,12L910,12L919,12L919,12L924,12L924,12L928,12L928,12L954,12L954,13L955,13L955,13L956,13L956,13L959,13L959,13L960,13L960,13L988,13L988,13L1005,13L1005,13L1016,13L1016,14L1018,14L1018,14L1047,14L1047,14L1055,14L1055,14L1068,14L1068,14L1072,14L1072,14L1080,14L1080,14L1082,14L1082,15L1107,15L1107,15L1110,15L1110,15L1113,15L1113,15L1118,15L1118,15L1124,15L1124,15L1130,15L1130,15L1132,15L1132,15L1142,15L1142,15L1155,15L1155,15L1158,15L1158,15L1163,15L1163,15L1167,15L1167,15L1180,15L1180,15L1194,15L1194,15L1195,15L1195,15L1199,15" tabindex="0">
						<title>l_r9gn1qpd-9d8eewld-0nwoqwkl.21dz3e4l</title>
					</path>
				</g>
				<g style="--index: 3;">
					<path d="M0,0L8,0L8,24L9,24L9,25L20,25L20,25L24,25L24,26L27,26L27,27L30,27L30,27L31,27L31,27L33,27L33,27L35,27L35,27L36,27L36,28L94,28L94,28L98,28L98,28L108,28L108,29L110,29L110,28L114,28L114,28L129,28L129,29L136,29L136,29L144,29L144,29L170,29L170,29L174,29L174,29L176,29L176,30L189,30L189,31L191,31L191,31L235,31L235,31L240,31L240,31L241,31L241,32L242,32L242,33L248,33L248,33L256,33L256,33L258,33L258,34L260,34L260,34L264,34L264,35L268,35L268,36L271,36L271,37L276,37L276,38L278,38L278,38L281,38L281,38L297,38L297,39L298,39L298,39L304,39L304,39L308,39L308,40L309,40L309,40L333,40L333,41L347,41L347,42L359,42L359,42L361,42L361,42L363,42L363,42L387,42L387,42L390,42L390,43L397,43L397,43L405,43L405,43L434,43L434,43L445,43L445,44L452,44L452,44L454,44L454,44L470,44L470,45L471,45L471,45L485,45L485,46L497,46L497,47L500,47L500,48L564,48L564,48L565,48L565,49L589,49L589,49L608,49L608,50L622,50L622,51L631,51L631,51L634,51L634,51L648,51L648,51L651,51L651,51L654,51L654,52L668,52L668,52L669,52L669,52L677,52L677,53L686,53L686,54L699,54L699,54L704,54L704,55L706,55L706,55L707,55L707,55L708,55L708,55L709,55L709,55L712,55L712,55L716,55L716,55L719,55L719,55L720,55L720,55L723,55L723,55L726,55L726,56L731,56L731,56L735,56L735,56L737,56L737,56L741,56L741,56L747,56L747,56L749,56L749,56L752,56L752,56L757,56L757,56L760,56L760,56L771,56L771,56L773,56L773,57L780,57L780,57L781,57L781,57L792,57L792,57L812,57L812,58L815,58L815,58L823,58L823,58L829,58L829,58L835,58L835,59L845,59L845,59L846,59L846,59L850,59L850,59L851,59L851,59L854,59L854,59L858,59L858,59L861,59L861,60L871,60L871,60L872,60L872,60L880,60L880,60L882,60L882,60L886,60L886,60L889,60L889,61L890,61L890,62L896,62L896,62L900,62L900,62L902,62L902,62L905,62L905,62L911,62L911,62L915,62L915,63L919,63L919,63L926,63L926,63L929,63L929,63L940,63L940,64L942,64L942,64L944,64L944,64L946,64L946,64L947,64L947,64L957,64L957,65L962,65L962,65L966,65L966,65L968,65L968,65L972,65L972,65L975,65L975,65L979,65L979,65L984,65L984,66L989,66L989,66L992,66L992,66L994,66L994,66L1000,66L1000,67L1003,67L1003,68L1005,68L1005,68L1011,68L1011,68L1016,68L1016,69L1019,69L1019,69L1022,69L1022,70L1025,70L1025,71L1026,71L1026,71L1030,71L1030,72L1031,72L1031,72L1033,72L1033,72L1038,72L1038,72L1040,72L1040,73L1049,73L1049,73L1054,73L1054,73L1061,73L1061,73L1062,73L1062,74L1069,74L1069,74L1071,74L1071,74L1076,74L1076,74L1078,74L1078,74L1083,74L1083,74L1084,74L1084,74L1086,74L1086,74L1087,74L1087,75L1093,75L1093,75L1095,75L1095,75L1096,75L1096,75L1099,75L1099,75L1101,75L1101,75L1105,75L1105,76L1106,76L1106,78L1110,78L1110,78L1117,78L1117,79L1118,79L1118,80L1121,80L1121,80L1122,80L1122,81L1124,81L1124,81L1129,81L1129,81L1132,81L1132,81L1133,81L1133,81L1135,81L1135,82L1139,82L1139,82L1141,82L1141,82L1145,82L1145,82L1146,82L1146,82L1150,82L1150,82L1152,82L1152,82L1154,82L1154,82L1155,82L1155,83L1156,83L1156,83L1161,83L1161,83L1166,83L1166,83L1167,83L1167,84L1168,84L1168,84L1169,84L1169,84L1171,84L1171,84L1178,84L1178,84L1179,84L1179,86L1180,86L1180,87L1185,87L1185,87L1186,87L1186,87L1190,87L1190,87L1194,87L1194,87L1198,87L1198,87L1199,87L1199,87L1199,87" tabindex="0">
						<title>l_r9gn1qpd-9d8eewld-0nwoqwkl.jqzpx3gl</title>
					</path>
				</g>
				<g style="--index: 4;">
					<path d="M0,0L8,0L8,4L20,4L20,5L34,5L34,5L62,5L62,5L140,5L140,5L189,5L189,6L239,6L239,6L244,6L244,6L298,6L298,6L356,6L356,7L390,7L390,8L396,8L396,9L431,9L431,9L507,9L507,9L513,9L513,10L519,10L519,9L608,9L608,10L681,10L681,10L725,10L725,10L753,10L753,10L775,10L775,10L845,10L845,10L895,10L895,11L928,11L928,11L929,11L929,11L994,11L994,11L1000,11L1000,12L1005,12L1005,12L1016,12L1016,13L1031,13L1031,13L1048,13L1048,13L1064,13L1064,14L1080,14L1080,14L1088,14L1088,15L1097,15L1097,15L1099,15L1099,15L1101,15L1101,15L1106,15L1106,15L1107,15L1107,15L1113,15L1113,15L1125,15L1125,16L1141,16L1141,16L1155,16L1155,17L1160,17L1160,17L1166,17L1166,17L1169,17L1169,17L1191,17L1191,17L1194,17L1194,17L1199,17" tabindex="0">
						<title>l_r9gn1qpd-9d8eewld-0nwoqwkl.21g82pnl</title>
					</path>
				</g>
				<g style="--index: 5;">
					<path d="M0,0L8,0L8,31L26,31L26,32L28,32L28,33L31,33L31,33L33,33L33,35L35,35L35,36L37,36L37,35L38,35L38,37L40,37L40,37L45,37L45,37L53,37L53,37L54,37L54,37L58,37L58,37L61,37L61,38L64,38L64,38L69,38L69,38L79,38L79,38L81,38L81,38L83,38L83,38L84,38L84,39L86,39L86,39L87,39L87,40L109,40L109,40L116,40L116,41L133,41L133,41L137,41L137,41L141,41L141,41L145,41L145,41L151,41L151,41L164,41L164,41L169,41L169,40L174,40L174,41L182,41L182,41L183,41L183,41L188,41L188,41L201,41L201,41L206,41L206,42L207,42L207,41L208,41L208,41L212,41L212,42L213,42L213,42L222,42L222,43L230,43L230,43L235,43L235,43L240,43L240,44L244,44L244,44L255,44L255,44L256,44L256,44L260,44L260,44L264,44L264,44L266,44L266,44L269,44L269,44L270,44L270,44L290,44L290,45L291,45L291,46L298,46L298,46L300,46L300,47L305,47L305,47L312,47L312,47L313,47L313,47L315,47L315,47L325,47L325,47L339,47L339,48L346,48L346,48L349,48L349,48L369,48L369,49L370,49L370,48L380,48L380,49L421,49L421,49L433,49L433,49L454,49L454,49L460,49L460,50L466,50L466,50L472,50L472,50L480,50L480,51L507,51L507,51L536,51L536,51L561,51L561,51L600,51L600,50L603,50L603,50L604,50L604,51L624,51L624,52L627,52L627,52L634,52L634,52L659,52L659,52L669,52L669,52L671,52L671,52L677,52L677,52L707,52L707,52L733,52L733,53L740,53L740,53L748,53L748,53L761,53L761,53L762,53L762,53L769,53L769,53L774,53L774,53L778,53L778,53L780,53L780,53L790,53L790,54L802,54L802,54L810,54L810,54L830,54L830,54L835,54L835,54L836,54L836,54L851,54L851,54L863,54L863,54L869,54L869,54L872,54L872,54L878,54L878,55L881,55L881,55L909,55L909,55L915,55L915,56L917,56L917,56L927,56L927,56L928,56L928,56L932,56L932,57L938,57L938,58L940,58L940,58L943,58L943,58L948,58L948,58L957,58L957,59L966,59L966,60L971,60L971,61L972,61L972,61L975,61L975,61L978,61L978,62L982,62L982,62L995,62L995,62L996,62L996,62L998,62L998,62L999,62L999,62L1000,62L1000,62L1004,62L1004,62L1005,62L1005,62L1007,62L1007,62L1012,62L1012,62L1016,62L1016,63L1052,63L1052,63L1067,63L1067,62L1073,62L1073,62L1079,62L1079,62L1084,62L1084,63L1091,63L1091,63L1096,63L1096,63L1100,63L1100,63L1104,63L1104,63L1106,63L1106,63L1108,63L1108,63L1121,63L1121,63L1129,63L1129,63L1131,63L1131,64L1140,64L1140,64L1151,64L1151,64L1155,64L1155,64L1158,64L1158,64L1171,64L1171,64L1184,64L1184,64L1187,64L1187,64L1194,64L1194,64L1197,64L1197,64L1199,64" tabindex="0">
						<title>l_r9gn1qpd-9d8eewld-0nwoqwkl.p12dx87q</title>
					</path>
				</g>
				<g style="--index: 6;">
					<path d="M0,0L8,0L8,1L20,1L20,1L34,1L34,1L38,1L38,2L49,2L49,2L52,2L52,2L114,2L114,2L141,2L141,2L153,2L153,2L189,2L189,2L206,2L206,2L230,2L230,2L242,2L242,3L256,3L256,3L310,3L310,3L339,3L339,3L352,3L352,3L357,3L357,3L500,3L500,3L507,3L507,3L510,3L510,3L532,3L532,3L571,3L571,3L574,3L574,3L640,3L640,4L651,4L651,4L655,4L655,5L717,5L717,5L727,5L727,6L749,6L749,6L750,6L750,6L753,6L753,6L772,6L772,6L781,6L781,6L802,6L802,7L832,7L832,7L852,7L852,7L889,7L889,7L895,7L895,7L897,7L897,8L919,8L919,8L935,8L935,8L939,8L939,8L943,8L943,9L944,9L944,9L948,9L948,9L955,9L955,10L956,10L956,10L957,10L957,10L963,10L963,10L964,10L964,10L982,10L982,10L984,10L984,10L1000,10L1000,10L1005,10L1005,10L1016,10L1016,10L1018,10L1018,10L1031,10L1031,10L1036,10L1036,10L1039,10L1039,10L1040,10L1040,10L1050,10L1050,10L1051,10L1051,10L1067,10L1067,10L1078,10L1078,10L1079,10L1079,10L1082,10L1082,10L1093,10L1093,10L1101,10L1101,10L1107,10L1107,10L1118,10L1118,10L1124,10L1124,10L1125,10L1125,10L1130,10L1130,10L1142,10L1142,10L1155,10L1155,10L1158,10L1158,10L1166,10L1166,10L1170,10L1170,10L1194,10L1194,10L1195,10L1195,10L1199,10" tabindex="0">
						<title>l_r9gn1qpd-9d8eewld-0nwoqwkl.klrpy2jq</title>
					</path>
				</g>
				<g style="--index: 7;">
					<path d="M0,0L8,0L8,22L21,22L21,22L33,22L33,22L37,22L37,22L48,22L48,23L49,23L49,23L83,23L83,24L102,24L102,25L105,25L105,25L151,25L151,26L192,26L192,27L240,27L240,27L244,27L244,27L255,27L255,27L284,27L284,29L291,29L291,30L298,30L298,30L333,30L333,30L345,30L345,30L390,30L390,31L396,31L396,32L401,32L401,33L507,33L507,33L514,33L514,33L544,33L544,33L653,33L653,34L664,34L664,35L669,35L669,35L684,35L684,35L697,35L697,35L699,35L699,35L710,35L710,35L716,35L716,36L718,36L718,36L772,36L772,36L802,36L802,36L816,36L816,37L845,37L845,37L861,37L861,37L870,37L870,38L883,38L883,39L886,39L886,40L904,40L904,41L909,41L909,41L924,41L924,41L932,41L932,42L939,42L939,43L940,43L940,45L942,45L942,46L945,46L945,46L948,46L948,47L949,47L949,47L954,47L954,47L960,47L960,47L970,47L970,47L990,47L990,48L994,48L994,48L1000,48L1000,48L1004,48L1004,48L1005,48L1005,48L1016,48L1016,49L1018,49L1018,49L1035,49L1035,49L1079,49L1079,49L1096,49L1096,49L1099,49L1099,49L1118,49L1118,49L1121,49L1121,49L1125,49L1125,49L1128,49L1128,49L1133,49L1133,49L1141,49L1141,49L1146,49L1146,49L1147,49L1147,49L1152,49L1152,49L1155,49L1155,49L1157,49L1157,49L1159,49L1159,49L1170,49L1170,49L1172,49L1172,49L1194,49L1194,49L1199,49L1199,49L1199,49" tabindex="0">
						<title>l_r9gn1qpd-9d8eewld-0nwoqwkl.xqkjmxnl</title>
					</path>
				</g>
				<g style="--index: 8;">
					<path d="M0,0L331,0L331,4L333,4L333,4L337,4L337,4L340,4L340,5L346,5L346,6L351,6L351,6L364,6L364,7L370,7L370,7L371,7L371,8L376,8L376,9L380,9L380,9L382,9L382,9L384,9L384,9L391,9L391,10L398,10L398,10L434,10L434,10L438,10L438,10L443,10L443,10L445,10L445,10L452,10L452,10L458,10L458,10L464,10L464,10L480,10L480,10L485,10L485,10L493,10L493,10L500,10L500,10L506,10L506,10L549,10L549,10L594,10L594,10L611,10L611,10L612,10L612,10L616,10L616,10L629,10L629,10L630,10L630,10L653,10L653,10L654,10L654,10L659,10L659,10L661,10L661,10L676,10L676,10L680,10L680,10L698,10L698,10L706,10L706,10L725,10L725,10L726,10L726,10L732,10L732,10L739,10L739,10L741,10L741,11L765,11L765,11L766,11L766,11L772,11L772,11L773,11L773,12L774,12L774,12L782,12L782,12L784,12L784,12L789,12L789,12L799,12L799,12L805,12L805,12L823,12L823,12L850,12L850,12L853,12L853,12L857,12L857,12L861,12L861,12L863,12L863,12L864,12L864,12L867,12L867,12L895,12L895,12L899,12L899,12L902,12L902,12L904,12L904,12L910,12L910,12L911,12L911,12L913,12L913,12L919,12L919,12L923,12L923,12L924,12L924,12L928,12L928,12L929,12L929,12L933,12L933,12L937,12L937,13L938,13L938,13L942,13L942,13L943,13L943,13L944,13L944,13L945,13L945,13L948,13L948,13L955,13L955,13L957,13L957,13L961,13L961,13L963,13L963,13L975,13L975,13L976,13L976,13L984,13L984,13L999,13L999,13L1006,13L1006,13L1015,13L1015,13L1018,13L1018,13L1026,13L1026,13L1027,13L1027,13L1031,13L1031,13L1033,13L1033,13L1039,13L1039,13L1041,13L1041,13L1043,13L1043,13L1045,13L1045,13L1051,13L1051,13L1055,13L1055,13L1061,13L1061,13L1062,13L1062,14L1069,14L1069,14L1070,14L1070,13L1083,13L1083,13L1084,13L1084,13L1088,13L1088,12L1090,12L1090,12L1091,12L1091,12L1096,12L1096,12L1099,12L1099,12L1103,12L1103,12L1104,12L1104,12L1106,12L1106,12L1110,12L1110,12L1111,12L1111,12L1112,12L1112,12L1114,12L1114,12L1115,12L1115,12L1116,12L1116,12L1118,12L1118,12L1126,12L1126,12L1131,12L1131,13L1135,13L1135,13L1138,13L1138,12L1141,12L1141,12L1144,12L1144,12L1147,12L1147,12L1151,12L1151,12L1159,12L1159,12L1160,12L1160,12L1161,12L1161,12L1166,12L1166,12L1167,12L1167,12L1168,12L1168,12L1177,12L1177,12L1178,12L1178,12L1187,12L1187,12L1188,12L1188,13L1190,13L1190,12L1191,12L1191,12L1192,12L1192,12L1193,12L1193,13L1195,13L1195,13L1196,13L1196,13L1197,13L1197,13L1199,13" tabindex="0">
						<title>xd17x94d-9l71pv4l.21gpn081</title>
					</path>
				</g>
			</g>
		</svg>
	</foreignObject>
</svg>
