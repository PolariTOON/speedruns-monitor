<svg xmlns="http://www.w3.org/2000/svg" lang="en">
	<title>Score by leaderboard for player 8ew371p8</title>
	<metadata>
		<meta xmlns="http://www.w3.org/1999/xhtml" charset="utf-8" />
		<meta xmlns="http://www.w3.org/1999/xhtml" name="viewport" content="width=device-width" />
		<meta xmlns="http://www.w3.org/1999/xhtml" name="color-scheme" content="dark light" />
	</metadata>
	<style>
		*,
		::before,
		::after {
			box-sizing: border-box;
		}
		:root {
			background: var(--canvas-background);
			color: var(--canvas-foreground);
			fill: var(--canvas-background);
			stroke: var(--canvas-foreground);
			stroke-linejoin: round;
			stroke-linecap: round;
		}
		foreignObject {
			pointer-events: none;
		}
		foreignObject::before,
		foreignObject::after {
			content: "";
			position: absolute;
			z-index: 1;
			left: 20px;
			bottom: 20px;
			pointer-events: auto;
		}
		foreignObject::before {
			width: calc(100% - 40px);
			height: calc(20px + var(--date-size) * 1px);
		}
		foreignObject::after {
			width: calc(20px + var(--value-size) * 1px);
			height: calc(100% - 40px);
		}
		foreignObject &gt; div {
			position: absolute;
			z-index: 4;
			right: 0;
			top: 0;
			margin: 20px;
			border: 1px solid var(--canvas-foreground);
			background: var(--highlighted);
			font: 16px / 1.25 serif;
			transition: opacity 1s;
			pointer-events: auto;
		}
		foreignObject:is(:hover, :focus-within) &gt; div:not(:hover, :focus-within) {
			opacity: 0;
			pointer-events: none;
		}
		foreignObject &gt; div &gt; p {
			margin: 0;
		}
		label {
			display: table;
			border-collapse: separate;
			border-spacing: 10px;
			direction: rtl;
		}
		progress,
		input[type="checkbox"] {
			display: table-cell;
			width: 16px;
			height: 16px;
			margin: 2px;
			accent-color: var(--canvas-foreground);
			vertical-align: top;
			direction: ltr;
		}
		progress + span,
		input[type="checkbox"] + span {
			display: table-cell;
			vertical-align: top;
			direction: ltr;
		}
		input[type="checkbox"]:not(:disabled):is(:hover, :focus-within) + span {
			text-decoration: underline;
		}
		foreignObject &gt; svg:first-of-type,
		foreignObject &gt; svg:first-of-type + svg,
		foreignObject &gt; svg:first-of-type + svg + svg,
		foreignObject &gt; svg:first-of-type + svg + svg + svg {
			position: absolute;
			z-index: 2;
			margin: 0;
			padding: 10px;
			background: var(--highlighted);
			font: 16px / 1.25 serif;
			font-weight: bold;
			pointer-events: auto;
		}
		foreignObject &gt; svg:first-of-type,
		foreignObject &gt; svg:first-of-type + svg + svg {
			border-top: 1px solid #0000;
			border-bottom: 1px solid var(--canvas-foreground);
		}
		foreignObject &gt; svg:first-of-type + svg,
		foreignObject &gt; svg:first-of-type + svg + svg + svg {
			border-top: 1px dashed var(--canvas-foreground);
			border-bottom: 1px dashed #0000;
		}
		foreignObject &gt; svg:first-of-type,
		foreignObject &gt; svg:first-of-type + svg {
			width: calc(20px + var(--date-size) * 1px);
			height: 42px;
			bottom: 20px;
		}
		foreignObject &gt; svg:first-of-type {
			left: calc(40px + var(--value-size) * 1px);
			transform: rotate(90deg) translate(-100%, 0);
			transform-origin: left bottom;
		}
		foreignObject &gt; svg:first-of-type + svg {
			right: 20px;
			transform: rotate(90deg) translate(0, 100%);
			transform-origin: right bottom;
		}
		foreignObject &gt; svg:first-of-type + svg + svg,
		foreignObject &gt; svg:first-of-type + svg + svg + svg {
			width: calc(20px + var(--value-size) * 1px);
			height: 42px;
			left: 20px;
		}
		foreignObject &gt; svg:first-of-type + svg + svg {
			bottom: calc(40px + var(--date-size) * 1px);
		}
		foreignObject &gt; svg:first-of-type + svg + svg + svg {
			top: 20px;
		}
		foreignObject &gt; svg:first-of-type &gt; text,
		foreignObject &gt; svg:first-of-type + svg &gt; text,
		foreignObject &gt; svg:first-of-type + svg + svg &gt; text,
		foreignObject &gt; svg:first-of-type + svg + svg + svg &gt; text {
			fill: var(--canvas-foreground);
			stroke: none;
		}
		foreignObject &gt; svg:first-of-type &gt; text,
		foreignObject &gt; svg:first-of-type + svg &gt; text {
			cursor: vertical-text;
		}
		foreignObject &gt; svg:first-of-type + svg + svg + svg + svg {
			position: relative;
			z-index: 3;
			width: calc(100% - 60px - var(--value-size) * 1px);
			height: calc(100% - 60px - var(--date-size) * 1px);
			margin: 20px 20px calc(40px + var(--date-size) * 1px) calc(40px + var(--value-size) * 1px);
			border-left: 1px solid var(--canvas-foreground);
			border-right: 1px dashed var(--canvas-foreground);
			border-top: 1px dashed var(--canvas-foreground);
			border-bottom: 1px solid var(--canvas-foreground);
			background: var(--canvas-background);
			pointer-events: auto;
		}
		g &gt; rect {
			vector-effect: non-scaling-stroke;
			fill: var(--highlighted);
			stroke: none;
		}
		g &gt; rect[tabindex] {
			fill: var(--tier, #0000);
			cursor: help;
		}
		g &gt; rect[tabindex][data-tier="diamond"] {
			--tier: var(--diamond);
		}
		g &gt; rect[tabindex][data-tier="gold"] {
			--tier: var(--gold);
		}
		g &gt; rect[tabindex][data-tier="silver"] {
			--tier: var(--silver);
		}
		g &gt; rect[tabindex][data-tier="bronze"] {
			--tier: var(--bronze);
		}
		g &gt; path[tabindex] {
			vector-effect: non-scaling-stroke;
			fill: none;
			stroke: hsl(calc(var(--index) / var(--count) * 360), 50%, 50%);
			cursor: help;
		}
		g:is(:hover, :focus-within) &gt; path[tabindex] {
			stroke: var(--canvas-foreground);
		}
		g &gt; path[tabindex]:is(:hover, :focus-within) {
			outline: 0;
		}
		g &gt; path[tabindex]:first-of-type {
			stroke-width: 2;
		}
		g &gt; path[tabindex]:first-of-type:is(:hover, :focus-within) {
			stroke-width: 4;
		}
		g &gt; path[tabindex]:first-of-type:not(:last-of-type) {
			stroke-dasharray: 4 8;
		}
		g &gt; path[tabindex]:first-of-type + rect {
			fill: none;
		}
		g &gt; path[tabindex]:first-of-type:is(:hover, :focus-within) + rect {
			fill: var(--highlighted);
			pointer-events: none;
		}
		g &gt; path[tabindex]:first-of-type ~ path[tabindex] {
			stroke-width: 8;
		}
		g &gt; path[tabindex]:first-of-type ~ path[tabindex]:is(:hover, :focus-within) {
			stroke-width: 16;
		}
		g &gt; path[tabindex]:first-of-type ~ path[tabindex] + line,
		g &gt; path[tabindex]:first-of-type ~ path[tabindex] + line + line {
			vector-effect: non-scaling-stroke;
			fill: none;
			stroke: none;
			stroke-width: 2;
			stroke-dasharray: 4 8;
			pointer-events: none;
		}
		g &gt; path[tabindex]:first-of-type:is(:hover, :focus-within) + rect + path[tabindex] + line,
		g &gt; path[tabindex]:first-of-type:is(:hover, :focus-within) + rect + path[tabindex] + line + line,
		g &gt; path[tabindex]:first-of-type:is(:hover, :focus-within) ~ path[tabindex]:last-of-type + line,
		g &gt; path[tabindex]:first-of-type:is(:hover, :focus-within) ~ path[tabindex]:last-of-type + line + line,
		g &gt; path[tabindex]:first-of-type ~ path[tabindex]:is(:hover, :focus-within) + line,
		g &gt; path[tabindex]:first-of-type ~ path[tabindex]:is(:hover, :focus-within) + line + line {
			stroke: var(--faded);
		}
		@media (prefers-color-scheme: dark) {
			:root {
				--canvas-background: #000;
				--canvas-foreground: #ccc;
				--diamond: #0363;
				--gold: #6303;
				--silver: #3633;
				--bronze: #6363;
				--highlighted: #6663;
				--faded: #9993;
			}
		}
		@media (prefers-color-scheme: light) {
			:root {
				--canvas-background: #fff;
				--canvas-foreground: #333;
				--diamond: #9cf3;
				--gold: #fc93;
				--silver: #9c93;
				--bronze: #c9c3;
				--highlighted: #9993;
				--faded: #6663;
			}
		}
	</style>
	<script>
		"use strict";
		{
			const script = document.currentScript;
			const documentUrl = location.href;
			const scope = new URL("../../", documentUrl).pathname;
			const playersKey = `${scope}#players`;
			const leaderboardsKey = `${scope}#leaderboards`;
			const sessionKey = `${scope}#session`;
			const lockKey = `${scope}#lock`;
			async function importDataScript(dataScriptUrl, signal) {
				signal.throwIfAborted();
				if (documentUrl.protocol !== "file:") {
					const response = await fetch(dataScriptUrl, {
						signal,
					});
					if (!response.ok) {
						throw new Error(response.statusText);
					}
					const data = await response.json();
					return data;
				}
				return await new Promise((resolve, reject) =&gt; {
					const name = `callback${Math.random() * 2 ** 53}`;
					let called = false;
					let data = null;
					function callback(value) {
						if (!called) {
							called = true;
							data = value;
						}
					}
					window[name] = callback;
					const dataScript = document.createElementNS("http://www.w3.org/2000/svg", "script");
					dataScript.href = `${dataScriptUrl}&amp;callback=${name}`;
					function handleAbort(event) {
						delete window[name];
						dataScript.removeEventListener("error", handleError);
						dataScript.removeEventListener("load", handleLoad);
						dataScript.remove();
						const reason = signal.reason;
						reject(reason);
					}
					function handleError(event) {
						delete window[name];
						signal.removeEventListener("abort", handleAbort);
						dataScript.removeEventListener("load", handleLoad);
						dataScript.remove();
						const error = event.error;
						reject(error);
					}
					function handleLoad(event) {
						delete window[name];
						signal.removeEventListener("abort", handleAbort);
						dataScript.removeEventListener("error", handleError);
						dataScript.remove();
						resolve(data);
					}
					signal.addEventListener("abort", handleAbort, {
						once: true,
					});
					dataScript.addEventListener("error", handleError, {
						once: true,
					});
					dataScript.addEventListener("load", handleLoad, {
						once: true,
					});
					script.after(dataScript);
				});
			}
			async function waitForTimeout(delay, signal) {
				signal.throwIfAborted();
				return await new Promise((resolve, reject) =&gt; {
					let timeout = null;
					function handleAbort() {
						clearTimeout(timeout);
						const reason = signal.reason;
						reject(reason);
					}
					function handleTimeout() {
						signal.removeEventListener("abort", handleAbort);
						resolve(delay);
					}
					signal.addEventListener("abort", handleAbort, {
						once: true,
					});
					timeout = setTimeout(handleTimeout, delay);
				});
			}
			window.addEventListener("pageshow", (event) =&gt; {
				if (event.persisted) {
					console.log(`Successfully restored page from cache`);
				} else {
					console.log(`Unsuccessfully restored page from cache`);
				}
			});
			window.addEventListener("pagehide", (event) =&gt; {
				if (event.persisted) {
					console.log(`Successfully saved page to cache`);
				} else {
					console.log(`Unsuccessfully saved page to cache`);
				}
			});
			window.addEventListener("pageshow", async (event) =&gt; {
				const rootTitleElement = document.querySelector("title");
				const rootTitleContent = rootTitleElement.textContent;
				const pathTitleElements = Array.from(document.querySelectorAll("g &gt; path[tabindex]:first-of-type &gt; title"));
				const pathTitleContents = pathTitleElements.map((pathTitleElement) =&gt; {
					const pathTitleContent = pathTitleElement.textContent;
					return pathTitleContent;
				});
				const foreignObject = document.querySelector("foreignObject");
				const div = document.createElementNS("http://www.w3.org/1999/xhtml", "div");
				div.dir = "ltr";
				const p = document.createElementNS("http://www.w3.org/1999/xhtml", "p");
				const label = document.createElementNS("http://www.w3.org/1999/xhtml", "label");
				const progress = document.createElementNS("http://www.w3.org/1999/xhtml", "progress");
				label.append(progress);
				const span = document.createElementNS("http://www.w3.org/1999/xhtml", "span");
				span.textContent = "Loading names";
				label.append(span);
				p.append(label);
				div.append(p);
				foreignObject.prepend(div);
				let lock = false;
				let session = null;
				const broadcastChannel = new BroadcastChannel(sessionKey);
				const abortController = new AbortController();
				const signal = abortController.signal;
				const abort = abortController.abort.bind(abortController);
				const {promise, resolve} = Promise.withResolvers();
				broadcastChannel.addEventListener("message", (event) =&gt; {
					const data = event.data;
					const type = data.type;
					switch (type) {
						case "request": {
							if (lock &amp;&amp; signal.aborted) {
								broadcastChannel.postMessage({
									type: "response",
									session,
								});
							}
							break;
						}
						case "response": {
							if (!signal.aborted) {
								session = data.session;
								if (session != null) {
									const {players, leaderboards} = session;
									try {
										const cachedPlayers = JSON.stringify(players);
										const cachedLeaderboards = JSON.stringify(leaderboards);
										sessionStorage.setItem(playersKey, cachedPlayers);
										sessionStorage.setItem(leaderboardsKey, cachedLeaderboards);
									} catch {}
									console.log(`Successfully loaded names from peer`);
									abort();
									resolve();
								} else {
									console.log(`Unsuccessfully loaded names from peer`);
									abort();
									resolve();
								}
							}
							break;
						}
					}
				});
				try {
					const cachedPlayers = sessionStorage.getItem(playersKey);
					const cachedLeaderboards = sessionStorage.getItem(leaderboardsKey);
					if (cachedPlayers == null || cachedLeaderboards == null) {
						throw new Error();
					}
					const players = JSON.parse(cachedPlayers);
					const leaderboards = JSON.parse(cachedLeaderboards);
					session = {players, leaderboards};
				} catch {}
				if (session != null) {
					broadcastChannel.postMessage({
						type: "response",
						session,
					});
					console.log(`Successfully loaded names from local`);
					abort();
					resolve();
				} else {
					broadcastChannel.postMessage({
						type: "request",
					});
					console.log(`Unsuccessfully loaded names from local`);
				}
				(async () =&gt; {
					for (;;) {
						await navigator.locks.request(lockKey, async () =&gt; {
							lock = true;
							console.log(`Acquired lock`);
							if (!signal.aborted) {
								try {
									const players = Object.create(null);
									const leaderboards = Object.create(null);
									const games = ["9d3rrxyd", "w6jl2ned"];
									for (const gameId of games) {
										const {data} = await importDataScript(`https://www.speedrun.com/api/v1/games/${gameId}?embed=categories,levels,variables`, signal);
										const versions = data.variables.data.find((variable) =&gt; {
											return variable.name === "Version";
										});
										const levels = Object.fromEntries(data.levels.data.map((level) =&gt; {
											return [level.id, level];
										}));
										const categories = Object.fromEntries(data.categories.data.map((category) =&gt; {
											return [category.id, category];
										}));
										const variables = Object.fromEntries(data.variables.data.map((variable) =&gt; {
											return [variable.id, variable];
										}));
										console.log(`Got game`);
										await waitForTimeout(800, signal);
										const slice = 200;
										for (const categoryId of Object.keys(categories)) {
											for (let offset = 0;; offset += slice) {
												const {data, pagination} = await importDataScript(`https://www.speedrun.com/api/v1/runs?game=${gameId}&amp;category=${categoryId}&amp;orderby=date&amp;direction=asc&amp;embed=players&amp;offset=${offset}&amp;max=${slice}`, signal);
												const {size} = pagination;
												if (size === 0) {
													break;
												}
												for (const run of data) {
													const status = run.status.status;
													if (status == null || status === "new") {
														continue;
													}
													const anonymous = (run.players.data[0]?.rel ?? "guest") !== "user";
													const player = !anonymous ? run.players.data[0].id : "814p2558";
													const playerName = !anonymous ? run.players.data[0].names.international : "anonymous";
													players[player] ??= playerName;
													const level = run.level;
													const category = run.category;
													const values = Object.entries(run.values).filter(([variable]) =&gt; {
														return variables[variable]?.["is-subcategory"] ?? false;
													}).map(([variable, value]) =&gt; {
														return `-${variable}.${value}`;
													});
													const leaderboard = `${level != null ? `l_${level}-` : ""}${category}${values.join("")}`;
													const levelName = run.level != null ? levels[run.level]?.name ?? null : null;
													const categoryName = categories[run.category]?.name ?? null;
													const valueNames = Object.entries(run.values).filter(([variable]) =&gt; {
														return variables[variable]?.["is-subcategory"] ?? false;
													}).map(([variable, value]) =&gt; {
														return variables[variable]?.values.values[value]?.label ?? null;
													}).filter((valueName) =&gt; {
														return valueName != null;
													});
													const leaderboardName = `${levelName != null ? `${levelName}: ` : ""}${categoryName ?? ""}${values.length !== 0 ? ` - ${valueNames.join(", ")}` : ""}`;
													leaderboards[leaderboard] ??= leaderboardName;
												}
												console.log(`Got runs ${offset}-${offset + size - 1}`);
												await waitForTimeout(800, signal);
											}
											console.log(`Got category ${categoryId}`);
										}
									}
									session = {players, leaderboards};
								} catch {}
								if (!signal.aborted) {
									if (session != null) {
										const {players, leaderboards} = session;
										try {
											const cachedPlayers = JSON.stringify(players);
											const cachedLeaderboards = JSON.stringify(leaderboards);
											sessionStorage.setItem(playersKey, cachedPlayers);
											sessionStorage.setItem(leaderboardsKey, cachedLeaderboards);
										} catch {}
										broadcastChannel.postMessage({
											type: "response",
											session,
										});
										console.log(`Successfully loaded names from remote`);
										abort();
										resolve();
									} else {
										broadcastChannel.postMessage({
											type: "response",
											session,
										});
										console.log(`Unsuccessfully loaded names from remote`);
										abort();
										resolve();
									}
								}
							}
							await new Promise((resolve) =&gt; {
								window.addEventListener("beforeunload", () =&gt; {
									resolve();
								}, {
									once: true,
								});
							});
						});
						lock = false;
						console.log(`Released lock`);
						await new Promise((resolve) =&gt; {
							window.addEventListener("pagehide", () =&gt; {
								resolve();
							}, {
								once: true,
							});
						});
						await new Promise((resolve) =&gt;{
							window.addEventListener("pageshow", () =&gt; {
								resolve();
							}, {
								once: true,
							});
						});
					}
				})();
				await promise;
				if (session == null) {
					const input = document.createElementNS("http://www.w3.org/1999/xhtml", "input");
					input.type = "checkbox";
					input.autocomplete = "off";
					input.disabled = true;
					input.indeterminate = true;
					progress.replaceWith(input);
					span.textContent = "Unavailable names";
					return;
				}
				const {players, leaderboards} = session;
				const hasPlayerRoot = rootTitleContent.includes(" for player");
				const hasLeaderboardRoot = rootTitleContent.includes(" for leaderboard");
				const rootTitle = {
					element: rootTitleElement,
					contentWithIds: rootTitleContent,
					contentWithNames: hasPlayerRoot ? rootTitleContent.replace(/ for player ([0-9_a-z]+)/, ($0, $1) =&gt; {
						return ` for player ${players[$1] ?? $1}`;
					}) : hasLeaderboardRoot ? rootTitleContent.replace(/ for leaderboard ((?:l_[0-9_a-z]+-)?[0-9_a-z]+(-[0-9_a-z]+\.[0-9_a-z]+)*)/, ($0, $1) =&gt; {
						return ` for leaderboard ${leaderboards[$1] ?? $1}`;
					}) : rootTitleContent,
				};
				const hasPlayerPaths = rootTitleContent.includes(" by player");
				const hasLeaderboardPaths = rootTitleContent.includes(" by leaderboard");
				const pathTitles = pathTitleElements.map((pathTitleElement, k) =&gt; {
					const pathTitleContent = pathTitleContents[k];
					const pathTitle = {
						element: pathTitleElement,
						contentWithIds: pathTitleContent,
						contentWithNames: hasPlayerPaths ? players[pathTitleContent] ?? pathTitleContent : hasLeaderboardPaths ? leaderboards[pathTitleContent] ?? pathTitleContent : pathTitleContent,
					};
					return pathTitle;
				});
				const input = document.createElementNS("http://www.w3.org/1999/xhtml", "input");
				input.type = "checkbox";
				input.autocomplete = "off";
				input.addEventListener("change", () =&gt; {
					input.disabled = true;
					if (input.checked) {
						rootTitle.element.textContent = rootTitle.contentWithNames;
						for (const pathTitle of pathTitles) {
							pathTitle.element.textContent = pathTitle.contentWithNames;
						}
					} else {
						rootTitle.element.textContent = rootTitle.contentWithIds;
						for (const pathTitle of pathTitles) {
							pathTitle.element.textContent = pathTitle.contentWithIds;
						}
					}
					input.disabled = false;
				});
				progress.replaceWith(input);
				span.textContent = "Reveal names";
			}, {
				once: true,
			});
		}
	</script>
	<script>
		"use strict";
		{
			document.addEventListener("DOMContentLoaded", (event) =&gt; {
				const minDateText = document.querySelector("foreignObject &gt; svg:first-of-type &gt; text");
				const maxDateText = document.querySelector("foreignObject &gt; svg:first-of-type + svg &gt; text");
				const startX = Math.round(Date.parse(minDateText.textContent) / 86400000);
				function xToDate(x) {
					const datetime = new Date((startX + x) * 86400000);
					const year = `${datetime.getUTCFullYear()}`.padStart(4, "0");
					const month = `${datetime.getUTCMonth() + 1}`.padStart(2, "0");
					const day = `${datetime.getUTCDate()}`.padStart(2, "0");
					const date = `${year}-${month}-${day}`;
					const size = date.length * 10;
					return {date, size};
				}
				function setMinDate(x) {
					const {date, size} = xToDate(Math.floor(x));
					minDateText.textLength.baseVal.value = size;
					minDateText.textContent = date;
				}
				function setMaxDate(x) {
					const {date, size} = xToDate(Math.ceil(x));
					maxDateText.textLength.baseVal.value = size;
					maxDateText.textContent = date;
				}
				const minValueText = document.querySelector("foreignObject &gt; svg:first-of-type + svg + svg &gt; text");
				const maxValueText = document.querySelector("foreignObject &gt; svg:first-of-type + svg + svg + svg &gt; text");
				const timed = minValueText.textContent.includes(":");
				function yToValue(y) {
					if (timed) {
						const minutes = `${(y - y % 6000) / 6000}`.padStart(2, "0");
						const seconds = `${(y - y % 100) / 100 % 60}`.padStart(2, "0");
						const centiseconds = `${y % 100}`.padStart(2, "0");
						const value = `${minutes}:${seconds}.${centiseconds}`;
						const size = value.length * 10;
						return {value, size};
					} else {
						const value = `${y}`;
						const size = value.length * 10;
						return {value, size};
					}
				}
				function setMinValue(y) {
					const {value, size} = yToValue(Math.floor(y));
					minValueText.textLength.baseVal.value = size;
					minValueText.textContent = value;
				}
				function setMaxValue(y) {
					const {value, size} = yToValue(Math.ceil(y));
					maxValueText.textLength.baseVal.value = size;
					maxValueText.textContent = value;
				}
				const svg = document.querySelector("foreignObject &gt; svg:first-of-type + svg + svg + svg + svg");
				svg.tabIndex = -1;
				svg.style.setProperty("outline", "0");
				if (CSS.supports("overflow-clip-margin", "8px")) {
					svg.style.setProperty("overflow", "clip");
					svg.style.setProperty("overflow-clip-margin", "8px");
				}
				svg.style.setProperty("touch-action", "none");
				const {x: baseX, y: baseY, width: baseWidth, height: baseHeight} = svg.viewBox.baseVal;
				let pointerCount = 0;
				const pointers = Object.create(null);
				const xPointerIds = [];
				const yPointerIds = [];
				let x = null;
				let y = null;
				let width = null;
				let height = null;
				svg.addEventListener("pointerdown", (event) =&gt; {
					const {pointerId} = event;
					if (pointerId in pointers) {
						return;
					}
					let {x: animX, y: animY, width: animWidth, height: animHeight} = svg.viewBox.baseVal;
					svg.setPointerCapture(pointerId);
					++pointerCount;
					const offsetX = event.offsetX;
					const offsetY = event.offsetY;
					pointers[pointerId] = {
						offsetX,
						offsetY,
					};
					xPointerIds.splice(xPointerIds.findLastIndex((pointerId) =&gt; {
						return pointers[pointerId].offsetX &lt;= offsetX;
					}) + 1, 0, pointerId);
					yPointerIds.splice(yPointerIds.findLastIndex((pointerId) =&gt; {
						return pointers[pointerId].offsetY &lt;= offsetY;
					}) + 1, 0, pointerId);
					const minOffsetX = pointers[xPointerIds[0]].offsetX;
					const minOffsetY = pointers[yPointerIds[0]].offsetY;
					const maxClientWidth = pointers[xPointerIds[pointerCount - 1]].offsetX - minOffsetX;
					const maxClientHeight = pointers[yPointerIds[pointerCount - 1]].offsetY - minOffsetY;
					width = Math.min(maxClientWidth / svg.clientWidth * animWidth, baseWidth);
					height = Math.min(maxClientHeight / svg.clientHeight * animHeight, baseHeight);
					x = Math.max(Math.min(animX + minOffsetX / svg.clientWidth * animWidth, baseX + baseWidth), baseX);
					y = Math.max(Math.min(animY + minOffsetY / svg.clientHeight * animHeight, baseY + baseHeight), baseY);
				});
				svg.addEventListener("pointerup", (event) =&gt; {
					const {pointerId} = event;
					if (!(pointerId in pointers)) {
						return;
					}
					let {x: animX, y: animY, width: animWidth, height: animHeight} = svg.viewBox.baseVal;
					svg.releasePointerCapture(pointerId);
					--pointerCount;
					delete pointers[pointerId];
					xPointerIds.splice(xPointerIds.lastIndexOf(pointerId), 1);
					yPointerIds.splice(yPointerIds.lastIndexOf(pointerId), 1);
					const minOffsetX = pointerCount !== 0 ? pointers[xPointerIds[0]].offsetX : null;
					const minOffsetY = pointerCount !== 0 ? pointers[yPointerIds[0]].offsetY : null;
					const maxClientWidth = pointerCount !== 0 ? pointers[xPointerIds[pointerCount - 1]].offsetX - minOffsetX : null;
					const maxClientHeight = pointerCount !== 0 ? pointers[yPointerIds[pointerCount - 1]].offsetY - minOffsetY : null;
					width = pointerCount !== 0 ? Math.min(maxClientWidth / svg.clientWidth * animWidth, baseWidth) : null;
					height = pointerCount !== 0 ? Math.min(maxClientHeight / svg.clientHeight * animHeight, baseHeight) : null;
					x = pointerCount !== 0 ? Math.max(Math.min(animX + minOffsetX / svg.clientWidth * animWidth, baseX + baseWidth), baseX) : null;
					y = pointerCount !== 0 ? Math.max(Math.min(animY + minOffsetY / svg.clientHeight * animHeight, baseY + baseHeight), baseY) : null;
				});
				svg.addEventListener("pointermove", (event) =&gt; {
					const {pointerId} = event;
					if (!(pointerId in pointers)) {
						return;
					}
					let {x: animX, y: animY, width: animWidth, height: animHeight} = svg.viewBox.baseVal;
					event.preventDefault();
					const offsetX = event.offsetX;
					const offsetY = event.offsetY;
					pointers[pointerId].offsetX = offsetX;
					pointers[pointerId].offsetY = offsetY;
					const oldXIndex = xPointerIds.lastIndexOf(pointerId);
					let newXIndex = oldXIndex;
					while (newXIndex &gt; 0 &amp;&amp; pointers[xPointerIds[newXIndex - 1]].offsetX &gt; offsetX) {
						--newXIndex;
					}
					xPointerIds.copyWithin(newXIndex + 1, newXIndex, oldXIndex);
					xPointerIds[newXIndex] = pointerId;
					while (newXIndex &lt; pointerCount - 1 &amp;&amp; pointers[xPointerIds[newXIndex + 1]].offsetX &lt; offsetX) {
						++newXIndex;
					}
					xPointerIds.copyWithin(oldXIndex, oldXIndex + 1, newXIndex + 1);
					xPointerIds[newXIndex] = pointerId;
					const oldYIndex = yPointerIds.lastIndexOf(pointerId);
					let newYIndex = oldYIndex;
					while (newYIndex &gt; 0 &amp;&amp; pointers[yPointerIds[newYIndex - 1]].offsetY &gt; offsetY) {
						--newYIndex;
					}
					yPointerIds.copyWithin(newYIndex + 1, newYIndex, oldYIndex);
					yPointerIds[newYIndex] = pointerId;
					while (newYIndex &lt; pointerCount - 1 &amp;&amp; pointers[yPointerIds[newYIndex + 1]].offsetY &lt; offsetY) {
						++newYIndex;
					}
					yPointerIds.copyWithin(oldYIndex, oldYIndex + 1, newYIndex + 1);
					yPointerIds[newYIndex] = pointerId;
					const minOffsetX = pointers[xPointerIds[0]].offsetX;
					const minOffsetY = pointers[yPointerIds[0]].offsetY;
					const maxClientWidth = pointers[xPointerIds[pointerCount - 1]].offsetX - minOffsetX;
					const maxClientHeight = pointers[yPointerIds[pointerCount - 1]].offsetY - minOffsetY;
					animWidth = width === 0 ? animWidth : maxClientWidth === 0 ? baseWidth : Math.min(svg.clientWidth / maxClientWidth * width, baseWidth);
					animHeight = height === 0 ? animHeight : maxClientHeight === 0 ? baseHeight : Math.min(svg.clientHeight / maxClientHeight * height, baseHeight);
					animX = Math.max(Math.min(x - minOffsetX / svg.clientWidth * animWidth, baseX + baseWidth - animWidth), baseX);
					animY = Math.max(Math.min(y - minOffsetY / svg.clientHeight * animHeight, baseY + baseHeight - animHeight), baseY);
					svg.viewBox.baseVal.x = animX;
					svg.viewBox.baseVal.y = animY;
					svg.viewBox.baseVal.width = animWidth;
					svg.viewBox.baseVal.height = animHeight;
					setMinDate(animX);
					setMaxDate(animX + animWidth);
					setMinValue(baseY * 2 + baseHeight - animHeight - animY);
					setMaxValue(baseY * 2 + baseHeight - animY);
				});
				const zoomIntensity = 1.25;
				svg.addEventListener("wheel", (event) =&gt; {
					let {x: animX, y: animY, width: animWidth, height: animHeight} = svg.viewBox.baseVal;
					const {ctrlKey, shiftKey} = event;
					event.preventDefault();
					if (pointerCount !== 0) {
						return;
					}
					if (!ctrlKey) {
						const vectorX = (shiftKey ? event.deltaY : event.deltaX) / svg.clientWidth * animWidth;
						const vectorY = (shiftKey ? 0 : event.deltaY) / svg.clientHeight * animHeight;
						animX = Math.max(Math.min(animX + vectorX, baseX + baseWidth - animWidth), baseX);
						animY = Math.max(Math.min(animY + vectorY, baseY + baseHeight - animHeight), baseY);
						svg.viewBox.baseVal.x = animX;
						svg.viewBox.baseVal.y = animY;
						setMinDate(animX);
						setMaxDate(animX + animWidth);
						setMinValue(baseY * 2 + baseHeight - animHeight - animY);
						setMaxValue(baseY * 2 + baseHeight - animY);
						return;
					}
					const ratioX = zoomIntensity ** Math.sign(shiftKey ? event.deltaY : event.deltaX);
					const ratioY = zoomIntensity ** Math.sign(shiftKey ? 0 : event.deltaY);
					const positionX = Math.max(0, Math.min(event.offsetX / svg.clientWidth, 1)) * animWidth;
					const positionY = Math.max(0, Math.min(event.offsetY / svg.clientHeight, 1)) * animHeight;
					animWidth = Math.min(animWidth * ratioX, baseWidth);
					animHeight = Math.min(animHeight * ratioY, baseHeight);
					animX = Math.max(Math.min(animX + positionX * (1 - ratioX), baseX + baseWidth - animWidth), baseX);
					animY = Math.max(Math.min(animY + positionY * (1 - ratioY), baseY + baseHeight - animHeight), baseY);
					svg.viewBox.baseVal.x = animX;
					svg.viewBox.baseVal.y = animY;
					svg.viewBox.baseVal.width = animWidth;
					svg.viewBox.baseVal.height = animHeight;
					setMinDate(animX);
					setMaxDate(animX + animWidth);
					setMinValue(baseY * 2 + baseHeight - animHeight - animY);
					setMaxValue(baseY * 2 + baseHeight - animY);
				}, {
					passive: false,
				});
				svg.addEventListener("keydown", (event) =&gt; {
					let {x: animX, y: animY, width: animWidth, height: animHeight} = svg.viewBox.baseVal;
					const {ctrlKey, shiftKey} = event;
					switch (event.code) {
						case "Space": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							const vectorY = shiftKey ? -animHeight : animHeight;
							animY = Math.max(Math.min(animY + vectorY, baseY + baseHeight - animHeight), baseY);
							svg.viewBox.baseVal.y = animY;
							setMinValue(baseY * 2 + baseHeight - animHeight - animY);
							setMaxValue(baseY * 2 + baseHeight - animY);
							return;
						}
						case "Equal":
						case "NumpadAdd":
						case "Minus":
						case "NumpadSubtract": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							const ratio = event.code === "Minus" || event.code === "NumpadSubtract" ? zoomIntensity : 1 / zoomIntensity;
							if (!shiftKey) {
								animHeight = Math.min(animHeight * ratio, baseHeight);
								animY = Math.max(Math.min(animY, baseY + baseHeight - animHeight), baseY);
								svg.viewBox.baseVal.y = animY;
								svg.viewBox.baseVal.height = animHeight;
								setMinValue(baseY * 2 + baseHeight - animHeight - animY);
								setMaxValue(baseY * 2 + baseHeight - animY);
								return;
							}
							animWidth = Math.min(animWidth * ratio, baseWidth);
							animX = Math.max(Math.min(animX, baseX + baseWidth - animWidth), baseX);
							svg.viewBox.baseVal.x = animX;
							svg.viewBox.baseVal.width = animWidth;
							setMinDate(animX);
							setMaxDate(animX + animWidth);
							return;
						}
						case "Digit0":
						case "Numpad0": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							if (!shiftKey) {
								animY = baseY;
								animHeight = baseHeight;
								svg.viewBox.baseVal.y = animY;
								svg.viewBox.baseVal.height = animHeight;
								setMinValue(baseY * 2 + baseHeight - animHeight - animY);
								setMaxValue(baseY * 2 + baseHeight - animY);
								return;
							}
							animX = baseX;
							animWidth = baseWidth;
							svg.viewBox.baseVal.x = animX;
							svg.viewBox.baseVal.width = animWidth;
							setMinDate(animX);
							setMaxDate(animX + animWidth);
							return;
						}
					}
					switch (event.key) {
						case "ArrowRight": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							const vectorX = 50 / svg.clientWidth * animWidth;
							animX = Math.max(Math.min(animX + vectorX, baseX + baseWidth - animWidth), baseX);
							svg.viewBox.baseVal.x = animX;
							setMinDate(animX);
							setMaxDate(animX + animWidth);
							return;
						}
						case "ArrowLeft": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							const vectorX = -50 / svg.clientWidth * animWidth;
							animX = Math.max(Math.min(animX + vectorX, baseX + baseWidth - animWidth), baseX);
							svg.viewBox.baseVal.x = animX;
							setMinDate(animX);
							setMaxDate(animX + animWidth);
							return;
						}
						case "ArrowDown": {
							if (!ctrlKey) {
								event.preventDefault();
								if (pointerCount !== 0) {
									return;
								}
								const vectorY = 50 / svg.clientHeight * animHeight;
								animY = Math.max(Math.min(animY + vectorY, baseY + baseHeight - animHeight), baseY);
								svg.viewBox.baseVal.y = animY;
								setMinValue(baseY * 2 + baseHeight - animHeight - animY);
								setMaxValue(baseY * 2 + baseHeight - animY);
								return;
							}
						}
						case "End": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							animY = baseY + baseHeight - animHeight;
							svg.viewBox.baseVal.y = animY;
							setMinValue(baseY * 2 + baseHeight - animHeight - animY);
							setMaxValue(baseY * 2 + baseHeight - animY);
							return;
						}
						case "ArrowUp": {
							if (!ctrlKey) {
								event.preventDefault();
								if (pointerCount !== 0) {
									return;
								}
								const vectorY = -50 / svg.clientHeight * animHeight;
								animY = Math.max(Math.min(animY + vectorY, baseY + baseHeight - animHeight), baseY);
								svg.viewBox.baseVal.y = animY;
								setMinValue(baseY * 2 + baseHeight - animHeight - animY);
								setMaxValue(baseY * 2 + baseHeight - animY);
								return;
							}
						}
						case "Home": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							animY = baseY;
							svg.viewBox.baseVal.y = animY;
							setMinValue(baseY * 2 + baseHeight - animHeight - animY);
							setMaxValue(baseY * 2 + baseHeight - animY);
							return;
						}
						case "PageDown":
						case "PageUp":
						case " ": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							const vectorY = event.key === "PageUp" || event.key === " " &amp;&amp; shiftKey ? -animHeight : animHeight;
							animY = Math.max(Math.min(animY + vectorY, baseY + baseHeight - animHeight), baseY);
							svg.viewBox.baseVal.y = animY;
							setMinValue(baseY * 2 + baseHeight - animHeight - animY);
							setMaxValue(baseY * 2 + baseHeight - animY);
							return;
						}
						case "+":
						case "-": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							const ratio = event.key === "-" ? 1 / zoomIntensity : zoomIntensity;
							animWidth = Math.min(animWidth * ratio, baseWidth);
							animHeight = Math.min(animHeight * ratio, baseHeight);
							animX = Math.max(Math.min(animX, baseX + baseWidth - animWidth), baseX);
							animY = Math.max(Math.min(animY, baseY + baseHeight - animHeight), baseY);
							svg.viewBox.baseVal.x = animX;
							svg.viewBox.baseVal.y = animY;
							svg.viewBox.baseVal.width = animWidth;
							svg.viewBox.baseVal.height = animHeight;
							setMinDate(animX);
							setMaxDate(animX + animWidth);
							setMinValue(baseY * 2 + baseHeight - animHeight - animY);
							setMaxValue(baseY * 2 + baseHeight - animY);
							return;
						}
						case "0": {
							event.preventDefault();
							if (pointerCount !== 0) {
								return;
							}
							animX = baseX;
							animY = baseY;
							animWidth = baseWidth;
							animHeight = baseHeight;
							svg.viewBox.baseVal.x = animX;
							svg.viewBox.baseVal.y = animY;
							svg.viewBox.baseVal.width = animWidth;
							svg.viewBox.baseVal.height = animHeight;
							setMinDate(animX);
							setMaxDate(animX + animWidth);
							setMinValue(baseY * 2 + baseHeight - animHeight - animY);
							setMaxValue(baseY * 2 + baseHeight - animY);
							return;
						}
					}
				});
				svg.addEventListener("focusin", (event) =&gt; {
					if (!event.target.matches(":focus-visible")) {
						return;
					}
					event.preventDefault();
					if (pointerCount !== 0) {
						return;
					}
					let {x: animX, y: animY, width: animWidth, height: animHeight} = svg.viewBox.baseVal;
					let {x, y, width, height} = event.target.getBBox();
					width = Math.round(width);
					height = Math.round(height);
					x = Math.round(x);
					y = baseY * 2 + baseHeight - height - Math.round(y);
					if (x + width &lt; animX) {
						animX = x;
						if (width &gt; animWidth) {
							animX += (width - animWidth) / 2;
						}
					} else if (x &gt; animX + animWidth) {
						animX = x + width - animWidth;
						if (width &gt; animWidth) {
							animX -= (width - animWidth) / 2;
						}
					}
					if (y + height &lt; animY) {
						animY = y;
						if (height &gt; animHeight) {
							animY += (height - animHeight) / 2;
						}
					} else if (y &gt; animY + animHeight) {
						animY = y + height - animHeight;
						if (height &gt; animHeight) {
							animY -= (height - animHeight) / 2;
						}
					}
					svg.viewBox.baseVal.x = animX;
					svg.viewBox.baseVal.y = animY;
					setMinDate(animX);
					setMaxDate(animX + animWidth);
					setMinValue(baseY * 2 + baseHeight - animHeight - animY);
					setMaxValue(baseY * 2 + baseHeight - animY);
				});
			}, {
				once: true,
			});
		}
	</script>
	<foreignObject x="0" y="0" width="100%" height="100%" style="--date-size: 100; --value-size: 30;">
		<svg viewBox="0 0 100 20">
			<text x="50" y="10" dx="0" dy="2" textLength="100" lengthAdjust="spacingAndGlyphs" text-anchor="middle" dominant-baseline="middle">2023-08-01</text>
		</svg>
		<svg viewBox="0 0 100 20">
			<text x="50" y="10" dx="0" dy="2" textLength="100" lengthAdjust="spacingAndGlyphs" text-anchor="middle" dominant-baseline="middle">2026-07-31</text>
		</svg>
		<svg viewBox="0 0 30 20">
			<text x="15" y="10" dx="0" dy="2" textLength="10" lengthAdjust="spacingAndGlyphs" text-anchor="middle" dominant-baseline="middle">0</text>
		</svg>
		<svg viewBox="0 0 30 20">
			<text x="15" y="10" dx="0" dy="2" textLength="30" lengthAdjust="spacingAndGlyphs" text-anchor="middle" dominant-baseline="middle">445</text>
		</svg>
		<svg viewBox="0 0 1095 445" preserveAspectRatio="none" overflow="visible">
			<g transform="translate(0 445) scale(1 -1)" style="--count: 9;">
				<g>
					<rect x="0" y="0" width="1095" height="445"/>
				</g>
				<g style="--index: 0;">
					<path d="M0,0L0,0L0,11L744,11L744,11L767,11L767,11L854,11L854,11L967,11L967,11L982,11L982,11L998,11L998,11L1019,11L1019,11L1054,11L1054,11L1095,11" tabindex="0">
						<title>9kv6l5e2-ql6rezjl.jq6d86v1</title>
					</path>
				</g>
				<g style="--index: 1;">
					<path d="M0,0L16,0L16,7L18,7L18,8L24,8L24,9L60,9L60,10L65,10L65,11L66,11L66,13L67,13L67,13L121,13L121,14L131,14L131,14L134,14L134,15L180,15L180,14L251,14L251,15L282,15L282,16L330,16L330,17L334,17L334,18L365,18L365,19L368,19L368,19L375,19L375,20L429,20L429,20L450,20L450,20L464,20L464,21L469,21L469,22L476,22L476,23L500,23L500,24L518,24L518,25L540,25L540,26L541,26L541,27L543,27L543,26L547,26L547,27L572,27L572,28L575,28L575,29L578,29L578,30L581,30L581,31L586,31L586,32L593,32L593,32L601,32L601,32L641,32L641,33L650,33L650,34L668,34L668,35L676,35L676,35L687,35L687,36L688,36L688,35L691,35L691,35L697,35L697,35L705,35L705,35L712,35L712,36L719,36L719,36L721,36L721,37L726,37L726,38L728,38L728,40L730,40L730,40L735,40L735,40L743,40L743,40L750,40L750,41L753,41L753,41L760,41L760,42L763,42L763,43L808,43L808,44L829,44L829,45L832,45L832,46L834,46L834,45L858,45L858,46L875,46L875,47L876,47L876,47L882,47L882,48L884,48L884,50L887,50L887,51L893,51L893,51L898,51L898,51L906,51L906,52L912,52L912,51L916,51L916,52L918,52L918,53L925,53L925,53L936,53L936,53L943,53L943,54L949,54L949,55L956,55L956,56L965,56L965,55L967,55L967,55L968,55L968,56L970,56L970,57L973,57L973,56L977,56L977,57L988,57L988,57L989,57L989,56L990,56L990,57L992,57L992,58L993,58L993,59L998,59L998,61L999,61L999,61L1004,61L1004,62L1011,62L1011,63L1021,63L1021,62L1025,62L1025,61L1026,61L1026,62L1028,62L1028,63L1029,63L1029,64L1031,64L1031,65L1032,65L1032,64L1040,64L1040,65L1044,65L1044,64L1046,64L1046,64L1050,64L1050,66L1053,66L1053,67L1054,67L1054,68L1061,68L1061,69L1070,69L1070,71L1072,71L1072,71L1077,71L1077,72L1078,72L1078,73L1083,73L1083,74L1092,74L1092,74L1095,74" tabindex="0">
						<title>9kv6l5e2-ql6rezjl.1dk62o3l</title>
					</path>
				</g>
				<g style="--index: 2;">
					<path d="M0,0L16,0L16,3L18,3L18,3L19,3L19,4L24,4L24,5L84,5L84,6L101,6L101,7L210,7L210,8L377,8L377,9L403,9L403,9L450,9L450,11L544,11L544,11L551,11L551,12L568,12L568,13L576,13L576,14L589,14L589,15L604,15L604,15L606,15L606,16L642,16L642,17L654,17L654,18L676,18L676,18L697,18L697,19L701,19L701,19L706,19L706,19L708,19L708,19L720,19L720,20L726,20L726,20L731,20L731,20L743,20L743,20L750,20L750,21L754,21L754,22L781,22L781,22L818,22L818,22L837,22L837,21L849,21L849,22L852,22L852,22L876,22L876,23L887,23L887,24L898,24L898,24L918,24L918,25L934,25L934,26L943,26L943,27L951,27L951,28L966,28L966,28L968,28L968,29L971,29L971,30L978,30L978,31L981,31L981,31L990,31L990,30L998,30L998,30L1009,30L1009,31L1028,31L1028,32L1029,32L1029,33L1048,33L1048,33L1070,33L1070,35L1072,35L1072,36L1085,36L1085,37L1088,37L1088,38L1090,38L1090,38L1095,38" tabindex="0">
						<title>rkl9qwrk-6njkg4el.lr3x560l</title>
					</path>
				</g>
				<g style="--index: 3;">
					<path d="M0,0L19,0L19,7L66,7L66,7L88,7L88,8L216,8L216,8L380,8L380,8L577,8L577,8L596,8L596,8L597,8L597,8L598,8L598,8L629,8L629,8L694,8L694,8L698,8L698,8L723,8L723,8L728,8L728,8L729,8L729,8L734,8L734,8L738,8L738,8L755,8L755,8L803,8L803,8L833,8L833,8L846,8L846,8L857,8L857,8L860,8L860,8L868,8L868,8L874,8L874,8L882,8L882,8L899,8L899,8L910,8L910,8L916,8L916,8L930,8L930,8L940,8L940,8L947,8L947,8L961,8L961,9L1001,9L1001,9L1005,9L1005,9L1006,9L1006,9L1019,9L1019,9L1021,9L1021,9L1030,9L1030,9L1047,9L1047,9L1048,9L1048,10L1053,10L1053,11L1056,11L1056,11L1060,11L1060,10L1062,10L1062,10L1064,10L1064,10L1071,10L1071,10L1073,10L1073,10L1078,10L1078,10L1081,10L1081,10L1083,10L1083,10L1089,10L1089,11L1095,11" tabindex="0">
						<title>l_o9xo3rp9-9d8eewld-7896y468.5lermx5q</title>
					</path>
				</g>
				<g style="--index: 4;">
					<path d="M0,0L19,0L19,27L24,27L24,28L130,28L130,28L137,28L137,28L167,28L167,28L276,28L276,29L287,29L287,30L292,30L292,30L307,30L307,30L320,30L320,30L322,30L322,30L333,30L333,30L339,30L339,31L341,31L341,31L395,31L395,31L425,31L425,31L439,31L439,32L468,32L468,32L484,32L484,32L493,32L493,33L506,33L506,33L509,33L509,34L527,34L527,34L532,34L532,34L547,34L547,34L555,34L555,35L562,35L562,36L563,36L563,36L565,36L565,36L568,36L568,36L571,36L571,37L572,37L572,37L577,37L577,37L583,37L583,37L593,37L593,37L613,37L613,38L617,38L617,38L623,38L623,38L627,38L627,38L628,38L628,38L639,38L639,38L641,38L641,38L658,38L658,38L702,38L702,38L719,38L719,38L722,38L722,38L741,38L741,38L744,38L744,38L748,38L748,38L751,38L751,38L756,38L756,38L764,38L764,38L769,38L769,38L770,38L770,38L775,38L775,38L778,38L778,38L780,38L780,38L782,38L782,38L793,38L793,38L795,38L795,38L817,38L817,38L822,38L822,38L833,38L833,38L838,38L838,38L847,38L847,38L848,38L848,39L851,39L851,39L855,39L855,39L858,39L858,39L859,39L859,39L864,39L864,39L871,39L871,39L878,39L878,39L879,39L879,39L880,39L880,39L881,39L881,39L883,39L883,39L886,39L886,39L888,39L888,39L889,39L889,39L892,39L892,39L893,39L893,39L894,39L894,39L895,39L895,39L903,39L903,39L905,39L905,39L909,39L909,41L910,41L910,41L914,41L914,41L915,41L915,41L920,41L920,41L921,41L921,41L924,41L924,41L927,41L927,41L942,41L942,41L943,41L943,41L947,41L947,41L951,41L951,41L955,41L955,41L957,41L957,42L958,42L958,41L965,41L965,41L972,41L972,41L976,41L976,41L979,41L979,41L980,41L980,41L983,41L983,41L984,41L984,41L992,41L992,41L995,41L995,41L996,41L996,41L1002,41L1002,41L1004,41L1004,41L1006,41L1006,41L1008,41L1008,41L1012,41L1012,41L1013,41L1013,41L1021,41L1021,41L1024,41L1024,41L1029,41L1029,41L1031,41L1031,41L1034,41L1034,42L1035,42L1035,43L1038,43L1038,43L1040,43L1040,43L1046,43L1046,44L1049,44L1049,44L1050,44L1050,44L1055,44L1055,44L1057,44L1057,44L1060,44L1060,44L1064,44L1064,45L1066,45L1066,46L1067,46L1067,46L1074,46L1074,46L1075,46L1075,46L1078,46L1078,46L1081,46L1081,46L1084,46L1084,46L1085,46L1085,46L1086,46L1086,46L1087,46L1087,46L1088,46L1088,46L1089,46L1089,46L1090,46L1090,47L1091,47L1091,47L1092,47L1092,47L1093,47L1093,48L1094,48L1094,48L1095,48L1095,48L1095,48" tabindex="0">
						<title>l_r9gn1qpd-9d8eewld-0nwoqwkl.xqkjmxnl</title>
					</path>
				</g>
				<g style="--index: 5;">
					<path d="M0,0L20,0L20,5L87,5L87,5L89,5L89,20L381,20L381,20L619,20L619,21L908,21L908,22L914,22L914,21L951,21L951,21L997,21L997,21L998,21L998,22L1081,22L1081,23L1095,23" tabindex="0">
						<title>l_495zlx39-9d8eewld-2lgr793n.21g8ownl</title>
					</path>
				</g>
				<g style="--index: 6;">
					<path d="M0,0L75,0L75,84L77,84L77,85L93,85L93,86L94,86L94,87L108,87L108,88L120,88L120,89L123,89L123,91L187,91L187,92L188,92L188,93L189,93L189,92L212,92L212,93L231,93L231,94L245,94L245,95L254,95L254,96L257,96L257,96L271,96L271,96L274,96L274,96L277,96L277,97L291,97L291,97L292,97L292,98L300,98L300,99L308,99L308,98L309,98L309,100L322,100L322,101L327,101L327,102L329,102L329,103L330,103L330,104L331,104L331,105L332,105L332,105L335,105L335,106L339,106L339,106L342,106L342,107L343,107L343,108L346,108L346,109L349,109L349,110L354,110L354,111L358,111L358,112L360,112L360,113L364,113L364,114L370,114L370,115L372,115L372,116L375,116L375,117L380,117L380,118L381,118L381,117L383,117L383,118L385,118L385,117L394,117L394,118L396,118L396,119L400,119L400,118L403,118L403,118L404,118L404,119L415,119L415,120L435,120L435,121L438,121L438,122L446,122L446,122L452,122L452,123L458,123L458,124L460,124L460,123L468,123L468,123L469,123L469,123L473,123L473,123L474,123L474,123L477,123L477,124L481,124L481,124L484,124L484,126L494,126L494,126L495,126L495,126L503,126L503,126L505,126L505,127L509,127L509,128L512,128L512,129L513,129L513,130L519,130L519,131L523,131L523,132L525,132L525,134L528,134L528,135L534,135L534,137L538,137L538,138L542,138L542,139L549,139L549,140L552,140L552,141L563,141L563,142L564,142L564,141L565,141L565,142L567,142L567,142L569,142L569,143L570,143L570,143L580,143L580,144L585,144L585,144L589,144L589,144L591,144L591,144L595,144L595,144L598,144L598,145L602,145L602,146L607,146L607,147L612,147L612,148L615,148L615,149L617,149L617,150L623,150L623,151L626,151L626,152L628,152L628,153L634,153L634,154L639,154L639,155L642,155L642,156L645,156L645,157L648,157L648,159L649,159L649,160L653,160L653,161L654,161L654,161L656,161L656,161L659,161L659,160L661,160L661,161L663,161L663,162L668,162L668,161L672,161L672,162L677,162L677,163L684,163L684,163L685,163L685,165L692,165L692,167L693,167L693,166L694,166L694,166L699,166L699,167L701,167L701,167L706,167L706,167L707,167L707,168L709,168L709,169L710,169L710,169L716,169L716,169L718,169L718,170L719,170L719,171L722,171L722,172L724,172L724,173L728,173L728,174L729,174L729,176L733,176L733,177L740,177L740,180L741,180L741,181L744,181L744,182L745,182L745,184L747,184L747,185L749,185L749,184L752,184L752,185L754,185L754,184L755,184L755,184L756,184L756,185L758,185L758,187L762,187L762,187L764,187L764,187L768,187L768,188L769,188L769,188L773,188L773,189L775,189L775,189L777,189L777,189L778,189L778,190L779,190L779,192L784,192L784,193L789,193L789,193L790,193L790,194L791,194L791,194L792,194L792,194L794,194L794,195L801,195L801,195L802,195L802,197L803,197L803,201L808,201L808,202L809,202L809,205L813,205L813,205L817,205L817,205L821,205L821,207L822,207L822,208L823,208L823,207L824,207L824,208L826,208L826,209L827,209L827,210L828,210L828,212L829,212L829,214L831,214L831,215L833,215L833,219L834,219L834,221L835,221L835,222L837,222L837,224L839,224L839,225L840,225L840,226L841,226L841,228L842,228L842,228L843,228L843,229L845,229L845,230L846,230L846,231L847,231L847,232L849,232L849,232L850,232L850,233L851,233L851,233L853,233L853,233L858,233L858,234L859,234L859,234L860,234L860,236L861,236L861,237L863,237L863,238L865,238L865,239L866,239L866,240L867,240L867,241L869,241L869,242L870,242L870,243L871,243L871,244L872,244L872,245L875,245L875,245L876,245L876,246L879,246L879,247L880,247L880,247L885,247L885,248L886,248L886,247L888,247L888,249L889,249L889,250L892,250L892,250L893,250L893,251L895,251L895,252L897,252L897,253L898,253L898,255L900,255L900,257L901,257L901,258L904,258L904,259L906,259L906,259L907,259L907,260L908,260L908,261L909,261L909,263L910,263L910,263L912,263L912,264L914,264L914,265L915,265L915,269L916,269L916,270L921,270L921,272L922,272L922,272L923,272L923,272L924,272L924,272L925,272L925,274L928,274L928,275L929,275L929,277L933,277L933,278L935,278L935,280L936,280L936,282L937,282L937,283L938,283L938,285L941,285L941,285L943,285L943,286L944,286L944,288L945,288L945,289L947,289L947,290L949,290L949,292L951,292L951,293L952,293L952,294L954,294L954,295L955,295L955,296L956,296L956,296L957,296L957,298L958,298L958,301L959,301L959,302L961,302L961,303L962,303L962,307L963,307L963,308L964,308L964,310L966,310L966,311L967,311L967,312L968,312L968,313L969,313L969,315L970,315L970,317L971,317L971,321L972,321L972,323L976,323L976,326L977,326L977,327L978,327L978,329L979,329L979,329L980,329L980,329L981,329L981,330L982,330L982,332L983,332L983,333L984,333L984,334L987,334L987,336L988,336L988,337L989,337L989,338L992,338L992,339L993,339L993,340L995,340L995,341L996,341L996,342L997,342L997,342L998,342L998,343L999,343L999,345L1000,345L1000,346L1001,346L1001,345L1002,345L1002,346L1004,346L1004,348L1005,348L1005,350L1006,350L1006,351L1007,351L1007,353L1009,353L1009,353L1010,353L1010,353L1012,353L1012,355L1013,355L1013,358L1014,358L1014,359L1015,359L1015,360L1016,360L1016,360L1017,360L1017,361L1020,361L1020,363L1022,363L1022,364L1023,364L1023,364L1024,364L1024,365L1025,365L1025,367L1026,367L1026,368L1027,368L1027,369L1028,369L1028,370L1029,370L1029,371L1030,371L1030,373L1031,373L1031,373L1032,373L1032,374L1034,374L1034,378L1035,378L1035,380L1036,380L1036,381L1037,381L1037,382L1038,382L1038,383L1039,383L1039,385L1040,385L1040,387L1041,387L1041,387L1043,387L1043,390L1044,390L1044,392L1046,392L1046,393L1047,393L1047,393L1048,393L1048,394L1049,394L1049,396L1050,396L1050,395L1051,395L1051,395L1052,395L1052,396L1053,396L1053,397L1055,397L1055,397L1056,397L1056,398L1057,398L1057,399L1058,399L1058,399L1059,399L1059,401L1061,401L1061,404L1062,404L1062,405L1063,405L1063,407L1064,407L1064,408L1065,408L1065,409L1066,409L1066,410L1067,410L1067,411L1068,411L1068,411L1069,411L1069,411L1070,411L1070,413L1071,413L1071,414L1073,414L1073,415L1075,415L1075,416L1076,416L1076,417L1077,417L1077,420L1078,420L1078,421L1079,421L1079,423L1080,423L1080,424L1081,424L1081,425L1082,425L1082,424L1084,424L1084,425L1085,425L1085,427L1086,427L1086,430L1088,430L1088,433L1089,433L1089,434L1090,434L1090,435L1091,435L1091,436L1092,436L1092,437L1093,437L1093,438L1094,438L1094,440L1095,440L1095,445L1095,445" tabindex="0">
						<title>l_r9gn1qpd-9d8eewld-0nwoqwkl.jqzpx3gl</title>
					</path>
				</g>
				<g style="--index: 7;">
					<path d="M0,0L89,0L89,132L95,132L95,133L103,133L103,134L130,134L130,135L146,135L146,134L159,134L159,135L184,135L184,136L226,136L226,137L227,137L227,138L250,138L250,139L257,139L257,140L282,140L282,141L292,141L292,142L294,142L294,143L300,143L300,144L330,144L330,144L333,144L333,143L356,143L356,145L363,145L363,146L371,146L371,147L384,147L384,147L385,147L385,147L392,147L392,148L397,148L397,149L401,149L401,150L403,150L403,151L413,151L413,152L425,152L425,152L433,152L433,153L453,153L453,153L458,153L458,154L459,154L459,155L474,155L474,155L486,155L486,156L492,156L492,157L495,157L495,158L501,158L501,159L504,159L504,160L513,160L513,159L532,159L532,159L538,159L538,160L540,160L540,161L550,161L550,162L551,162L551,163L555,163L555,164L561,164L561,165L563,165L563,165L566,165L566,165L571,165L571,166L580,166L580,167L589,167L589,169L594,169L594,170L595,170L595,172L598,172L598,172L601,172L601,173L605,173L605,173L611,173L611,172L616,172L616,171L618,171L618,172L619,172L619,173L621,173L621,173L622,173L622,174L623,174L623,174L627,174L627,175L628,175L628,175L630,175L630,176L635,176L635,177L639,177L639,178L675,178L675,179L690,179L690,178L696,178L696,178L702,178L702,178L707,178L707,180L714,180L714,181L719,181L719,181L723,181L723,182L727,182L727,182L729,182L729,183L731,183L731,184L744,184L744,185L752,185L752,185L754,185L754,186L763,186L763,187L774,187L774,188L778,188L778,189L781,189L781,190L794,190L794,190L807,190L807,191L810,191L810,192L817,192L817,193L820,193L820,194L830,194L830,195L848,195L848,197L849,197L849,198L850,198L850,199L855,199L855,199L858,199L858,199L864,199L864,199L867,199L867,200L873,200L873,201L884,201L884,202L892,202L892,203L893,203L893,204L894,204L894,205L897,205L897,205L909,205L909,207L915,207L915,207L925,207L925,207L927,207L927,207L929,207L929,207L930,207L930,207L935,207L935,206L944,206L944,207L947,207L947,208L953,208L953,210L957,210L957,210L959,210L959,211L961,211L961,212L962,212L962,211L966,211L966,212L973,212L973,213L986,213L986,215L989,215L989,216L991,216L991,217L995,217L995,217L996,217L996,218L1000,218L1000,219L1002,219L1002,221L1004,221L1004,221L1005,221L1005,222L1009,222L1009,222L1013,222L1013,223L1016,223L1016,223L1026,223L1026,224L1028,224L1028,225L1029,225L1029,225L1030,225L1030,225L1035,225L1035,226L1036,226L1036,227L1038,227L1038,227L1039,227L1039,227L1043,227L1043,228L1047,228L1047,229L1052,229L1052,229L1053,229L1053,230L1055,230L1055,230L1056,230L1056,230L1057,230L1057,231L1058,231L1058,232L1059,232L1059,234L1069,234L1069,235L1070,235L1070,235L1072,235L1072,236L1073,236L1073,236L1078,236L1078,237L1079,237L1079,238L1080,238L1080,239L1082,239L1082,240L1084,240L1084,241L1085,241L1085,241L1086,241L1086,242L1087,242L1087,243L1094,243L1094,243L1095,243" tabindex="0">
						<title>l_r9gn1qpd-9d8eewld-0nwoqwkl.p12dx87q</title>
					</path>
				</g>
				<g style="--index: 8;">
					<path d="M0,0L570,0L570,2L724,2L724,2L779,2L779,2L963,2L963,2L972,2L972,2L992,2L992,2L1001,2L1001,2L1006,2L1006,2L1007,2L1007,2L1009,2L1009,2L1033,2L1033,2L1056,2L1056,2L1057,2L1057,2L1070,2L1070,2L1095,2" tabindex="0">
						<title>l_ldykv1zw-rkl3znrk-wl3059ol.9qj0kxel</title>
					</path>
				</g>
			</g>
		</svg>
	</foreignObject>
</svg>
